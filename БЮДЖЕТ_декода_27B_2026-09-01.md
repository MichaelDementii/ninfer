# Бюджет раунда декода на 27B-nvfp4, поядерно. Замерено 1 сентября 2026

Кампания меряла декод на 35B-A3B; на **27B-nvfp4** поядерного бюджета не было, а очередь
кандидатов из разбора чужих движков планируется именно под него. Вот он.

Инстанс 49380254, RTX 5090, `qwen3_8_27b_nvfp4.ninfer`, KV int8, `-pg 2048,512
--mtp-draft-tokens 3 --lm-head-draft`, `nsys -t cuda --cuda-graph-trace=node`.
Прогон декодо-доминирующий: 512 сгенерированных токенов против промпта в 2048.

**Итого 4897.3 мс GPU, 230 676 пусков ядер.**

| доля | ядро | пусков | мкс/пуск |
|---:|---|---:|---:|
| **21.8%** | `nvfp4_linear_swiglu_small_t` | 15 624 | 68.3 |
| **14.6%** | `fp8_small_t` (A) | 13 392 | 53.3 |
| **11.6%** | `nvfp4_small_t` | 15 624 | 36.5 |
| 7.9% | `fp8_small_t` (B) | 17 856 | 21.7 |
| 5.3% | `fp8_mma` | 2 240 | 116.2 |
| 4.4% | `fp8_small_t` (C) | 4 464 | 48.8 |
| 4.3% | `fp8_a16_small_t_mma` | 279 | 751.6 |
| 3.7% | `q4_rowsplit_gemv` | 846 | 213.4 |
| 2.7% | `rmsnorm_cta_bf16x2` | 40 608 | 3.2 |
| 2.6% | `fp8_small_t` (D) | 2 232 | 57.1 |
| 1.7% | `recurrent_record` (GDN) | 13 392 | 6.3 |
| 1.3% | `w8_small_t_mma` | 567 | 115.6 |
| **1.1%** | `causal_attention_small_t_i8_tiled` | 4 624 | 12.2 |

## Три числа, которые меняют планирование очереди

**1. Внимание — это единицы процентов, а не десятки.** При коротком контексте 1.1%; при 16k,
замерено отдельно, **3.4%** (38.45 мс из ≈1144 мс декода). Всё, что трогает декодное внимание,
упирается в этот потолок. Пересчёт для очереди — в `14_decode_split_policy/STATUS.md`.

**2. Половину раунда съедают три ядра**, и все три — вес, читаемый один раз:
`nvfp4_linear_swiglu_small_t` + `fp8_small_t` (A) + `nvfp4_small_t` = **48.0%**.

**3. Мелких ядер очень много.** `rmsnorm_cta_bf16x2` — 40 608 пусков по 3.2 мкс. Это ровно та
картина, под которую написан закон раунда: 3.3 мкс на запуск плюс байты/1.75 ГБ/мкс. Нормы
уже правит пакет 08.

## Где именно теряет главное ядро

`nvfp4_linear_swiglu_small_t` читает за пуск 89.1 МБ кодов плюс 11.1 МБ масштабов = **100.2 МБ**
весов MLP одного слоя. Операторный бенч, политика A16 (это декодная политика):

| T | слитый SwiGLU | ГБ/с | % пика 1792 |
|---:|---:|---:|---:|
| 1 | 73.73 мкс | 1360.6 | 76% |
| 2 | 77.79 | 1290.1 | 72% |
| **4** | **81.92** | **1226.2** | **68%** |
| 8 | 104.48 | 963.2 | 54% |
| 16 | 172.10 | 586.8 | 33% |

**Рабочая точка — T=4** (верификация mtp3 идёт одним проходом по всем 64 слоям; черновые шаги
идут через голову MTP, а не через тело модели: 15 624 пуска на ~256 раундов = 61 на раунд,
то есть примерно один на слой). Там ядро на **68% пика**, а кампания уже мерила, что крупное
ядро на этой карте берёт 1698 ГБ/с. Зазор до него — **28%**, то есть до **6% раунда**.

Байты веса от T не зависят, а время растёт: 73.7 → 172.1 мкс от T=1 к T=16 при том же чтении.
Регистры показывают, где перелом: T≤4 идёт `SharedPhase` при **REG 72**, T≥5 переключается на
`TokenPacked` и прыгает на **REG 115…128**. Нашей рабочей точки это не касается, но при более
широкой спекуляции коснётся.

## Две оси закрыты замером

Обе — «неиспользуемое значение в уже готовом перечислении», то есть ровно тот случай, который
обычно стоит переоткрывать. Здесь оба раза автор оказался прав.

**`Nvfp4CodeCache::Streaming` (`ld.global.cg`) — проигрыш.** Значение есть в перечислении, но ни
одна из пяти настроенных заготовок его не берёт. Палиндром, слитый SwiGLU:

| | T=1 | T=2 | T=4 |
|---|---:|---:|---:|
| `Default` | 73.728 | 77.79 | 80.91 |
| `Streaming` | 73.728 | 83.97 (**+7.9%**) | 88.06 (**+8.8%**) |

`linear_add` не сдвинулся вовсе. Гипотеза «веса читаются один раз, L1 им не нужен» не
подтвердилась.

**`kValuesPerLane = 32` (загрузки по 16 Б) — проигрыш, и крупный.** Заготовки берут 16, а
значение 32 поддержано кодом (`ld.global.cg.v4.u32`) и не используется нигде. Палиндром:

| | T=1 | T=2 | T=4 | T=8 | T=16 |
|---|---:|---:|---:|---:|---:|
| 16 значений на нить | 73.73 | 77.81 | 81.92 | 105.49 | 172.06 |
| 32 значения на нить | 73.73 | 79.87 | 104.45 (**+27%**) | 133.12 (**+26%**) | 241.66 (**+40%**) |

Комментарий автора над заготовками — «measured occupancy/register crossovers, not semantic
frontiers» — подтверждается: заготовки подобраны, и обе свободные ручки хуже умолчания.

⚠️ Правка `kValuesPerLane` попала только в две из четырёх специализаций (`sed` не совпал с
остальными), поэтому числа `linear_add` в этой таблице — от неизменённого ядра и в счёт не идут.
Вывод строится только на слитом SwiGLU, который правился напрямую.

## Что отсюда следует делать

Зазор у главного ядра **разобран ниже**: это не расписание, не занятость и не арифметика, а
неперекрытая обвязка распаковки. Ядро только с подвозом построено, см. дописанный раздел.

## Сырое

* Профиль: `/tmp/decpure.nsys-rep` на сервере, сводка выше снята
  `nsys stats --report cuda_gpu_kern_sum`
* Ветки экспериментов на сервере: `exp/codecache`, `exp/vpl32` (обе — отрицательный результат,
  в подачу не идут)

---

# Дополнено: где именно лежат 31% главного ядра. Ядро только с подвозом

Построено ядро с той же сеткой, тем же размером блока, той же раскладкой чтений и тем же числом
фаз, что у `nvfp4_linear_swiglu_small_t` при T=4 — 2176 CTA по 256 нитей, восемь варпов, две
строки на варп, десять фаз по 16 значений на нить, **счёта нет** (`90_harness/decsupply.cu`).

⚠️ Две ловушки, на которых первые два прогона дали бессмыслицу, обе стоит помнить:

1. **Сток должен быть недоказуемо недостижим.** С `if (threadIdx.x == 0xffffu)` компилятор
   доказал, что запись не нужна, и выбросил все чтения: получилось 24 928 ГБ/с, то есть
   четырнадцать пиков HBM. Помогает `if (accumulator != accumulator)`.
2. **Буфер должен быть больше L2.** Веса одного слоя — 100.3 МБ при L2 в 96 МБ, поэтому
   повторные проходы по одному буферу шли из кеша: 5135 ГБ/с, почти три пика. В движке такого
   не бывает — между двумя обращениями к слою через L2 проходит 64 слоя. Лечится восемью
   копиями буфера с ротацией.

После обеих поправок:

| вариант | время | ГБ/с | от ядра |
|---|---:|---:|---:|
| только коды и масштабы | 56.24 мкс | 1783 | 68.6% |
| + барьер на каждой фазе | 56.33 | 1780 | 68.8% |
| + стейджинг активаций и барьер | 56.29 | 1781 | 68.7% |
| **настоящее ядро при T=4** | **81.92** | 1226 | 100% |

**Три вывода, и они меняют направление работы по этому ядру.**

1. **Подвоз уже упёрся в железо: 1783 ГБ/с — это 99.5% пика HBM.** Улучшать путь памяти здесь
   нечего, и приёмы пакетов 12 и 13 сюда не переносятся: там ядро стояло на 54% и лечилось
   формой дескриптора, тут оно на 99.5%.
2. **Барьер и стейджинг активаций бесплатны.** 56.33 против 56.24 — разница внутри шума. Гипотеза
   «`__syncthreads()` внутри цикла фаз режет поток весов» **опровергнута**.
3. **31% времени ядра (25.6 мкс на пуск) — это счёт, который не перекрывается подвозом.**
   Разбор SASS тела при T=4: 296 `FFMA` полезной работы против 153 `SHF` + 72 `PRMT` + 36 `LOP3`
   + 77 `MOV` = 338 инструкций распаковки байт и 68 `HADD2` + 40 `HFMA2` + 40 `F2FP` = 148 на
   преобразование. То есть на каждую полезную FMA приходится примерно 1.6 инструкции обвязки.

Само преобразование fp4 уже аппаратное: `static_cast<float2>(__nv_fp4x2_e2m1)` разворачивается
в одну `cvt.rn.f16x2.e2m1x2` (`cuda_fp4.hpp:428`). Дорого не оно, а то, что вокруг: байт
достаётся из 32-битного слова по одному сдвигом, и на каждую пару значений идёт отдельный путь.

## Четыре ручки расписания — четыре проигрыша

Все замерены палиндромом на слитом SwiGLU, политика A16.

| ручка | умолчание | проба | T=4 | T=16 |
|---|---|---|---:|---:|
| `Nvfp4CodeCache` | `Default` | `Streaming` (`ld.global.cg`) | **+8.8%** | — |
| `kValuesPerLane` | 16 | 32 (загрузки по 16 Б) | **+27%** | **+40%** |
| `kAccumulatorChains` + `kPhaseUnroll` | 1 и 1 | 4 и 2 | **+7.5%** | **+787%** |

Последняя строка объясняется прямо: `cuobjdump -res-usage` при T=16 показывает `STACK:616`
против `STACK:0` у умолчания — регистры поехали в локальную память.

**Вывод: пространство расписаний этого ядра исчерпано, комментарий автора над заготовками
(«measured occupancy/register crossovers, not semantic frontiers») подтверждён с четырёх сторон.**
Оставшиеся 31% структурны: их снимет только другая раскладка распаковки — меньше инструкций
обвязки на одну FMA, — а не параметр расписания. Это уже переписывание ядра, и это решение
автора, а не наше.

**Потолок направления, если обвязку убрать целиком:** 81.92 → 56.3 мкс, то есть −31% на ядре,
которое занимает 21.8% раунда декода = **−6.8% раунда**. Это самая крупная одиночная цель в
декоде из всего, что кампания измерила.

## Сырое

* `90_harness/decsupply.cu` — ядро только с подвозом, три варианта
* `90_harness/cvtprobe.cu` — проверка, что распаковка fp4 уже аппаратная
* Ветки экспериментов на сервере (все отрицательные): `exp/codecache`, `exp/vpl32`, `exp/chains`
