# Внимание префилла: MMA — треть ядра. Замер абляцией, 1 сентября 2026

**Этот файл отменяет два моих собственных вывода за сегодня.** Оба были сделаны не тем прибором,
подробности ниже. Число, которому можно верить, получено абляцией и воспроизводится.

Инстанс 49380254, RTX 5090, ветка `all123` (пакеты 11+12+13), геометрия `d256-h24-kv4`,
контекст 8192, eager, палиндром.

## Замер

В `mma_bf16` (`src/ops/common/mma.cuh`) тензорная инструкция заменена дешёвой зависимостью,
которая держит все шесть операндов живыми, чтобы `ldmatrix` и стейджинг не были выброшены
оптимизатором. Результаты в таком билде намеренно неверны — он существует только чтобы
измерить всё, кроме MMA.

| W | с MMA | без MMA | доля MMA |
|---:|---:|---:|---:|
| 4096 | 5192.5 / 5195.5 мкс | 3441.8 / 3439.5 | **33.7%** |
| 8192 | 12084.0 / 12076.8 | 8000.9 / 8001.8 | **33.8%** |

Совпадение на двух ширинах до десятой процента. Оценка снизу: замена стоит четыре `FADD` и
несколько `xor` на вызов, значит настоящая доля MMA чуть выше 34%.

**Отсюда потолок f16-аккумулятора.** Он удваивает ярус PV (255.3 → 506.6, замерено
`90_harness/mmapeak4.cu`). Половина от 33.8% — это **≈17% ядра**, а ядро занимает 27.7%
префилла на 27B, то есть **до 4.7% префилла**. Направление живое.

## Отменяю вывод №1: «ядро берёт 92.6% потолка, кандидат 12 закрыт»

Я взял знаменателем то, что печатает бенч — 209.5. Это **спецификационное** число
(`kFp8Fp32AccumulatePeak / 2`), а не измеренное. Измеренный сырой пик bf16 на этой карте —
**266.0**, кормимый ≈263 (у fp8 отношение кормимого к сырому 526.9/531.9 = 99.1%).

Против измеренного: 194.08 / 263 = **73.8%**, а не 92.6%. Запас есть, и это ровно то, что
независимо посчитал напарник (75% от 255.3).

⚠️ **Правило, которое я нарушил дважды за вечер, и оно уже было записано:** бенч печатает
проценты от спецификации, а решения принимаются от измеренного пика. Пересчёт:
`TC_% × 419 / 530.8` для fp8, аналогично `× 209.5 / 266` для bf16.

## Отменяю вывод №2: «ярус ни при чём, потому что int8 с вчетверо дешёвым QK даёт всего −2.9%»

Замер верный, вывод — нет. Числа (палиндром, воспроизводимость 0.1%):

| W | bf16 (QK@266) | int8 (QK@1026.7) | fp8 (QK@531.9) |
|---:|---:|---:|---:|
| 4096 | 5197.6 мкс | 5047.4 (−2.9%) | 4715.6 (−9.3%) |
| 8192 | 12082.0 | 11715.9 (−2.9%) | 10951.7 (−9.4%) |

**Ошибка в том, что это разные ядра, а не одно ядро с разным ярусом.** `prompt_i8` построен
иначе: производители и потребители, матрица P через общую память. Архитектурные накладные этой
схемы съедают выигрыш от вчетверо более дешёвого QK, и в сумме остаётся 2.9%. Абляция, где
меняется **только** инструкция внутри одного и того же ядра, показывает настоящую долю: 34%.

Это же снимает мой довод против кандидата 12 в прошлом файле: **сравнение bf16 против int8 не
годится ни для яруса, ни для d-split.**

## Что теперь со статусом кандидатов

| | было у меня | стало |
|---|---|---|
| 1, ярус PV (маршруты А и Б) | «не поддержан: ядро не тензорно-ограничено» | **поддержан замером**: MMA 34% ядра, потолок ≈4.7% префилла |
| 12, d-split/занятость | «закрыт: 92.6% потолка» | **НЕ ЗАКРЫТ**: знаменатель был спецификационный. 66% ядра — не-MMA, и занятость может их прятать. Свип фактора d-split внутри `prompt_i8` (8/16/32 варпа) и потолок по регистрам остаются в силе как отдельные факты |

## Что остаётся верным из прежних замеров

* Свип d-split внутри `prompt_i8`: 8 варпов дают 76.6% против 97.5% у 16, а 32 варпа не
  запускаются (1024 нити × REG:120 = 122 880 регистров против 65 536). Потолок по варпам — 17.
* `ОТВЕРГНУТО_внимание_fp8_KV.md`: перебалансировка варпов 8 QK / 8 PV → 4 QK / 12 PV даёт
  **на 5.2% хуже**. Это про баланс варпов, а не про ярус, и остаётся в силе.
* `logical GB/s` у этих ядер 1000–1800% от DRAM-полосы: трафик идёт через кеш и shared.
  Это по-прежнему означает, что **66% не-MMA — это память, а не счёт**, и что снимать их
  надо трафиком, а не ярусом.

## Что это значит для черновика напарника

Его заметка автору (`РАЗБОР_чужих_движков/ЧЕРНОВИК_автору_про_f16_аккумулятор.md`) утверждает,
что рычаг — ярус, а не занятость. **На нашей модели это подтверждается абляцией: 34% ядра.**
Его собственное число (75% от 255.3 на 35B) и моё (73.8% от 266 на 27B) сходятся.

⚠️ Одна поправка к его тексту: там написано «occupancy is not the lever there — it is already
close to its ceiling». Про занятость у нас **нет** замера, который это подтверждал бы: 73.8%
от яруса не означает, что занятость ни при чём, потому что 66% времени вообще не в MMA.
Формулировку лучше сузить до того, что действительно измерено.

## Сырое

* Ветка абляции на сервере: `ablate` (в подачу не идёт), бинарь `/root/bins/AB_attn`
* Неабляционный бинарь того же дерева: `/root/bins/W16_attn`

---

# Дополнено: ярус подтверждён ровно вдвое, но узкое место — регистры

## Ярус f16-аккумулятора: ×2.00, замерено на этой карте

`90_harness/accpeak.cu` — три формы одной и той же `m16n8k16`, всё в регистрах, цепочка из 32 MMA
в теле цикла (то же правило приёмки, что у `mmapeak`):

| форма | время | TOP/s | отношение |
|---|---:|---:|---:|
| `f32.bf16.bf16.f32` | 53.83 / 53.82 мс | 264.9 / 265.0 | ×1.000 |
| `f32.f16.f16.f32` | 53.83 | 264.9 | ×1.000 |
| **`f16.f16.f16.f16`** | **27.00 / 26.86** | **528.2 / 530.9** | **×1.994 / ×2.004** |

**Число напарника (506.6 против 255.3) подтверждено независимо.** Заодно видно, что операнды
ни при чём: bf16 и f16 с f32-аккумулятором идут ровно одинаково, удваивает именно аккумулятор.

## И сразу главная ловушка: наивная вставка проигрывает

Зонд `90_harness/patch_f16probe.py` ставит ту же инструкцию в цикл PV без перестройки: то же
число инструкций, те же операнды, тот же конвейер `ldmatrix`, один общий двухрегистровый буфер.
Результаты неверны намеренно — мерится только цена.

| | W=4096 | W=8192 |
|---|---:|---:|
| как есть | 5200.1 / 5195.6 мкс | 12082.9 / 12082.0 |
| зонд с f16-аккумулятором | 5240.4 / 5240.7 (**+0.8%**) | 12190.3 / 12191.1 (**+0.9%**) |

**Причина видна в ресурсах: `REG:255 STACK:56` против `REG:254 STACK:0`.** Ядро уже стоит вплотную
к потолку регистров, и любая добавка уезжает в локальную память. Спилл съедает весь выигрыш
яруса и ещё немного сверху.

## Что это значит для реализации

Рычаг настоящий (ярус ×2, доля MMA 33.8%, потолок ≈17% ядра = 4.7% префилла), но **упирается он
не в ярус, а в регистровый бюджет**. Арифметика бюджета:

* сейчас `acc[PVNt][4]` при `PVNt = 32` — это **128 регистров** только под аккумулятор, и ядро
  уже на 254;
* у f16-формы аккумулятор **два регистра вместо четырёх**, но f32-накопитель между блоками ключей
  никуда не девается, поэтому «в лоб» получается 128 + буфер, то есть строго хуже;
* поэтому перестройка обязательна: n снаружи, k внутри, чтобы жил буфер только текущей пары
  n-плиток — четыре регистра вместо шестидесяти четырёх. Это ровно то, что делает напарник на
  `perf/pv-f16acc-on-author`.

**Вывод для его заметки автору: цифру про ярус можно ставить смело, она подтверждена дважды.
Но стоит добавить, что наивная вставка даёт −0.8%, и что вся сложность в регистрах.** Автору это
сэкономит первый неудачный подход.

## Checks not run

* Честная реализация с перестройкой цикла и сворачиванием в f32 на границе блока ключей мной
  **не написана** — она есть у напарника, и дублировать её я не стал.
* Точность (условие диапазона `|buffer| <= Bc * max|V|`, то есть `max|V| <= 1023` при `Bc = 64`)
  не проверялась замером ни в каком виде.
