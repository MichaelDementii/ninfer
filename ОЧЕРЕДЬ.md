# Очередь заявок по префиллу: состояние на 2026-08-29, 12:30

База — `origin/master` = **`1fc1cb76`**. **Обе первые заявки поданы напарником и открыты:**

| PR | что | размер | состояние |
|---|---|---|---|
| [#112](https://github.com/Neroued/ninfer/pull/112) | W8 row-split | 1 файл, +25/-35 | OPEN/MERGEABLE, ревью Codex — **без замечаний** |
| [#113](https://github.com/Neroued/ninfer/pull/113) | NVFP4 SwiGLU | 1 файл, +9/-4 | OPEN/MERGEABLE, ревью Codex — **одно замечание P2** |

Мейнтейнер не отвечал ни там, ни там. Оба слота заняты, поэтому кандидаты 3-5 готовятся, но не
подаются.

**Заметки напарнику по обоим PR — `ОТВЕТЫ_ПО_PR_2026-08-29.md`.** Замечание по #113 разобрано там
и в `02_nvfp4_swiglu/FOLLOWUP_codex.md` — бот заметил, что `ctest` не
исполняет ни одной новой ширины, потому что список случаев A4 кончается на 1024, а 1024 была фьюзед
и до правки. Замечание верное; правка — добавить 256 в `kA4Cases`.

| # | папка | ветка | статус | размер | побитово |
|---|---|---|---|---|---|
| 1 | `01_w8_rowsplit` | `beced522` | **готово, 3 круга ревью** | 1 файл, +25/−35 | да |
| 2 | `02_nvfp4_swiglu` | `436aba7b` | **готово, 2 круга ревью** | 1 файл, +10/−4 | нет, и не может быть |
| 3 | `03_moe_stage_from_x` | `7efba321` | **готово: тексты, bundle, make_pr** | 2 файла, +76/−20 | да |
| 4a | `06_moe_slice_max` | `b696680e` | **готово: тексты, bundle, make_pr** | 1 файл, +1/−1 | да |
| 4bc | `05_moe_bn128` | `786db14f` | **готово: тексты, bundle, make_pr** | 2 файла, +89/−43 | да |
| 5 | `04_pv_fp16` | `a9b6650d` (авторство `sergiuszm`) | **не PR, а комментарий в #27**; кампания идёт | 2 файла, +34 | **нет** |
| 7 | `07_pv_fp16_bf16_route` | — | **не начинать**: идея автора #27, первый ход за ним | — | нет |
| 8 | `08_rmsnorm_shape_prefetch` | `74dcc514`, **в форке** | замерено, ждёт слота | 3 файла, +124/−8 | часть 1 да, часть 2 нет |
| 9 | `09_mtp_sampled_draft` | `ce71f787`, **в форке** | замерено, ждёт слота и `eval/` | 18 файлов, +425/−34 | под `--greedy` да |

---

## 1 и 2 — готовы

Детали в `01_w8_rowsplit/RUNBOOK.md` и `02_nvfp4_swiglu/RUNBOOK.md`. Порядок: сначала Issue по W8,
дождаться ответа, потом PR; NVFP4 не открывать, пока первый не закрыт или пока не станет ясно, что
слот свободен.

`ctest` на новой базе **зелёный на обеих ветках и на master**: 94 теста, 0 падений, 1 пропуск
(нужен второй артефакт 27B, которого на этой машине нет). Наш issue **#105 закрыт его же правкой**
`fd48e2fa`, которая переписала оракул теста — то есть ожидание в тесте было неверным, а не движок.

---

## 3 — стейджинг MoE из `x` (`9c67c692`)

Ветка `perf/moe-stage-from-x` создана, три конфликта с `3a61ef3f` разрешены скриптом
`/root/resolve_b1.py` (лежит в `90_harness/`). Суть конфликта: master разделил то, что раньше было
одним буфером — `select_count` пишет ранг в `local_rank`, а `gather` публикует обратную карту в
`packed_index`, алиасящийся на мёртвый префикс `tile_counts`. Наше индексное ядро читало ранг
из `packed_index` на месте; после разрешения оно читает `local_rank`.

**Побочный эффект в нашу пользу, который стоит сказать в заявке:** правка убирает перекрытие
чтения-записи, о котором предупреждает его собственный комментарий в `3a61ef3f` — индексное ядро
читает ранг и пишет карту из одного потока, и две величины никогда не живут в одном буфере.

**Перемерено на `1fc1cb76`.** Операторный бенч, два прохода: q4-кодеки ×1.038…1.050, нетронутый
`w8-w8` — 0.9965…1.0045, то есть шумовой пол ±0.45%. `ctest` 94/94. `workspace_bytes` одинаков на
всех ширинах. Все генерации побитово совпадают. Сквозняк см. `03_moe_stage_from_x/`.

**Из четырёх собранных форм подаём регистровую** (`VARIANTS.md`): она быстрее остальных своего
объёма в обоих проходах и **оставляет shared ровно как на master**, 33792 × 3 = 101376 при
запрошенном у драйвера `sharedMemPerMultiprocessor` = 102400.

**Что не забыть:** `grouped_io` остаётся **не ради W8-кодека**, а потому что в него пишет routed
down — это lifetime-union, который заявлен в комментарии самого автора. Воркспейс **не падает**,
экономится трафик. И nsys поправил модель: сборщик исчезает целиком, а `q4_gate_up` **не дешевеет**
(705.44 → 705.05 мс). Весь выигрыш — ровно трафик сборщика.

---

## 4 — потолок среза и ширина задания: две заявки, не одна цепочка

**4a — готова**, папка `06_moe_slice_max`, ветка `b696680e`, одна строка. Операторный бенч сам себе
контроль: девять строк из двенадцати обязаны читать 1.000 и читают 0.9982…1.0010, а на 8192 все три
кодека дают ×1.068…×1.084. SASS не двигается вообще. Главное открытие: **пик арены движка не
меняется ни на одной ширине чанка** — операторный запрос удваивается, но MoE не крупнейший
потребитель арены. Разбор в `06_moe_slice_max/NEXT.md`.

**4bc — готова**, ветка `786db14f`, написана заново на master без `NINFER_MOE_BN` и без макросов
запуска. Оператор ×1.059…1.069 на 4096 и 8192, два контроля по построению (1024/2048 ниже порога и
кодек `w8-w8`, который идёт через другие ядра) дают 0.9981…1.0015. Воркспейс не меняется вообще.
Сквозняк +2.11% и +1.73% на чанке 8192, контроль на чанке 1024 −0.02%.

**Вместе с 4a они дают больше, чем по отдельности:** +5.69% и +4.53% против произведения +4.19% и
+3.37%. Потолок делает колонки вдвое длиннее, широкая плитка — то, чему есть чем эти колонки
заполнить. И они конфликтуют по соседним строкам заголовка, так что второму нужен ребейз.

Подача по-прежнему не «ускорение префилла», а **«снятие внутреннего потолка для того, кто уже выбрал
широкий чанк»**, с дельтой по памяти в явном виде.

---

## 5 — PV на f16 (`3ccb531d`)

Самая крупная одиночная правка по проценту: **+4.3 / +6.9 / +9.2%** префилла на 33K / 67K / 115K,
растёт с длиной, потому что живёт во внимании. И самая сложная в подаче, по трём причинам.

**Не побитово, и только на `--kv-dtype int8`.** Все наши сквозные кампании шли на bf16-KV, поэтому
для неё нужна отдельная кампания на int8.

**Стенд точности уже есть** — `БЕНЧМАРКИ_три_конфигурации_2026-08-24.md`: EvalScope, AIME25 (30) и
GPQA-Diamond (198) целиком. Конфигурация C = int8-KV + PV fp16 дала AIME25 **90.0** против 90.0 у
стока и GPQA **84.85** против 84.34. Ценность стенда не в «C не хуже», а в измеренном шумовом поле:
конфигурации A и B отличаются только порядком суммирования, и разрыв между ними оказался **ровно
нулевым** на обоих датасетах. Честное ограничение, которое надо написать самим: на 198 вопросах
одна сигма ≈ 2.5%, стенд отвергает деградацию больше ~5%, но не подтверждает отсутствие 1–2%.

**Это не наша правка целиком, и подавать её как PR, скорее всего, вообще не надо.** Прочитал
issue #27 (открыт 15 августа, автор `sergiuszm`, Sergiusz Michalik). Там не идея — там **готовая
реализация**: он даёт ссылку на свой коммит `sergiuszm/ninfer-5090@4483c820`, те самые 34 строки в
двух файлах, и пишет, что они ложатся на master чисто. Наш `3ccb531d` — это черри-пик его работы, и
авторство в коммите стоит его, как и должно.

**Состояние issue на 29 августа: открыт, ноль комментариев, ответа мейнтейнера нет, связанного PR
нет.** Две недели тишины.

Из этого следует другой ход, и он лучше нашего прежнего. Открывать свой PR с чужим патчем — значит
встать между автором и мейнтейнером на его же заявке. Чего этой заявке действительно не хватает —
это **независимого воспроизведения на второй машине**, и вот это у нас есть. Поэтому:

* пишем **комментарий в #27** с нашими замерами, а не отдельный Issue и не PR;
* прямо говорим, что правка не наша, что мы её только проверили, и приводим свою конфигурацию —
  она отличается от его: он мерил операторные TFLOP/s (185.9 → 226.3 на 8K, 191.9 → 236.0 на 64K,
  192.5 → 229.6 на 128K) и сквозняк на 64K/128K/200K (+2.3 / +3.6 / +4.7%); мы мерили сквозной
  префилл на Qwen3.6-35B-A3B, int8-KV, чанк 4096: **+4.3 / +6.9 / +9.2%** на 33K / 67K / 115K;
* отвечаем **на его открытый вопрос** — он спрашивает, каким делать arch-guard: убрать совсем,
  сузить до `sm_120a` или оставить перечисление. У нас есть железо ровно этого класса, и ответ на
  этот вопрос — измеримая вещь, а не мнение;
* добавляем то, чего у него нет: стенд точности на генерации (AIME25 и GPQA-Diamond целиком) с
  измеренным шумовым полом.

Если после этого мейнтейнер попросит PR — первое слово за автором. Мы подаём, только если он
попросит нас, и тогда автор коммита не меняется.

**Проверено 29 августа: `3ccb531d` ложится на текущий master `1fc1cb76` чисто**, 2 файла, +34,
без конфликтов. Это был главный неизвестный по этому кандидату, и он закрыт.

Общее правило остаётся: если используем чужое — авторство сохраняем всегда.

**Ляжет ли на master — проверено, ложится.** `git cherry-pick 3ccb531d` на `1fc1cb76` проходит без
конфликтов: `src/ops/common/mma.cuh` +11 и
`src/ops/softmax_attention/dense/causal_cache/prompt_i8.cuh` +23. Две правки master в этих файлах
(`6183c9be` fp8 kv cache, `17a7275f` int8 kv hadamard rotation) не задели тронутые строки.

---

## Что усвоено за три круга ревью и стоит применять к 3, 4 и 5

1. **Заголовок обещает только то, за что отвечает тело.** Дважды ловили: x1.12, которую тело
   дезавуирует, и +2.4%, существующие только вне умолчательной конфигурации.
2. **Ни одного одиночного прогона, поданного как результат.** Каждое число — либо диапазон по
   проходам, либо явно помеченное «один проход».
3. **Ресурсную заявку проверять замером, а не цитатой из своих заметок.** «570 МБ арены» и
   «убирает материализацию» оба не пережили проверки в том виде, в каком были записаны.
4. **Возражение автора считать за него.** Где он сказал «широкий чанк не бесплатен» — самим
   привести цифру и сказать, что заявка её не уменьшает.
5. **Свои инструменты тоже врут.** `sass_diff.py` недосчитывал опкоды под униформным предикатом и
   льстил нашим же числам; нашли только потому, что перепроверяли.
6. **Гейт на 32 токена бесполезен** — 40 байт и два различных текста на все точки. Настоящий
   гейт — полная генерация со стопом.

---

## 8 и 9 — добавлено 31 августа

База сдвинулась: `origin/master` теперь **`feaf4dd0`**, не `1fc1cb76`. Обе новые ветки собраны
поверх неё и **уже лежат в форке** — `perf/rmsnorm-shape-prefetch` @ `74dcc514` и
`perf/mtp-sampled-draft` @ `ce71f787`. Подавать нельзя: оба слота заняты #112 и #113.

**8 — нормы.** Одна неразрывная цепочка из трёх частей: подъём загрузки весов до редукции,
специализация под d=5120, и выбор предзагрузки по форме запуска. Третья часть обязательна —
без неё первая вносит **+19.3%** на гейтованной пост-норме GDN при T=1024 (операторный бенч,
два прохода, оба порядка ног). В графе: нормы раунда 652.4 → 360.0 мкс (−44.8%), раунд −1.49%.
Контроль `dflash_hidden` в пределах ±1.5% — это шумовой пол бенча.
Правка обновляет копию предиката в `bench/ops/rmsnorm_bench.cu`: без этого бенч печатает имя не
того ядра, которое исполняет.

**9 — сэмплированный черновик MTP.** Приёмка 45.26 → 52.57%, ток/с +8.98% на прозе, цена
**+0.25%** времени раунда. Уровень заявки — **фаза запроса**, не оператор; это надо написать
первой строкой, потому что операторного выигрыша тут нет вовсе. Под `--greedy` обе ноги совпали
до токена — контроль вырождения.

**Что снято.** Слитый TMA-свиглу вне чанка 1024 был найден независимо и оказался **дублем
пакета 02** (тот же предикат, те же два места, PR #113 уже открыт, у напарника вариант шире —
от 256, а не от 1024). Заявка не подаётся; независимое воспроизведение (+2.02% на чанке 4096,
+2.38% на 8192, контроль на 1024 без изменений, другая машина и другая цель) лежит в
`08_rmsnorm_shape_prefetch/data/` и может пойти в тред #113 как подтверждение.

**Расхождение по `ctest`, которое стоит проверить перед подачей.** Выше записано, что на базе
`1fc1cb76` сюита зелёная, 94 теста, 0 падений. На **`feaf4dd0`** у меня падает
`ninfer_qwen3_6_frontend_test` (Subprocess aborted) — и падает **на чистом мастере тоже**, то есть
это не наши ветки. Либо поломка приехала с новой базой, либо разница в окружении (CUDA 13.1.115,
Ubuntu 24.04, инстанс 49380254). Перед подачей надо решить, как это описывать: как существующую
поломку дерева, как было сделано в #106.

**Незакрытое.** Штатный `eval/` (IFBench, AIME25, AIME26, GPQA-Diamond) запущен на ветке 9;
результаты дописываются в оба `STATUS.md`. Для 9 это критично: правка меняет то, что движок
сэмплирует, и иголка с побитовым сравнением под `--greedy` проверяют вырожденный режим, а не
рабочий.

---

## Дополнено 2026-09-01, вечер: база ушла на 111 коммитов, два кандидата закрыты как дубли

**Актуальный `upstream/master` — `da49c0d6`** («fix(runtime): exclude materialization sources from
pressure»). От `feaf4dd0`, на котором снят весь профиль кампании 08/09, он отстоит на **111
коммитов**, +147222/−32620 в 567 файлах.

### Что из этих 111 коммитов закрывает наши находки

| коммит | что | последствие |
|---|---|---|
| `92bb06eb` | write the gdn prefill convolution straight into q/k/v | **закрывает П7** — папка `10_gdn_prefill_conv_split`, заявку не готовлю |
| `00369f63` | register the fused TMA SwiGLU for every width its block accepts | закрывает П3, это же PR #113 |
| `93cdc264` | widen w8 rowsplit weight decode to eight codes per thread | это пакет `01`, merged |

П7 был реализован с нуля и замерен, прежде чем выяснилось, что он уже в апстриме. Числа оставлены
в `10_gdn_prefill_conv_split/STATUS.md` как независимое подтверждение величины правки автора:
операторно ×1.51…×1.78, копий device-to-device 2326 → 25, префилл −0.79%, рабочая область −80 МиБ.

### Коммиты из этих 111, которые меняют почву под нашими открытыми осями

* `6183c9be feat(attention): add fp8 kv cache support` и `17a7275f feat(attention): add int8 kv
  hadamard rotation` — прямо в области `04_pv_fp16` и `07_pv_fp16_bf16_route`. Обе оси надо
  пересматривать заново, старые замеры к этой базе не относятся.
* `e9c7046f refactor(gdn): unify recurrent kernel semantics` и `1965308a perf(runtime): prepare
  speculative gdn fold` — декодный профиль (вся серия Г) снимался до них.
* `02bd904e perf(runtime): keep mtp prefill token on device` — область пакета 09.
* `1fc1cb76 perf(linear): add fp8 w8a16 vocabulary gemm` — голова словаря, которую мы мерили как
  94.4% полосы.
* `89e297e1 feat(profiling): instrument engine execution with nvtx` — **полезно нам**: в движке
  теперь есть NVTX-разметка, сегменты в трассе больше не надо угадывать по именам ядер.

### Живы ли 08 и 09 на `da49c0d6`

Проверено грепом по свежему дереву: предзагрузки весов до редукции в `rmsnorm.cuh` нет,
сэмплированного предложения черновика в сэмплере нет. **Оба механизма всё ещё не в апстриме.**
Но замеры обоих сняты на `feaf4dd0` и на этой базе не действительны — их надо переснять.

### Порядок работ отсюда

1. Собрать `da49c0d6` и снять **свежий профиль декода и префилла** на нём.
2. Перенести и **перемерить 08**, затем **09** (у 09 ожидаются конфликты с `70434721`).
3. Дальше искать только на актуальной базе; перед реализацией любой находки — грепать механизм
   в свежем дереве.

---

## Новая заявка: `11_fp8_a8_tma` — fp8-GEMM префилла на TMA

Ветка **`perf/fp8-a8-tma` @ `745d8a06`**, собрана **на актуальном `upstream/master` `da49c0d6`**,
6 файлов, +438/−2. Полный разбор — `11_fp8_a8_tma/STATUS.md`.

**Коротко.** `fp8_mma_kernel` стейджит через `cp.async` и заперт на двух стадиях бюджетом shared;
свип из тринадцати расписаний это доказал ещё 31 августа. Соседний `nvfp4_w4a4_tma_kernel` решает
ту же задачу нитями-продюсерами под `mbarrier` и одним CTA на SM. Здесь тот же приём перенесён на
fp8; у fp8 он проще, потому что масштабы построчные.

| прибор | результат |
|---|---|
| оператор, T=4096, палиндром | 0.977 / 0.946 / 0.953; контроль nvfp4 **1.0000** |
| поузловая трасса, 4 трассы | fp8-GEMM **−7.45%**, всё остальное −0.75%, сегмент **−3.21%** |
| сквозные часы префилла | 4231.7 → **4091.4 мс** (**−3.31%**), 7744 → 8009 ток/с |
| декод (контроль) | 16.4631 → 16.4475, не затронут |
| жадная генерация | **побитово совпала** — порядок накопления по K не меняется |
| `ctest` | 97/98, падает только известный `ninfer_qwen3_6_frontend_test` |
| `clang-format` | 0 расхождений во всех шести файлах |

**Что осталось за скобками:** `linear_swiglu` (34816×5120, 4.9% GPU префилла) — там парная запись
строк и `RowPolicy`, которых у TMA-ядра нет. Отдельная работа.

**Заодно исправлена приборная ошибка:** бенч проекта печатает `TC_%` от паспортных 419 TFLOP/s, а
`mma.sync` в цикле на этой карте даёт 530.8. По первой шкале fp8 выглядел на 97.6% и запаса не
было видно; по второй он был на 77%. Любой процент из бенча проекта надо переводить.
