# Очередь заявок по префиллу: состояние на 2026-08-29, 12:30

База — `origin/master` = **`1fc1cb76`**. **Обе первые заявки поданы напарником и открыты:**

| PR | что | размер | состояние |
|---|---|---|---|
| [#112](https://github.com/Neroued/ninfer/pull/112) | W8 row-split | 1 файл, +25/-35 | OPEN/MERGEABLE, ревью Codex — **без замечаний** |
| [#113](https://github.com/Neroued/ninfer/pull/113) | NVFP4 SwiGLU | 1 файл, +9/-4 | OPEN/MERGEABLE, ревью Codex — **одно замечание P2** |

Мейнтейнер не отвечал ни там, ни там. Оба слота заняты, поэтому кандидаты 3-5 готовятся, но не
подаются.

**Замечание по #113 разобрано в `02_nvfp4_swiglu/FOLLOWUP_codex.md`** — бот заметил, что `ctest` не
исполняет ни одной новой ширины, потому что список случаев A4 кончается на 1024, а 1024 была фьюзед
и до правки. Замечание верное; правка — добавить 256 в `kA4Cases`.

| # | папка | ветка | статус | размер | побитово |
|---|---|---|---|---|---|
| 1 | `01_w8_rowsplit` | `beced522` | **готово, 3 круга ревью** | 1 файл, +25/−35 | да |
| 2 | `02_nvfp4_swiglu` | `436aba7b` | **готово, 2 круга ревью** | 1 файл, +10/−4 | нет, и не может быть |
| 3 | `03_moe_stage_from_x` | ребейз сделан, конфликты разрешены | замеры не начаты | 2 файла | да |
| 4a | `05_moe_bn128` | `864230d0` | **распалась на три; 4a — одна строка** | 1 файл, +1/−1 | да |
| 4b | `05_moe_bn128` | `0a6b3331` | не начата, чинить `NINFER_MOE_BN` | 1 файл, +15/−15 | да |
| 4c | `05_moe_bn128` | `2ffdea3e` | не начата, стоит на кандидате 3 | 1 файл, +77/−32 | да |
| 5 | `04_pv_fp16` | ложится на master чисто | **не PR, а комментарий в #27** | 2 файла, +34 | **нет** |

---

## 1 и 2 — готовы

Детали в `01_w8_rowsplit/RUNBOOK.md` и `02_nvfp4_swiglu/RUNBOOK.md`. Порядок: сначала Issue по W8,
дождаться ответа, потом PR; NVFP4 не открывать, пока первый не закрыт или пока не станет ясно, что
слот свободен.

`ctest` на новой базе **зелёный на обеих ветках и на master**: 94 теста, 0 падений, 1 пропуск
(нужен второй артефакт 27B, которого на этой машине нет). Наш issue **#105 закрыт его же правкой**
`fd48e2fa`, которая переписала оракул теста — то есть ожидание в тесте было неверным, а не движок.

---

## 3 — стейджинг MoE из `x` (`9c67c692`)

Ветка `perf/moe-stage-from-x` создана, три конфликта с `3a61ef3f` разрешены скриптом
`/root/resolve_b1.py` (лежит в `90_harness/`). Суть конфликта: master разделил то, что раньше было
одним буфером — `select_count` пишет ранг в `local_rank`, а `gather` публикует обратную карту в
`packed_index`, алиасящийся на мёртвый префикс `tile_counts`. Наше индексное ядро читало ранг
из `packed_index` на месте; после разрешения оно читает `local_rank`.

**Побочный эффект в нашу пользу, который стоит сказать в заявке:** правка убирает перекрытие
чтения-записи, о котором предупреждает его собственный комментарий в `3a61ef3f` — индексное ядро
читает ранг и пишет карту из одного потока, и две величины никогда не живут в одном буфере.

Замерено раньше (на старой базе, поверх группы A): **+1.9 / +1.5 / +1.3%** сквозняка. Нужно
перемерить на `1fc1cb76`.

**Что не забыть:** `grouped_io` остаётся ради W8-кодека, поэтому воркспейс **не падает**;
экономится трафик. Это ровно та ошибка, которую я допустил в NVFP4 и которую поймало ревью — не
повторять.

---

## 4 — плитки и нарезка grouped-MoE: цепочки нет, есть три независимые заявки

**Пересмотрено 29 августа, разбор в `05_moe_bn128/NEXT.md`.** Раньше здесь было записано, что три
коммита неразрывны. Это неверно: они трогают разные вещи, и только BN=128 стоит на кандидате 3.

* **4a `864230d0` — одна строка**, `kSparseMoePrefillSliceMax` 4096 → 8192. Константа входит в два
  места, оба через `min()`, поэтому для чанка ≤ 4096 не меняется ничего **по построению**. Лучшее
  отношение «доказательство / размер» из всего, что осталось. Цена названа в самом коммите: на
  чанке 8192 воркспейс MoE удваивается, 481.53 → 963.06 МиБ.
* **4b `0a6b3331`** — ширина задания от чанка, +15/−15. Чинить `NINFER_MOE_BN`.
* **4c `2ffdea3e`** — BN=128, +77/−32, стоит на кандидате 3, подавать последним.

Подача по-прежнему не «ускорение префилла», а **«снятие внутреннего потолка для того, кто уже выбрал
широкий чанк»**, с дельтой по памяти в явном виде.

---

## 5 — PV на f16 (`3ccb531d`)

Самая крупная одиночная правка по проценту: **+4.3 / +6.9 / +9.2%** префилла на 33K / 67K / 115K,
растёт с длиной, потому что живёт во внимании. И самая сложная в подаче, по трём причинам.

**Не побитово, и только на `--kv-dtype int8`.** Все наши сквозные кампании шли на bf16-KV, поэтому
для неё нужна отдельная кампания на int8.

**Стенд точности уже есть** — `БЕНЧМАРКИ_три_конфигурации_2026-08-24.md`: EvalScope, AIME25 (30) и
GPQA-Diamond (198) целиком. Конфигурация C = int8-KV + PV fp16 дала AIME25 **90.0** против 90.0 у
стока и GPQA **84.85** против 84.34. Ценность стенда не в «C не хуже», а в измеренном шумовом поле:
конфигурации A и B отличаются только порядком суммирования, и разрыв между ними оказался **ровно
нулевым** на обоих датасетах. Честное ограничение, которое надо написать самим: на 198 вопросах
одна сигма ≈ 2.5%, стенд отвергает деградацию больше ~5%, но не подтверждает отсутствие 1–2%.

**Это не наша правка целиком, и подавать её как PR, скорее всего, вообще не надо.** Прочитал
issue #27 (открыт 15 августа, автор `sergiuszm`, Sergiusz Michalik). Там не идея — там **готовая
реализация**: он даёт ссылку на свой коммит `sergiuszm/ninfer-5090@4483c820`, те самые 34 строки в
двух файлах, и пишет, что они ложатся на master чисто. Наш `3ccb531d` — это черри-пик его работы, и
авторство в коммите стоит его, как и должно.

**Состояние issue на 29 августа: открыт, ноль комментариев, ответа мейнтейнера нет, связанного PR
нет.** Две недели тишины.

Из этого следует другой ход, и он лучше нашего прежнего. Открывать свой PR с чужим патчем — значит
встать между автором и мейнтейнером на его же заявке. Чего этой заявке действительно не хватает —
это **независимого воспроизведения на второй машине**, и вот это у нас есть. Поэтому:

* пишем **комментарий в #27** с нашими замерами, а не отдельный Issue и не PR;
* прямо говорим, что правка не наша, что мы её только проверили, и приводим свою конфигурацию —
  она отличается от его: он мерил операторные TFLOP/s (185.9 → 226.3 на 8K, 191.9 → 236.0 на 64K,
  192.5 → 229.6 на 128K) и сквозняк на 64K/128K/200K (+2.3 / +3.6 / +4.7%); мы мерили сквозной
  префилл на Qwen3.6-35B-A3B, int8-KV, чанк 4096: **+4.3 / +6.9 / +9.2%** на 33K / 67K / 115K;
* отвечаем **на его открытый вопрос** — он спрашивает, каким делать arch-guard: убрать совсем,
  сузить до `sm_120a` или оставить перечисление. У нас есть железо ровно этого класса, и ответ на
  этот вопрос — измеримая вещь, а не мнение;
* добавляем то, чего у него нет: стенд точности на генерации (AIME25 и GPQA-Diamond целиком) с
  измеренным шумовым полом.

Если после этого мейнтейнер попросит PR — первое слово за автором. Мы подаём, только если он
попросит нас, и тогда автор коммита не меняется.

**Проверено 29 августа: `3ccb531d` ложится на текущий master `1fc1cb76` чисто**, 2 файла, +34,
без конфликтов. Это был главный неизвестный по этому кандидату, и он закрыт.

Общее правило остаётся: если используем чужое — авторство сохраняем всегда.

**Ляжет ли на master — проверено, ложится.** `git cherry-pick 3ccb531d` на `1fc1cb76` проходит без
конфликтов: `src/ops/common/mma.cuh` +11 и
`src/ops/softmax_attention/dense/causal_cache/prompt_i8.cuh` +23. Две правки master в этих файлах
(`6183c9be` fp8 kv cache, `17a7275f` int8 kv hadamard rotation) не задели тронутые строки.

---

## Что усвоено за три круга ревью и стоит применять к 3, 4 и 5

1. **Заголовок обещает только то, за что отвечает тело.** Дважды ловили: x1.12, которую тело
   дезавуирует, и +2.4%, существующие только вне умолчательной конфигурации.
2. **Ни одного одиночного прогона, поданного как результат.** Каждое число — либо диапазон по
   проходам, либо явно помеченное «один проход».
3. **Ресурсную заявку проверять замером, а не цитатой из своих заметок.** «570 МБ арены» и
   «убирает материализацию» оба не пережили проверки в том виде, в каком были записаны.
4. **Возражение автора считать за него.** Где он сказал «широкий чанк не бесплатен» — самим
   привести цифру и сказать, что заявка её не уменьшает.
5. **Свои инструменты тоже врут.** `sass_diff.py` недосчитывал опкоды под униформным предикатом и
   льстил нашим же числам; нашли только потому, что перепроверяли.
6. **Гейт на 32 токена бесполезен** — 40 байт и два различных текста на все точки. Настоящий
   гейт — полная генерация со стопом.
