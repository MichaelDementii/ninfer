# Состояние кампании на 2 сентября 2026

База — **`upstream/master` `5973313d`**. Инстанс 49380254, RTX 5090 (170 SM, L2 96 МБ,
полоса 1792.1 ГБ/с), Release, `sm_120a`, CUDA 13.1.115, артефакт `qwen3_6_35b_a3b.ninfer`.

`ОЧЕРЕДЬ.md` относится к прошлой базе `1fc1cb76` и устарела. Актуально — этот файл.

## Что уже наверху

| наше | коммит | что |
|---|---|---|
| пакет 01 | (раньше) | восемь кодов веса на нить в W8 row-split |
| пакет 03 | `0a204aee` | стадирование MoE из `x`, копирующее ядро убрано |
| пакет 08 | `9954867a` | префетч веса в RMSNorm, шаблонным параметром |

Разбор всех десяти коммитов окна — `17_обзор_апстрима/ОБЗОР_смердженного.md`.

## Что готово к подаче

### Пакет 16 — плитка варпа 2×2 в маршрутизируемых MoE-GEMM. **Все гейты закрыты**

| гейт | результат |
|---|---|
| побитовость | **совпало, токен в токен, 804 байта** |
| спилл | **нет**: `q4_gate_up<8,64>` REG:80 STACK:0 на обеих руках |
| занятость | не меняется, общая память не меняется |
| декод | −0.6% на старой базе **не воспроизводится**; поядерно Δ = −0.06% |
| clang-format | 0 строк расхождения, **SASS до и после совпал** |
| кириллица в диффе | 0 строк |

**Префилл 32768, чанк 4096: 1.751867 → 1.726863 с, −1.43%.** Поядерно `q4_gate_up` −5.59%,
`qx_down` −4.62%. Подробно — `16_moe_warp_tile/STATUS.md`.

## Что найдено в этом круге и ещё не оформлено в заявку

### Кандидат 18 — порог широкого плана MoE задан не в той величине

`wide_plan = tokens >= 768` выбирает плитку колонок 64, а решает `tokens × top-k / эксперты`.
Перелом измерен: планы сравниваются вничью при **40 колонках на эксперта**, то есть порог = 1280.
При чанке 1024 на эксперта приходится 32 колонки — **половина плитки перемножает пустоту**.

| рука при чанке 1024 | префилл | Δ |
|---|---:|---:|
| апстрим (порог 768) | 2.199269 с | — |
| порог 1280 (перелом измерен) | 2.170226 с | **−1.32…−1.43%** |
| порог 2048 + плитка 2×2 | 2.142529 с | **−2.37%** |

Контроль на чанке 2048, где порог не решает: −0.07%. Правка действует ровно там, где должна.

**Побочная находка:** узкий план `<8,32>` — восемь варпов при BN 32 — под исходной раскладкой 4×1
**невыразим** (`WarpCols = 32/8 = 4` ломает `WarpCols % 8 == 0`), а под нашей 2×2 законен и
собирается с **REG:76 STACK:0**. То есть пакет 16 открывает форму, которой без него нет.

### Проверка чужой ветки `perf/pv-f16acc`

Ветка напарника, замеренная им на операторном стенде. В движке на 35B: **внимание −14.67%,
префилл −3.90%**, попадает ровно в одно ядро. С нашей плиткой складывается: **−5.38%**.
Потолок fp16-накопления переснят прибором: **531.0 TFLOPS, ровно вдвое от fp32-накопления**.
Подробно — `19_проверка_pv_f16acc/STATUS.md`.

## Профиль 35B: где лежит время

**Префилл 32768, чанк 4096 — 5214.2 мс GPU.** Четыре ядра держат 83.4%.

| ядро | доля | КПД от своего потолка |
|---|---:|---:|
| `causal_attention_prompt_i8` | 28.17% | 43.5% смешанного int8/fp16 |
| `w8_rowsplit_gemm_mma` | 25.66% | **73.2%** bf16 |
| `moe_pre_q4_gate_up` | 19.00% | 51.9% |
| `moe_pre_qx_down` | 10.53% | 46.8% |

**Декод 256 токенов, MTP 3 — 1943.1 мс GPU, 200 237 пусков, 528 пусков на раунд.**
`lm_head` (Q6, 397.4 МБ) берёт 20.2% и читается **четыре раза за раунд**; само ядро идёт на
**90.6% полосы DRAM**, улучшать в нём нечего.

**Чанк 1024 — умолчание движка — стоит +23.45% GPU-времени против 4096.**

Полный разбор — `18_профиль_35B/ПРОФИЛЬ_чанк_1024_и_4096.md`,
список того, что стоит проверять — `18_профиль_35B/СПИСОК_что_проверять.md`.

## Параллельные потоки: 1, 2, 4 линии

Прибор — `apps/serve --max-concurrency N` и настоящие одновременные HTTP-запросы; `ninfer_bench`
линий не умеет. Восемь запросов, работа одна и та же, меняется только число линий.

| линий | чанк 4096 | ускорение | чанк 1024 | ускорение |
|---:|---:|---:|---:|---:|
| 1 | 2.4775 с | — | 2.6625 с | — |
| 2 | 2.0205 с | 1.226× | 2.2485 с | 1.184× |
| 4 | **1.8010 с** | **1.376×** | 2.0205 с | 1.318× |

**Четыре линии дают 1.38×, а не 4×**, и причина найдена поядерно: плотные веса и `lm_head`
амортизируются между линиями почти полностью (×1.06 и ×1.07 времени за ×2.66 работы), а
**маршрутизируемые эксперты не амортизируются вовсе** (`moe_d4_token` ×2.56 за ×2.61). Разные
линии выбирают разных экспертов, и трафик весов растёт вместе с линиями.

**Большой чанк выигрывает тем сильнее, чем больше линий** (6.9% → 10.1% → 10.9%) — обратно
ожиданию, что он будет запирать соседние линии.

## Настройка, которую стоит поменять без единой строки кода

**`--lm-head-draft` выключен по умолчанию и даёт +23.7% декода.** Черновая голова — отдельное
ядро `q4_rowsplit_gemv` на 142.6 МБ вместо Q6-головы на 397.4 МБ; скалярный Q6-маршрут исчезает
на 99.6%. Флаг ещё и поднимает приёмку (0.5906 → 0.7073), поэтому раундов становится меньше.

**Оптимум окна MTP зависит от флага:** без него минимум на двойке (569.7 ток/с против 561.0 на
тройке, +1.55%), с ним — на тройке (704.5 против 623.8 на двойке). Лучшее к лучшему **569.7 →
704.5, +23.7%**. Две ручки нельзя крутить по отдельности.

> ⚠️ **И флаг, и окно меняют текст.** Жадная генерация с флагом и без расходится; окна 1/2/3/5
> тоже дают разные ответы. Механизм: от числа принятых за раунд токенов зависит `T` следующего
> раунда, от `T` — выбор расписания ядер, от расписания — округление, и на близких логитах
> изредка другой `argmax`. **Эти числа верны как пропускная способность движка и неверны как
> «тот же ответ, только быстрее».**

## Всё вместе: апстрим + ветка PV + плитка 2×2 + порог 1280

Одна сборка со всеми тремя правками. Регистры `q4_gate_up<8,64>` — **REG:80 STACK:0 SHARED:33792**,
то есть спилла нет даже при трёх правках сразу, занятость та же.

Префилл 32768, KV int8, палиндром:

| чанк | апстрим | всё вместе | Δ |
|---|---:|---:|---:|
| 4096 | 1.747782 / 1.757581 / 1.759787 → **1.755050** | 1.654644 / 1.660378 / 1.661381 → **1.658801** | **−5.48%** |
| 1024 | 2.198100 / 2.202347 / 2.204847 → **2.201765** | 2.064666 / 2.068737 / 2.071312 → **2.068238** | **−6.07%** |

Декодный контроль: правки только в префилле, и декод не двигается —
`U0` 555.4 ток/с против `ALL` 561.5, приёмка совпадает до десятого знака. С флагом
`--lm-head-draft` обе руки дают 709.1 и 709.4 ток/с.

При чанке 1024 выигрыш больше, потому что там работает ещё и порог.

Сложение по частям сходится: при чанке 4096 порог не действует, и −5.48% раскладывается на
−3.90% от ветки PV и −1.43% от нашей плитки (вместе −5.33%, измерено −5.48%). При чанке 1024
добавляется −1.36% от порога и −0.98% от плитки в узком плане.

---

## Указатель документов этого круга

| документ | о чём |
|---|---|
| `17_обзор_апстрима/ОБЗОР_смердженного.md` | все десять коммитов окна, что из них наше, что меняет базу под пакетом 16 |
| `18_профиль_35B/ПРОФИЛЬ_чанк_1024_и_4096.md` | поядерный профиль префилла и декода, КПД четырёх главных ядер от измеренного потолка |
| `18_профиль_35B/СПИСОК_что_проверять.md` | **главный список**: сводка по убыванию приза, тринадцать открытых осей, закрытое замером, приборные уроки |
| `19_проверка_pv_f16acc/STATUS.md` | чужая ветка PV в движке: скорость, сложение с нашей правкой, гейт качества |
| `20_параллельные_потоки/ЗАМЕР.md` | линии 1/2/4 на префилл- и декод-тяжёлой нагрузке, поядерно, ловушки прибора |
| `16_moe_warp_tile/STATUS.md` | наш пакет 16: все гейты на новой базе |
| `90_harness/` | скрипты приборов: патчи абляций, `mmapeak`, `parbench.py`, `kern.py`, `parlog.py` |

## Чему научил этот круг — про приборы, а не про ядра

1. **`cuobjdump -res-usage` на каждой руке.** В смердженном пакете 03 записано, что ядро заняло
   все 80 регистров; без проверки наложение могло дать спилл и выглядеть медленнее.
2. **Охранитель на длину вывода.** Проверка побитовости сказала «совпало, 1 байт»: модель
   думающая, 160 токенов ушли в размышление, и совпали два пустых файла.
3. **Прибор должен быть непустым.** Проверка «меняет ли чанк вывод» шла на промпте в двадцать
   токенов, который при любом чанке укладывается в один. Ответ «не меняет» ничего не значил.
4. **Приёмка — свойство текста, а не кодека.** Сравнение кодеков KV по скорости декода мерило
   везение черновика; чистый замер требует убрать спекуляцию совсем.
5. **Абляция меняет число раундов — нормируй на пуск и проверь контролями.** В замере потолка
   дедупликации раундов стало на 29% больше; два незатронутых ядра подтвердили, что нормировка
   на пуск честная (+0.30% и +0.18%).
6. **`static` внутри device-функции — это глобальная память.** Поймано до сборки.
7. **Имя ядра в `nsys` содержит `<unnamed>::`.** Резка по `<` до снятия префикса схлопывала
   все MoE-ядра в пустую строку — 37% времени префилла пропадало из таблицы.
8. **Порт 8080 на этой машине занят jupyter'ом**, а `kv_capacity` ограничен правилом
   `страниц ≤ max_concurrency × page_count(max_context)`.
9. **Один распорядитель вместо цепочки ожиданий.** Две копии круга поделили GPU, одна упала и
   записала маркер завершения раньше времени, разблокировав следующие.
