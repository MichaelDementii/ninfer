# `sparse_moe_prefill_q4_gate_up`: 18.6% префилла 35B, разобрано по фазам

Ядро с самым большим весом в префилле `qwen3_6_35b_a3b` после внимания. База `upstream/master`
`21a0e85f`, инстанс 49380254, RTX 5090.

Прибор — поядерная трасса операторного стенда
(`ninfer_sparse_moe_bench --codec q4-q5 --tokens 4096 --distribution trace-like --cache cold
--execution graph --warmup 3 --repeat 20`, `nsys --cuda-graph-trace=node`), между руками
палиндромная нога N0. **Шумовой пол по девяти точкам N0: 934.6…936.3 мкс, то есть ±0.09%.**

## ⚠️ Приборная ошибка, которую пришлось исправить

Первый круг заменял `mma.sync` на «дешёвую зависимость», сохраняющую операнды живыми:

```
const float mix = __int_as_float((a0^a1^a2^a3^b0^b1) & 0x3f800000u);
partial[0] += mix; partial[1] += mix; partial[2] += mix; partial[3] += mix;
```

Это **пять XOR, AND и четыре FADD — десять инструкций вместо одной**. Замена дороже заменяемого,
и полученные «6.5%» — не доля MMA, а нижняя граница. Тот же дефект у абляции распаковки Q4
(замена в пять операций вместо `decode_eight`): её «0.8%» — тоже нижняя граница.

**Исправленный прибор не заменяет инструкцию, а кратно уменьшает её число**, оставляя весь
остальной код байт в байт: `mma` выполняется для `mi < 2` из 4, затем для `mi < 1` из 4.

| рука | тензорных инструкций | мкс | Δ |
|---|---:|---:|---:|
| N0 | 16 на k-тайл | 935.0 | — |
| `F_half` | 8 | **747.6** | **−187.4** |
| `F_quarter` | 4 | **670.2** | **−264.8** |
| ~~`F_none`~~ | 0 | ~~1025.3~~ | **недействительна: `STACK:72`, компилятор ушёл в спилл** |

Наклон по двум действительным точкам — 19.35 мкс на «четверть», то есть снятие всех шестнадцати
инструкций стоило бы ≈355–375 мкс **непокрытого** времени.

**Отсюда правило, добавленное в приборный список: у каждой руки проверять `cuobjdump -res-usage`.**
Из тринадцати собранных рук спилл дали две (`C_ldm` `STACK:56`, `F_none` `STACK:72`), и обе
показывали ядро *медленнее* базы. Без этой проверки их легко принять за находку.

## Что чем занято

| фаза | как измерено | доля ядра |
|---|---|---:|
| **тензорная работа** | нижняя оценка по наклону F_half/F_quarter | **≥20%**, по roofline **57%** |
| локальность весов в кэше | `--distribution same` (8 экспертов вместо 256) | **15.6%** |
| свёртка масштабов по k-тайлам | абляция | 5.1% |
| · из неё два `__shfl_sync` | замена на широковещательное чтение | **2.5%** |
| глобальное чтение активаций | стейджинг нулём байт | 5.0% |
| глобальное чтение кодов веса | стейджинг нулём байт | 4.4% |
| два `__syncthreads` в цикле по k | абляция | 2.1% |
| эпилог (глобальная запись) | абляция | 1.3% |
| распаковка Q4 | абляция (нижняя граница) | ≥0.8% |
| **ожидание прихода данных** | `cp.async.wait_group` снят | **0.1% — ноль** |

## Главный вывод: ядро тензорное, но недокормленное

Полезной работы на срез — 32768 назначений × 1024 × 2048 × 2 = **137.4 GFLOP**. При измеренном
пике bf16 **256.7 TFLOPS** это 535 мкс чистой тензорной работы. Измерено 935.0 мкс, то есть
**57.2% потолка**, и тензорный конвейер занят 57% времени.

Оставшиеся 43% — то, что не удаётся спрятать под MMA. **Ожидание памяти тут ни при чём**
(абляция `cp_wait` дала ноль): данные приходят вовремя, а вот работа вокруг них конкурирует с
тензорной инструкцией за слоты выдачи.

**Потолок направления — 400 мкс на срез = 43% ядра = 8.0% префилла**, если бы всё нетензорное
пряталось полностью. Это недостижимо, но задаёт масштаб.

## Где структурно берётся лишняя работа

Плитка варпа — **4×1**: `ExpertBN = 64`, восемь варпов, `WarpCols = 8`, `WarpNT = 1`. За один
`ki` варп делает четыре `ldmatrix.x4` на A и один `ldmatrix.x2` на B ради **восьми** `mma`;
за k-тайл это **20 чтений из shared на 16 тензорных инструкций**.

Фрагмент A читается ради единственной плитки B — то есть **не переиспользуется вообще**. При
`WarpNT = 2` те же четыре чтения A обслуживали бы вдвое больше `mma`, и отношение упало бы с
1.25 до 0.75. Легальный способ этого добиться — `ExpertBN = 128`, и это ровно механизм
заготовленного пакета `05_moe_bn128`, у которого до сих пор не было объяснения, **почему** он
должен помогать.

Цена: общая память 33792 → 49152 байт, то есть **2 CTA на SM вместо 3**. А занятость здесь
дорога — `B_st3` (три стадии конвейера вместо двух, ровно та же потеря CTA и ничего взамен) дал
**+17.9%**. Значит расширение плитки обязано выиграть больше 18%, чтобы окупиться.

## Занятость: обе границы упираются в 3 CTA одновременно

`REG:75 STACK:0 SHARED:33792` при 256 нитях:

* по общей памяти: 3 × 33792 = 101 376 при `sharedMemPerMultiprocessor` = 102 400 — **запас 1024 байта**;
* по регистрам: 75 × 256 × 3 = 57 600 при регистровом файле 65 536 — на четвёртую CTA не хватает.

Занятость **24 варпа из 48, ровно 50%**, и подняться некуда: чтобы получить 4 CTA, нужно
одновременно ≤25 600 байт shared и ≤64 регистра.

## Checks not run

* `ldmatrix` как доля — прибор («вдвое меньше чтений A при том же числе `mma`») собирается сейчас.
* `ExpertBN = 128` не собран: `qx_down` жёстко считает `warp * 8` колонок и при 128 покрывает
  только половину, а список работ у обоих ядер общий (`route_job_bn`). Пакет `05` это знал и
  поэтому предлагал переписанную правку, а не перенос коммита.
* Побитовость кандидата `shfl` проверяется сейчас сквозной жадной генерацией.
