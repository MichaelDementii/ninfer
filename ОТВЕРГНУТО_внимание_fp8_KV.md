# Отвергнуто: «префилльное внимание на fp8-KV недобирает 22% до тензорного пола»

**Ось закрыта 1 сентября 2026.** Сигнал был красивый и ложный; ниже — чем именно.

## Как это выглядело

Собственный бенч проекта, `ninfer_causal_softmax_attention_bench`, печатает для каждой формы
процент от смешанного тензорного пола. На продовой геометрии `d256-h24-kv4`, W=4096,
контекст 32768:

| KV | время | % тензорного пола |
|---|---:|---:|
| int8 | 17259.5 мкс | **96.9%** |
| bf16 | 17821.7 | 93.9% |
| **fp8** | **16151.5** | **77.7%** |

Пол для fp8 — 12546.8 мкс. То есть на глаз: fp8-вариант уже быстрее int8 на 6.4%, а до своего пола
недобирает 22%. Внимание — самое дорогое ядро префилла (27.8% GPU), значит 22% на нём это около
5–6% префилла. И fp8-KV сквозняком тоже быстрее: 4123.2 мс против 4228.4 у int8.

## Почему казалось, что причина найдена

`prompt_fp8.cuh` расщеплён по варпам: 8 варпов считают QK, 8 — PV
(`kCausalPromptFp8ProducerWarps = 2 * RowTiles`). Модель бенча считает QK по 419 TFLOP/s, а PV по
209.5 — то есть **PV вдвое дороже**. При расщеплении пополам время должно равняться
`max(2·t_QK, 2·t_PV) = 2·t_PV`:

* предсказание: 2 × 8363 = **16726 мкс**
* измерено: **16151.5 мкс**

Совпадение с точностью 3.5%. Отсюда вывод: половина варпов, считающая QK, простаивает, а лечится
это перебалансировкой в пользу PV.

## Что показал замер

Число QK-варпов вынесено в параметр (`QKSplits`), всё остальное без изменений. Тест
`ninfer_softmax_attention_test` проходит на обеих точках.

| баланс | время |
|---|---:|
| 8 QK / 8 PV — как сейчас | 16152.6 мкс (воспроизводит базовые 16151.5) |
| 4 QK / **12 PV** | **16990.2 мкс, то есть на 5.2% ХУЖЕ** |

**Дать PV больше варпов — хуже.** Значит PV не узкое место. Но и QK не узкое: вдвое урезав его
варпы, ядро потеряло 5.2%, а не удвоилось. Ни одна из половин не является ограничением.

## Что ограничивает на самом деле

Тот же бенч печатает рядом: `logical= 18231.7 GB/s (1017.4% of 1792)` при
`physical_kv_read= 4.7 GB/s (0.3% of 1674.5)`. То есть из DRAM ядро почти не читает — весь трафик
идёт через кеш и **shared**: стейджинг K и V, плюс круг матрицы P через shared между
варпами-производителями и варпами-потребителями. 18.2 ТБ/с — это уже разговор про пропускную
способность shared, а не про тензорные ядра.

Косвенное подтверждение: три варианта ядра лежат в пределах 10% друг от друга (bf16 17821, int8
17259, fp8 16151), хотя стоимость их QK различается **вчетверо** (f16 против s8 против fp8). Если
бы время задавали тензорные ядра, разброс был бы кратно больше.

**Вывод: «77.7% от тензорного пола» — не запас. Пол просто не является здесь ограничением.**

## Что из этого следует

* Ось «перебалансировать варпы в `prompt_fp8`» закрыта замером, а не рассуждением.
* Ось «довести fp8-KV до КПД int8-KV» тоже закрыта: разница между ними объясняется не КПД, а тем,
  что модель пола у них разная.
* Если возвращаться к этому ядру, целиться надо в **трафик shared**, а не в баланс варпов: убрать
  круг P через shared (он же и есть цена расщепления) или уменьшить стейджинг K/V. Это перестройка
  архитектуры ядра, а не настройка, и она конфликтует с самим смыслом расщепления — перекрытием QK
  следующего блока с PV текущего.
* Процент от тензорного пола в этом бенче нельзя читать как «столько можно взять»: рядом всегда
  надо смотреть на `logical GB/s`. При тысяче процентов от DRAM-полосы ограничение памятное.

## Сырое

Замер — `90_harness/patch_fp8attn.py` (параметризация баланса) и лог q73; обе точки собраны из
одного дерева на `upstream/master` `da49c0d6`, тест внимания проходит на обеих.
