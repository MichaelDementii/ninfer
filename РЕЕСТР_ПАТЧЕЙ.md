# Реестр наработок: что у нас есть, сколько это даёт и в каком режиме

Обновлено **2026-09-02**. Здесь инвентарь — «что это и сколько стоит». Стадии подачи и правила
автора — в [`ОЧЕРЕДЬ.md`](ОЧЕРЕДЬ.md) и [`00_rules/`](00_rules/).

⚠️ **Смена базы 2 сентября.** Всё, что помечено «круг 1–2 сентября», мерялось на
**`upstream/master` `5973313d`** и артефакте `qwen3_6_35b_a3b`, а не на стеке
`arvi/pr-consolidated`. По правилу выше **эти числа не складываются с остальными**: часть
механизмов старого стека уже в базе (в том числе П1), и время они делят иначе.
Сводка круга — [`СОСТОЯНИЕ_2026-09-02.md`](СОСТОЯНИЕ_2026-09-02.md).

## Как читать

**Числа нельзя складывать.** Большая часть мерялась внутри стека из 38 коммитов
(`arvi/pr-consolidated`, база `59febed2`), где механизмы делят одно и то же время. Потолок суммы
известен: весь стек против `origin/dev` давал **префилл +17.1 / +14.6 / +11.4%** (8.5K / 33K / 67K)
и **декод +13.0 / +13.3 / +12.4%**, mtp3 **+4.7%**, вывод побитово совпадал на всех десяти прогонах.

Колонка «замер»:

* **соло** — правка мерялась одна против master;
* **в стеке** — мерялась поверх остальных, в одиночку обычно даёт меньше;
* **старый стенд** — карта под 500 Вт; отношения переносятся, абсолютные значения нет.

Колонка «режим» — где выигрыш вообще наблюдаем. Правка, дающая +2% в mtp0, может давать 0.1% в
mtp3, и наоборот; это не разброс, а разные узкие места.

---

## 0. База. Читать первым

**`origin/master` = `3d9fda22`** (31 августа, 11:49). `origin/dev` сейчас **тот же коммит**.

⚠️ **`feaf4dd0`, записанный в `README.md` и `ОЧЕРЕДЬ.md` как «новая база», отстаёт от master на
110 коммитов.** Пакеты 08 и 09 собраны поверх него. Проверено переносом обеих веток на `3d9fda22`:

| ветка | перенос на `3d9fda22` | объём |
|---|---|---|
| `perf/rmsnorm-shape-prefetch` | **чисто, без конфликтов** | 3 файла, +124/−8 |
| `perf/mtp-sampled-draft` | **конфликты в 3 файлах**: `sampling.cuh`, `mtp_impl.h`, `text_context.h` | 18 файлов, +425/−34 |

Причина конфликтов 09 — апстримный `70434721 fix(engine): close tool output and sampling contracts`
переписал `speculative_round.cuh` (+134) и `mtp_impl.h` (198 строк). **Механизм там другой**
(штрафы в жадном режиме, не сэмплированное предложение), так что 09 не вытеснен — но переносить
его придётся руками.

---

## 1. Уже в апстриме — 6 из 38 коммитов стека, плюс пакет 08

| PR | апстрим | коммит стека | что |
|---|---|---|---|
| [#99](https://github.com/Neroued/ninfer/pull/99) | `92bb06eb` | `d0be2b3c` | GDN-свёртка префилла пишет прямо в q/k/v |
| [#106](https://github.com/Neroued/ninfer/pull/106) | `8d343527` | `5114a452` + `e1851ebb` | восемь кодов на поток в MoE-префилле |
| [#112](https://github.com/Neroued/ninfer/pull/112) | `93cdc264` | `7c5e3d4d` | то же в W8 row-split |
| [#113](https://github.com/Neroued/ninfer/pull/113) | `00369f63` | `f3e42970` | слитый TMA SwiGLU на всех кратных 256 |
| [#115](https://github.com/Neroued/ninfer/pull/115) | `5327d676` | — | тест на ширину, которую #113 открыл |
| [#140](https://github.com/Neroued/ninfer/pull/140) | **`0a204aee`** | `839c6017` | routed gate/up стейджит из `x` |
| — | **`9954867a`** | — | префетч веса в RMSNorm (пакет `08`), вмержен 2 сентября |

Это **единственные** PR, вмерженные автором за весь период; остальные внешние заявки закрыты или
без ответа.

**Осталось неподанного из стека: 32 коммита.** Плюс два новых пакета напарника (08, 09), которых
в стеке нет.

---

## 2. Префилл

| # | коммит | что делает | режим | прирост | замер | побитово | состояние |
|---|---|---|---|---|---|---|---|
| **П1** | `839c6017` | MoE не копирует токен по разу на каждого из 8 экспертов: копия заменена картой «место → токен» | префилл, любой чанк | оператор **×1.046**; сквозной **+1.45%** (чанк 1024) / **+1.38%** (8192); по всем ядрам прогона −1.09% | соло, новый стенд, ABAB ×12 | да | **вмержен как `0a204aee`** |
| **П2** | `8b5734ce` + `7f2bdfd3` | ширина задания MoE 128 вместо 64: слэб весов эксперта читается один раз вместо двух | префилл, **срез ≥ 3584** | оператор **×1.05** на срезе 4096; на 1024 **×0.86** — хуже | соло, новый стенд | да | порог поставить по замеренному переходу (3072–3584) и перемерить |
| **П3** | `9d556bad` | PV-стадия внимания копит в f16 вместо f32 | префилл, **только `--kv-dtype int8`** | **+1.6 / +4.3 / +6.9 / +9.2%** на 8.5K / 33K / 67K / 115K | соло, старый стенд | **нет** | правка **чужая** (`sergiuszm`, issue #27); у нас воспроизведение — пакет `04_pv_fp16` |
| **П4** | — | тот же приём на bf16-маршруте внимания | префилл, bf16-KV | **кода нет.** Потолок: `prompt_bf16` = 28.5% префилла на 33K и 44.2% на 67K, PV — половина по MAC, предел ×1.333 на PV → до ×1.15 на внимании | — | нет | идея автора (#27), **не начинать без его слова** |
| **П5** | — | int8-активации в `w8_rowsplit` | префилл | **+7.3%**, но только если квантование вынесено к производителю активаций; внутри ядра съедается | соло, старый стенд | нет | форма не найдена |
| **П6** | — | `fp8_mma` префилла на TMA и варп-специализацию | префилл | **~6%** оценкой: все четыре формы `fp8_mma` берут 71–75% потолка карты, соседний `nvfp4_w4a4_tma` — 87–95% | оценка по потолкам | нет | ядро с нуля; **самая крупная незакрытая цель** |

**Отклонено автором, не подавать:** `55bf090a` — потолок среза 4096 → 8192. Даёт рост, но удваивает
воркспейс, а автор считает эту память принадлежащей KV-кешу и сделку уже отклонил (пакет
`06_moe_slice_max` закрыт по этой причине).

### Круг 1–2 сентября. База **`upstream/master` `5973313d`**, инстанс 49380254

Числа этого круга **не складываются с таблицей выше**: другая база (в ней уже есть П1) и другой
артефакт по умолчанию.

| # | ветка / коммит | что делает | режим | прирост | замер | побитово | состояние |
|---|---|---|---|---|---|---|---|
| **П7** | `perf/moe-warp-tile` @ `0e33d990` | плитка варпа 2×2 вместо 4×1 в `q4_gate_up` и `qx_down`: четыре чтения из общей памяти на четыре `mma` вместо пяти, фрагмент A переиспользуется | префилл, любой чанк, маршрутизируемый Q4/Q5 | поядерно `q4_gate_up` **−5.6%**, `qx_down` **−4.6%**; сквозной **−1.43%** (32768, чанк 4096) | соло, палиндром ×6, разброс 0.15% | **да** | **готово к подаче**, все гейты сняты |
| **П8** | там же, `kSparseMoePrefillWideMin` 768 → 1280 | порог широкого плана задан в токенах, а решает число колонок на эксперта `T·top-k/экспертов`; при 24–32 колонках плитка в 64 перемножает 50–62% пустоты | префилл, **токенов в чанке 768–1279** | **−3.3…−4.1%** на промптах 768–1152; **−2.40%** на 32768 при чанке 1024; **−3.64%** на pp2048 при чанке 1024 | соло, три ноги, разброс 0.05–0.47% | **да** | **готово к подаче** вместе с П7 |
| **П9** | `perf/pv-f16acc` @ `8f70d975` (напарник) | накопитель `PV` во внимании в fp16 вместо fp32 — **та же идея, что П3**, доведённая до кода | префилл, все кодеки KV | внимание **−14.67%**; префилл **−3.90%** (32768), с П7+П8 **−5.48%** (32768) и **−7.36%** (65536) | мной в движке, палиндром, поядерно | **нет** | проверено мной; перплексия ±0.01% со знаком, неустойчивым между подвыборками |

**П9 подтверждает П3 через две разные машины и базы.** П3 давал +4.3% при 33K и +6.9% при 67K на
старом стенде; П9 у меня даёт +4.06% при 32768 и ~+6.3% при 65536 (за вычетом доли П7+П8).
Совпадение формы и величины — сильный довод, что мерялось одно и то же.

**Обязательное к П7+П8, снято:** побитово (804 и 392 байта токен в токен), перплексия на полном
корпусе `ninfer-ppl-1m-v1` — **4.610141 против 4.610141, совпадение до седьмого знака на
1 044 557 токенах**; `ctest` 102/102 на трёх деревьях; спилла нет (REG:80 STACK:0 SHARED:33792);
SASS всего бинаря после `clang-format` совпал; на промптах 48–512 и 1280–1536 деградации нет.

---

## 3. Декод

| # | коммит | что делает | режим | прирост | замер | побитово | состояние |
|---|---|---|---|---|---|---|---|
| **Д1** | `95af7153` | сигмоидный гейт считается внутри эпилога внимания, а не отдельным ядром; фьюзится и батчевая ветка | декод, **все режимы** | **+2.4…3.8%** декода, **+2.0%** mtp3 | в стеке, старый стенд | да | **крупнейшее неподанное из стека по декоду**; нужен соло-замер |
| **Д2** | `f164a590` | префетч весов out-proj из хвоста GDN, длина берётся из самого веса | декод | **+0.99…1.59%** | в стеке | да | в ветке |
| **Д3** | `462b20f6` | четыре строки на CTA в проекциях декода при K=2048 | декод, **mtp0 сильнее всего** | mtp0 **+2.3%**, mtp3/mtp5 +0.1…0.2% | соло | да | под флагом `NINFER_OUTPROJ_DIRECT` |
| **Д4** | `59445790` | вынос групповых шкал W8 shared-эксперта из скалярного произведения | декод | **+1.2%** | соло | да | в ветке |
| **Д5** | `f9001b96` | свёртка гейт-нормы в батчевое обновление GDN | декод | **+0.39…0.57%** | в стеке | да | в ветке |
| **Д6** | `b229fcd0` | свёртка гейт-нормы в хвост записи GDN | mtp3 | **+0.25%** | в стеке | да | в ветке |
| **Д7** | — | реквант GDN W8 → Q4 (qk) и Q5 (value_z) | декод, все режимы | ожидание **+12% декода ≈ +3…4% сквозного**; снимает 13.1% байтового бюджета прохода (314 МБ) | оценка по бюджету; гейт качества **пройден** 32/32 иглы | **нет** (меняет веса) | три блокера в коде, порт не начат |

### Мелочь из стека, поштучно не костированная

Замерена только внутри стека; каждая ниже шумового пола соло-замера, вместе — заметная часть
декодных +13%. Подавать по одной невыгодно: цена ревью выше выигрыша.

`1ad22a76` `a919ab26` `b8f12f26` — прямой маршрут out-проекции декода и его умолчание ·
`418c56bd` `c18dc48c` `bf810787` — три префетча весов из хвостов соседних ядер ·
`2581eb42` `2129d991` — свёртка выбора роутера и пре-MoE нормы в прологи D1/D3 ·
`00f87e25` `4b9074c3` `394fa2a3` `6c7801ce` — четыре правки расписаний MoE на малых T ·
`58a00349` `6873ca44` — слияние q/k-нормы в одно ядро и маршрут черновой головы через него ·
`6870d530` — окно черновика 15 токенов для 27B

### Исправления, не ускорения

| коммит | что чинит | стоит ли подавать |
|---|---|---|
| `101e54fc` | барьер скана MoE-префилла перед перезаписью ячейки, прочитанной соседом | **нет** — автор забрал эту гонку своим коммитом при разборе #96 |
| `39333e8e` | GDN несёт bf16-округлённую проекцию как отвод свёртки следующей колонки | да, отдельной заявкой как фикс |
| `7aec42a8` | разные профили исполнения графа для verify-маршрутов 35B-A3B | да, если воспроизведётся на текущем master |
| `1a9622b6` | диагностика: что именно изменилось при отказе обновления графа | да, дешёвая и полезная автору |

---

## 4. Пакеты напарника — вне стека

| # | ветка | что делает | режим | прирост | побитово | состояние |
|---|---|---|---|---|---|---|
| **08** | `perf/rmsnorm-shape-prefetch` @ `74dcc514` | норма читает веса до блочной редукции; специализация под d=5120; предзагрузка выбирается по форме запуска | **декод (главное) + префилл** | нормы раунда **652.4 → 360.0 мкс (−44.8%)**, раунд **−1.49%**; нормы префилла 8K **−12.0%** | часть 1 — да; часть 2 меняет порядок суммирования, но жадная генерация совпала побитово на трёх целях | **переносится на `3d9fda22` чисто**; текстов Issue/PR нет |
| **09** | `perf/mtp-sampled-draft` @ `ce71f787` | черновик MTP сэмплируется из своего усечённого распределения вместо argmax; приёмка `min(1, p/q)` | **декод, только стохастический режим** | приёмка **45.26 → 52.57%**, ток/с **+8.98%** на прозе; до **+13.1%** на groupwise-int. Цена: **+0.25%** времени раунда | под `--greedy` совпало до токена (вырожденный контроль) | конфликты при переносе; **штатный `eval/` не закончен** |

**Про 08 — что назвать слабым местом самим.** Порог `kRmsPrefetchBlocks = 512` подобран под 170 SM
этой карты: это зашитая константа под конкретное железо. Третья часть цепочки обязательна — без неё
первая вносит **+19.3%** на гейтованной пост-норме GDN при T=1024.

**Про 09 — уровень заявки не операторный, а фаза запроса.** Операторного выигрыша нет вовсе:
время раунда правка слегка увеличивает, а выигрыш берётся тем, что полезных токенов в раунде
больше. Это надо писать первой строкой.

**Снято как дубль:** слитый TMA-свиглу вне чанка 1024 найден напарником независимо и оказался тем
же механизмом, что PR #113. Воспроизведение годится в тред #113, отдельной заявки нет.

---

## 5. Не про пропускную способность

| # | коммит | что делает | что улучшает | прирост |
|---|---|---|---|---|
| **Х1** | `2aa3ee45` | мемоизация BPE-слияний внутри одного вызова | TTFT дозапроса с прогретым префиксом | render/preprocess **0.016 → 0.005 с** |
| **Х2** | `a4a74621` | пропуск NFC-нормализации для чистого ASCII | то же | часть той же связки |

Хостовая сторона целиком: рендер и препроцессинг **×5…×11** против стока (98 → 12 мс на 33K,
384 → 35 мс на 128K). В пропускной способности не видно, видно в TTFT.

---

## 6. 27B NVFP4

| # | коммит | что делает | режим | состояние |
|---|---|---|---|---|
| **N1** | `c735909b` | обход сетки TMA группами токенов, чтобы веса оставались в L2 | префилл 27B NVFP4 | часть связки, дававшей +3.5% на 32K и +8.1% на 128K; вторая половина **уже вмержена** как #113. После мержа не переоценивалось |

---

## 7. Настроечные находки — не код, но деньги

| что | эффект |
|---|---|
| ширина префилл-чанка 1024 → 8192 | **+25.7% (32K), +28.4% (128K)**. Цена: воркспейс 120 → 963 МиБ. Автор **отказался менять умолчание**: эта память принадлежит KV |
| MTP4 вместо MTP3 как универсальный дефолт | среднее **+17%** по категориям, нигде не хуже mtp3 |
| MTP2 на прозе | **+23%** против mtp3 |
| DFlash5 на коротком reasoning | **+34%** |
| int8-KV против bf16-KV | декод **+3.9 / +8.4 / +14.3%** с ростом контекста, префилл +2% |
| «чанк 8192 + PV fp16» | 128K: 7 147 → **9 537** (+33%), 256K: 4 689 → **6 242** (+33%) |

### Круг 1–2 сентября, база `5973313d`, артефакт `qwen3_6_35b_a3b`

**Все четыре меняют вывод** — не побитово, и это проверено, а не предположено. Механизм общий: от
формы вызова (длина чанка, число токенов в раунде проверки) зависит выбор расписания ядер, от
расписания округление, и на близких логитах изредка другой `argmax`. Числа верны как «столько
выдаёт движок» и неверны как «тот же ответ быстрее».

| что | эффект | цена |
|---|---|---|
| **`--lm-head-draft`, выключен по умолчанию** | декод **+25.9%** при окне 3 (560 → 705 ток/с). Черновая голова — отдельное ядро `q4_rowsplit_gemv` на 142.6 МБ вместо Q6-головы на 397.4 МБ; скалярный Q6-маршрут исчезает на **99.6%**. Приёмка растёт 0.5906 → 0.7073 | вывод может измениться |
| **окно MTP зависит от этого флага** | без флага минимум на **2** (569.7 против 561.0 на тройке, +1.55%); с флагом — на **3** (704.5 против 623.8 на двойке). Лучшее к лучшему **+23.7%** | две ручки нельзя крутить порознь |
| **чанк 4096 вместо умолчания 1024** | префилл 32768: 2.1983 → **1.7575 с, −19.8%**. Свип: 512 → 2.8077, 1024 → 2.1983, 2048 → 1.8960, 3072 → 1.7914, 4096 → 1.7575 | воркспейс 109 → 438 МБ (линейно по чанку). **Чанк 2048 берёт 86.8% выигрыша за половину памяти** |
| **кодек KV `fp8` вместо умолчания `bf16`** | декод **+9.2 / +19.6 / +28.2 / +41.6%** при 32K / 64K / 128K / **260K** под настоящей нагрузкой; **+5.46%** мс/раунд в производственном рецепте; **KV вдвое меньше** (374 против 743 МБ) | перплексия **+0.0215…+0.0449%**, вдесятеро меньше цены `nvfp4` |
| **кодек KV `fp8` вместо `int8`** (что в рецептах автора) | декод **+1.77 / +2.15 / +4.79 / +5.60%** чисто при 1K/8K/32K/64K; **+0.36…+1.73%** мс/раунд в производственном; префилл **+2.20%** при 32K, **+4.16%** при 65536; KV на 2.3% меньше | перплексия **+0.0458%** при контексте 4096, **+0.0197%** при 65536, **+0.0036%** при 262144 — **сжимается там, где растёт выигрыш** |
| кодек KV `k8v4` | **быстрее** fp8 в обоих режимах (352.81 против 348.78 ток/с чисто при 32K; 4.8918 против 4.9593 мс/раунд в производственном) и KV на 22% меньше | перплексия **+0.1876%** против **+0.0458%** у fp8 — вчетверо хуже. Уступает fp8 **только в качестве**, не в скорости и не в памяти (замер 2 сентября, `21_кодек_KV_fp8/`) |

### Круг 3 сентября: полная матрица пяти кодеков KV

Замерено на одной двоичной, менялся ровно один флаг `--kv-dtype`. Подробности —
[`21_кодек_KV_fp8/`](21_кодек_KV_fp8/).

| кодек | перплексия к int8 | AIME (60) | GPQA (198) | IFBench (300) | иголка | KV |
|---|---:|---:|---:|---:|:--:|---:|
| bf16 | −0.0062% | 54 | 168 | 179 | 1.0000 | 100% |
| int8 | — | 54 | **171** | 183 | 1.0000 | 51.6% |
| **fp8** | +0.0197% | **55** | 161 | **190** | 1.0000 | **50.4%** |
| k8v4 | +0.1280% | **55** | 165 | 180 | 1.0000 | 39.3% |
| nvfp4 | +0.2619% | 54 | 168 | 185 | 1.0000 | **28.1%** |

**Главный методический вывод круга: способности не отражают точность кодека.** Ранговая
корреляция Спирмена между местом по перплексии и местом на бенчмарке: GPQA **+0.325**,
IFBench **−0.500**, AIME **−0.125** — при пороге значимости \|ρ\| ≥ 0.90 и **разных знаках**.
`nvfp4`, чьё отставание по перплексии втринадцать раз больше, чем у `fp8`, набирает на GPQA
столько же, сколько `bf16`, и на семь задач больше `fp8`.

**Ранжирует кодеки только перплексия.** AIME, GPQA и IFBench годятся ровно на одно —
показать, что катастрофы нет; эту проверку прошли все пять, включая четырёхбитный `nvfp4`.
«Иголка» тоже насыщена: все пять рук дают идеальное извлечение до 260 тыс. токенов.

Собственные `ctest` движка по кодекам KV: **11 из 11**, плюс **3 из 3** на реальном
артефакте 35B.

**Про кодек KV — важная поправка к методу.** Первый замер (со спекуляцией) давал «fp8 +12.7%» и
переворачивал знак между длинами контекста: приёмка скакала от 0.373 до 0.995, потому что разные
кодеки дают разный текст, а от текста зависит число раундов. **Мерилось везение черновика, а не
цена кодека.** Правильный замер — `--mtp-draft-tokens 0`: без черновиков нет приёмки, и картина
становится монотонной. Строки выше — из правильного замера.

---

## 8. Закрыто замером — не возвращаться

| направление | почему |
|---|---|
| потолок среза MoE 4096 → 8192 | автор отклонил сделку «воркспейс против KV» |
| адаптивная глубина черновика | оракул +4.0% на прозе, +15.7% на JSON, но потолок держат **классы топологии графа**: k≥6 роняет `cudaErrorGraphExecUpdateFailure` |
| реквант MTP + shared expert (Идея 3) | потолок 2.91% раунда при пороге 8%; шаг черновика латентностный, не трафиковый |
| древовидная спекуляция | колонка дороже шага, дерево не окупается |
| `multicast::cluster` | медленнее в 600 раз; аппаратного пути на потребительском Blackwell нет |
| `cp.reduce.async.bulk` | MoE down не делает read-modify-write в глобальную память |
| batch-invariant greedy | «Greedy does not mean batch invariant» (#80) |
| расписания `nvfp4` при T=4 | 30 точек, ни одна не быстрее |
| путь масштабов `nvfp4` `StagedRaw` вместо `Direct` | −1.7% |
| параллелизм `recurrent_record` | удвоение нитей не даёт ничего |
| одноядерные маршруты gating при T=4 | все хуже |
| гейтинг GDN параллельно с qkvz на втором потоке | −0.73% |
| расписание `fp8_mma` в префилле | 13 точек, ни одна не быстрее; два CTA на SM обязательны внутри `cp.async`-структуры |
| приёмка по попаданию в ядро цели | +53.8% скорости ценой −26.7 пункта точности |
| норма в пролог/эпилог GEMM (Г9) | после пакета 08 пул норм упал с 3.93% до 2.2% раунда, и почти всё оставшееся — **пол запуска ядра** (~1.4 мкс × 228 запусков) |

**Общий вывод по декодному раунду:** он исчерпан механизмами. Голова верификации на 94.4% полосы,
черновая на ~98%, `recurrent_fold` на 94%, внимание на 82% и на длинном контексте это предел.

### Закрыто в круге 1–2 сентября, база `5973313d`

| направление | почему |
|---|---|
| узкий план MoE `<8,32>` — восемь варпов при BN 32 | −0.41% против −2.40% у `<4,32>` с плиткой 2×2. Под 2×2 при BN 32 выходит плитка **2×1**: 1.5 чтения на `mma` против 1.0. Форма при этом **невыразима** без нашей правки (`WarpCols = 4` ломает `static_assert`) — то есть пакет открывает форму, и она оказалась хуже |
| развести порог широкого плана отдельно для gate/up и для down | **по построению:** `route_job_bn` уходит в `scan`, а тот строит **один список работ на оба ядра**. Второй проход скана стоит 0.30–0.35% префилла, приз — 0.1% |
| геометрия `NStrip 16` для `gdn::state_passing` (256 блоков вместо 128 на 170 SM) | правка **побитовая**, но **+4.44%** по `state_passing` и +4.43% по `gdn::output`. Простаивающие SM дешевле, чем повторное чтение `W`, `U`, `k`, `g_cumsum` каждым блоком. Порог автора (`H_v >= 48`) для нашей геометрии верен |
| та же раскладка 4×1 в W8-ядрах общего эксперта | форма есть (`acc[4][4]`, `warp * 8`), правка перенеслась бы механически и побитово, но приз **0.2% префилла** — ниже порога, как и у ветки напарника `research/w8-moe-prefill-staged` (0.1%) |
| распаковка V из int8 в fp16 во внимании | **14 мс из 294** не-MMA времени: её делают выделенные варпы параллельно с MMA, она спрятана за конвейером |
| чтения V из общей памяти во внимании (`ldmatrix_x2_t`) | **ровно 0**: половина чтений не меняет ничего. Устройство цикла `PV` уже правильное — фрагмент `P` грузится раз на `k` и переиспользуется по всем `n`. **Идея «сделать во внимании то же, что в MoE» не работает: там уже сделано** |
| запись `P` в общую память | **5 мс из 294** (удвоение записей стоит +1.29%) |
| дедупликация экспертов в декоде | потолок **3.4% декода** при одной линии: четырёхкратное сокращение читаемых весов даёт `moe_d3` −15.6%, а `moe_d4` — **−2.5%**, то есть `moe_d4_token` в полосу не упирается |

**Общий вывод по вниманию в префилле:** четыре независимых долевых прибора, и все дают малое или
ноль — опознано **57 мс из 294**. **Дорогой операции в ядре нет.** Время съедает устройство:
92 672 байта общей памяти → **один CTA на SM**, конвейер со специализированными варпами и
барьерами, простой занять нечем. Единственный содержательный рычаг — уложиться в 50 КБ ради двух
блоков на SM; это большая перестройка, и оценивать её надо отдельно.
Единственный оставшийся структурный рычаг — **сокращать число ядер**: пол запуска даёт около
770 мкс = 4.8% раунда на ~550 мелких запусков.

---

## 9. Что торгуется прямо сейчас

> **Обновление 2 сентября.** Оба слота **освободились**: #140 вмержен как `0a204aee`, пакет 08 —
> как `9954867a`. Приоритет ниже переписан с учётом того, что появилось в круге 1–2 сентября.
>
> | приоритет | что | почему именно это |
> |---|---|---|
> | **1** | **П7 + П8 одной заявкой** (`perf/moe-warp-tile` @ `0e33d990`) | **один файл, +105/−83**, побитово, все гейты сняты: перплексия совпала до седьмого знака на миллионе токенов, `ctest` 102/102 на трёх деревьях, SASS после форматирования совпал, деградации нет на всём диапазоне промптов 48–32768. Файл тот же, что у `0a204aee`, но тот уже вмержен — конфликта нет |
> | 2 | **П9, ветка напарника `perf/pv-f16acc`** | крупнее по эффекту (−3.90% против −1.43%) и растёт с контекстом, но **не побитовая**; подавать **отдельно** от П7+П8, иначе смешанная сборка тянет лотерею приёмки. Ветка его, замер в движке мой |
> | 3 | **настроечные находки** (флаг черновой головы, окно MTP, чанк, кодек KV) | это не код, а умолчания; каждая меняет вывод, и подавать их можно только вместе с перплексией. Крупнейшая — `--lm-head-draft`, **+25.9% декода** при нуле строк кода |
> | 4 | П2, BN=128 | лежит в том же файле, что П7+П8; подавать после них |
>
> Прежний список слотов ниже оставлен как запись состояния на 1 сентября.

Слотов у автора **два**, занят один (#140).

| приоритет | что | почему именно это |
|---|---|---|
| **1** | **пакет 08, нормы** | переносится на текущий master **чисто**; 3 файла; трогает файлы, которых #140 не касается, — заявки не конфликтуют; эффект крупнейший среди готового |
| 2 | **П2, BN=128** | готово по механизму, но лежит в `sparse_moe_prefill_kernels.cu` — **том же файле, что #140**. Подавать только после того, как #140 закроется |
| 3 | **пакет 09, сэмплированный черновик** | самый крупный выигрыш из всего неподанного (+9…13% ток/с), но: конфликты переноса, 18 файлов, и **`eval/` не закончен** — без него доказательств для стохастического режима нет |
| 4 | **Д1, сигмоидный гейт в эпилоге внимания** | крупнейшее из стека по декоду; нужен соло-замер, сейчас число только «в стеке» |
