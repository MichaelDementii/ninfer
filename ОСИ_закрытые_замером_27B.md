# Оси, закрытые замером на плотной 27B. 1 сентября 2026

Каждая проверена **всеми доступными вариантами**, а не отвергнута рассуждением.
Ветка `all123` (пакеты 11+12+13), `qwen3_8_27b_nvfp4`, палиндром.

## Кандидат 6 — осушение конвейера между чанками. ОПРОВЕРГНУТ

`ctx_.synchronize()` в конце каждого чанка (`text_context_impl.h:1312`) убран целиком; всё идёт
по одному потоку, поэтому порядок ядер сохраняется.

| чанк | с осушением | без него | отношение |
|---:|---:|---:|---:|
| 1024 | 4263.0 / 4294.6 мс | 4261.5 / 4269.1 | **0.997** |
| 4096 | 3942.9 / 3945.5 | 3938.2 / 3938.2 | **0.9985** |

Обе разницы внутри шума: сама рука с осушением на чанке 1024 гуляет на 0.7%.

**Заявленный потолок «~6% префилла при чанке 1024» не подтверждается.** Хостовая работа GPU не
блокирует — очередь запусков её поглощает. Это ровно то объяснение, которое напарник и
предполагал («очередь переполняется, и время пишется в submit»), теперь оно проверено снятием.

## Кандидат 11 — подсказки вытеснения L2. ЗАКРЫТ, проверены ОБА направления

В дереве не было ни одного `cache_hint`. Добавлены `createpolicy.fractional.L2::evict_last/first`
и вариант `cp.async.bulk.tensor...L2::cache_hint`, включены в продюсер слитого nvfp4-SwiGLU —
это единственная геометрия на грани L2: веса 89.1 МБ плюс активации 10.5 МБ = 99.6 МБ при L2 96.

| вариант | T=2048 | T=4096 | T=8192 |
|---|---:|---:|---:|
| без подсказок | 620.5 мкс | 1171.5 | 2286.1 |
| держим **активации**, веса мимо | 702.5 (**+13.0%**) | 1336.3 (**+14.0%**) | 2633.2 (**+15.1%**) |
| держим **веса**, активации мимо | 627.7 (+1.2%) | 1182.7 (+1.0%) | 2316.8 (+1.3%) |

**Направление политики важно втрое больше, чем сама подсказка:** ошибка в назначении стоит 12
процентных пунктов. Горячи именно **веса** — при планировании CTA по выходным плиткам соседние
CTA делят веса, а не активации. Но и правильное направление проигрывает 1.0–1.3%, то есть цена
самих `createpolicy` и лишнего регистра больше, чем выигрыш от подсказки.

**Ось закрыта.** Обе стороны проверены, обе хуже умолчания.

## Кандидат 9 — L2-норма q/k внутрь свёртки. МЕЛОК на этой модели

`l2norm_warp_bf16x2` — **8.6 мс из 3884.8, то есть 0.2% префилла**. Даже полное исчезновение
ядра вместе с его круговоротом даёт единицы десятых процента, а не заявленные «2.2 мс на чанк».
Правку не писал: приз меньше разрешения сквозного прибора.

## Ручка `NStrip` в `state_passing` GDN. ЗАКРЫТА

| NStrip | сетка | время |
|---:|---:|---:|
| **16** (текущее, при `H_v >= 48`) | 384 CTA | **704.5 / 704.5 мкс** |
| 32 | 192 CTA | 745.5 / 745.5 (**+5.8%**) |

Выбор автора верен.

## Расписание fp8-TMA. ЗАКРЫТО

Свип BlockM × Stages по всем пяти геометриям префилла: три стадии ломают длинное K (+25%),
BlockM=128 ломает слитый SwiGLU (+74%). Единственный кандидат — специализация входа GDN на
M128 S4 — дал **−1.9% в бенче и +2.3% в движке**. Подробности — `ОСЬ_fp8_расписание_TMA.md`.

## Расписание декодного nvfp4. ЗАКРЫТО

Четыре свободные ручки, четыре проигрыша: `Nvfp4CodeCache::Streaming` +8.8%,
`kValuesPerLane = 32` +27%, `kAccumulatorChains = 4` с `kPhaseUnroll = 2` +7.5% на T=4 и **+787%**
на T=16 со спиллом (`STACK:616`). Подробности — `БЮДЖЕТ_декода_27B_2026-09-01.md`.

## Общий приборный вывод

Из семи закрытых осей **пять** закрылись только потому, что вариант был собран и замерен:
рассуждение от потолков, от занятости и от изолированного бенча в трёх случаях из семи давало
противоположный ответ. Список приборных ловушек, на которые я наступил за вечер, — в конце
`КАРТА_префилла_27B_чем_ограничено.md`.
