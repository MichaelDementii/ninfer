# Ось закрыта: расписание fp8-TMA не улучшается ни глобально, ни по геометриям

Замерено 1 сентября 2026. Инстанс 49380254, RTX 5090, `qwen3_8_27b_nvfp4.ninfer`, T=4096.
База — ветка `all123` (пакеты **11+12+13** вместе; сливаются чисто, конфликтов нет).

## Зачем смотрел

fp8-GEMM — **38.2% времени префилла на 27B**, самый крупный блок. Расписание маршрута TMA из
пакета 11 задано **одним типом на все геометрии**: `Fp8A8TmaSchedule<256, 4, 1>`, без
специализаций, хотя у маршрута MMA специализации по геометриям есть.

## Достигнутый ярус по геометриям (11+12+13)

Потолок — **кормимый** fp8 526.9 TOP/s, а не спецификационные 419, по которым бенч печатает `TC_%`.

| геометрия | % префилла | TFLOP/s | % кормимого потолка |
|---|---:|---:|---:|
| 16384×5120 — вход GDN | 16.3% | 463.0 | 87.9% |
| 5120×6144 — `linear_add` | 8.4% | 411.2 | 78.0% |
| 34816×5120 — слитый SwiGLU | 5.7% | 482.6 | 91.6% |
| 14336×5120 — вход внимания | 4.8% | 474.6 | 90.1% |
| 5120×17408 — `linear_add` | 3.0% | 439.6 | 83.4% |

Взвешенно **86.4%**. Слабее всех — обе геометрии с узким N=5120.

## Свип BlockM × Stages, палиндром, все пять геометрий

| вариант | add 6144 | add 17408 | SwiGLU | вход GDN | вход внимания |
|---|---:|---:|---:|---:|---:|
| **M256 S4** (текущее) | 626.7 | **1656.8** | **3022.8** | 1479.9 | **1266.2** |
| M256 S3 | 626.7 | 2068.9 (**+25%**) | 3078.1 | 1479.5 | 1266.5 |
| M128 S4 | 628.7 | 1670.1 | 5263.4 (**+74%**) | **1451.7 (−1.9%)** | 1278.9 |
| M128 S6 | 632.8 | 1716.2 | 5227.5 | 1453.8 | 1278.7 |

Два жёстких вывода сразу:

* **Три стадии ломают длинное K**: на `linear_add` k=17408 это +25%. Четыре — правильный выбор.
* **BlockM=128 ломает слитый SwiGLU**: +74%. Глобально ставить 128 нельзя.

Оставался ровно один кандидат: специализировать **только вход GDN** на M128 S4, где изолированный
бенч давал −1.9% на 16.3% префилла.

## И вот тут изолированный бенч соврал

Специализация написана (`Fp8LinearA8TmaSchedule<Fp8GdnInputGeometry>` = `<128, 4, 1>`), собрана,
двенадцать тестов fp8/nvfp4/gdn/attn проходят, контроль на слитом SwiGLU не сдвинулся
(3026.9/3021.8 против 3022.8/3022.8). Операторный бенч подтвердил: 1480.3 → 1452.6, **×0.981**.

**В движке всё наоборот** (`nsys --cuda-graph-trace=node`, префилл 32768, два круга):

| | ядро входа GDN | всё GPU префилла |
|---|---:|---:|
| M256 S4 | 619.9 / 621.2 мс | 3884.8 / 3894.3 мс |
| M128 S4 | 634.4 / 635.5 (**+2.3%**) | 3910.5 / 3918.6 (**+0.65%**) |

**Механизм.** BlockM=128 удваивает число M-плиток, а каждая плитка перечитывает всю матрицу
весов. В изолированном бенче ядро одно и L2 горячий — перечитывание бесплатно. В движке L2 занят
соседними ядрами, и удвоенное перечитывание становится настоящим трафиком.

**Ось закрыта. Глобальное `Fp8A8TmaSchedule<256, 4, 1>` из пакета 11 верно для всех пяти
геометрий.**

## Приборный урок, третий за вечер и в третью сторону

* пакет 13: изолированный стенд подвоза **не предсказал** слитое ядро (сказал выигрыш, получилась
  регрессия 9%);
* память, правило 5: операторный бенч **занижает** выигрыш от развязки стейджинга (−4.2% против
  −8.4% в движке);
* здесь: операторный бенч **завышает** выигрыш от уменьшения плитки, потому что в одиночку у ядра
  горячий L2, а в движке — нет.

Общее правило: **любая правка, меняющая число перечитываний через L2, обязана мериться в движке.**
Размер плитки, число стадий, порядок растеризации — всё это про перечитывание.

## Сырое

* Ветки на сервере: `all123` (11+12+13), `fp8gdn` (специализация, отвергнута)
* Бинари: `/root/bins/F_M{256,128}S{3,4,6}_*`, `/root/bins/GS_*`
* Трассы: `/tmp/g_A123{1,2}.nsys-rep`, `/tmp/g_GS{1,2}.nsys-rep`
