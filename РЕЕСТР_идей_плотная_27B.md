# Реестр идей для плотной 27B: состояние каждой. 1 сентября 2026

Итог кампании по `qwen3_8_27b_nvfp4` на RTX 5090. Все замеры — ветка `all123` (пакеты 11+12+13,
сливаются чисто), палиндром, `nsys --cuda-graph-trace=node` там, где эффект меньше процента.

**Правило реестра: «закрыто» означает, что вариант собран и замерен, а не отвергнут рассуждением.**
Там, где вариант не собирался, стоит «оценено» с указанием, что именно не проверено.

## Карта: где вообще лежит время

**Префилл 3884.8 мс** (промпт 32768, чанк 4096, KV int8):

| семейство | % | доля MMA (абляция) | чем ограничено | КПД |
|---|---:|---:|---|---:|
| внимание `prompt_bf16/i8` | 27.5% | **34%** | 34% счёт, 66% память | 73.8% яруса 266 |
| fp8-GEMM (5 геометрий) | 38.2% | 39–48% | **счёт, MMA на критическом пути** | 84–92% яруса 526.9 |
| nvfp4-GEMM | 22.0% | **8%** | подвоз 59%, эпилог 9…21% | ярус не рычаг |
| GDN (6 ядер) | 8.4% | — | **трафик, ×5.3 усиление** | 59% пика HBM |
| квантователи | 1.6% | — | память, **91% пика** | оптимален |
| нормы | 1.5% | — | — | правит пакет 08 |

**Декод, раунд 16.4 мс:** `nvfp4_linear_swiglu_small_t` 21.8%, `fp8_small_t` 14.6%,
`nvfp4_small_t` 11.6%, внимание 1.1% (3.4% при контексте 16k).

## Закрыто замером — вариант собран и померен

| идея | как проверено | результат |
|---|---|---|
| **12** d-split во внимании | свип фактора внутри `prompt_i8`: 8 / 16 / 32 варпа | 8 варпов −27%, 32 не запускаются (регистры). Потолок по варпам 17, текущие 16 на границе железа |
| **1-А** f16-аккумулятор PV | напарник реализовал на ветке автора | **+4.4% скорости, но 64k «иголка в стоге» падает 3/3.** Ярус ×2 подтверждён мной отдельно (`accpeak.cu`) |
| **6** осушение между чанками | `ctx_.synchronize()` убран целиком | чанк 1024 ×0.997, чанк 4096 ×0.9985 — **внутри шума**. Заявленных ~6% нет |
| **11** подсказки вытеснения L2 | добавлен `L2::cache_hint`, проверены **оба** направления | держим активации **+13…15%**, держим веса **+1.0…1.3%**. Обе хуже |
| **19** политика сплитов декода | окна по обе стороны границ ярусов | вдвое меньше активных сплитов — **то же время до третьего знака** |
| расписание fp8-TMA | свип BlockM × Stages по пяти геометриям | S3 ломает длинное K (+25%), M128 ломает SwiGLU (+74%). Специализация входа GDN: бенч −1.9%, **движок +2.3%** |
| расписание декодного nvfp4 | четыре свободные ручки | `Streaming` +8.8%, `kValuesPerLane=32` +27%, `chains=4`+`unroll=2` +7.5% и **+787%** со спиллом |
| подвоз декодного ядра | ядро только с подвозом, холодная память | **1783 ГБ/с = 99.5% пика.** Барьер и стейджинг бесплатны |
| `NStrip` в GDN | обе точки | 16 верно, 32 хуже на 5.8% |

## Приз измерен, правка не написана — что мешает, указано

| идея | потолок на нашей модели | что мешает |
|---|---:|---|
| **эпилог nvfp4: не транспонировать либо персистентная CTA** | **3.5% префилла, ИЗМЕРЕНО абляцией** | эпилог 8.9% ядра при K=17408, 20.8% при K=6144, **17.2% у слитого SwiGLU**. Дорога не запись (1.1…4.5%), а круг транспонирования через shared. Качество не затрагивается вовсе |
| **16** квантование в эпилог | **1.7% префилла** | квантователь уже на 91% пика; экономится 285 МБ круга на пару. Но самая жирная пара висит за **SwiGLU**, а не за нормой, и эпилогов с квантованием в дереве нет |
| **h_chunk без материализации** в GDN | 2.3% префилла | заблокировано разбиением: `state_passing` владеет полосой состояния через все чанки, `output` разбит по (чанк, голова) |
| **1-Б** операнды PV в fp8 | часть от 66% памяти внимания | убирает стейджинг `v_f16` (32 КиБ из 92); аккумулятор остаётся f32, механизма отказа маршрута А нет |
| **4** конвейеризация K-шага | до 1.5% префилла | ограничен 22% обвязки nvfp4-ядра |
| **5** `BlockK = 256` | **ЗАКРЫТ конструкцией** | стадия становится 55 296 Б: две стадии это 110 592 при потолке общей памяти 101 376. Влезает только с `BlockM = 128`, а он в свипе пакета 12 уже дал −20% |
| **8** состояние GDN в FP16 | ≈0.5% раунда + 144 МиБ VRAM | **в префилле почти ничего**: `h_chunk` уже BF16, в FP32 только `g_cumsum` (0.2%) |
| **9** L2-норма внутрь свёртки | 0.2% префилла | `l2norm` это 8.6 мс из 3884.8 |
| **20** редуктор split-K декода | ≈0.6% раунда | 18% от 3.4% внимания в раунде |

## Не относится к плотной 27B

| идея | почему |
|---|---|
| **2, 3, 15, 15-бис, 17** | все про MoE, а у 27B его нет — в трассах префилла и декода **ни одного** `sparse_moe`-ядра |
| **1-бис-А** целочисленный ярус | про маршруты q4/q5/q6/w8 с разжатием в bf16; на 27B их нет (38.2% это `fp8_mma`, 22.0% `nvfp4_*_tma`) |
| **7** кольцо ReplaySSM, **13** единый проход по KV | оба про широкое окно спекуляции; наш канонический режим mtp3, то есть T=4 |
| int8-KV ради префилла | **ноль**: внимание 1117.1 мс на int8 против 1120.1 на bf16 |
| **18** гранулярное разбиение головы | то же ядро, что 12 |

## Требует офлайн-работы, не ядра

| идея | состояние |
|---|---|
| **O-1** сузить черновую голову | сетка покрытия уже в репозитории, нужна пересборка артефакта |
| **O-2 / Q-2** голова 27B в Q6 / NVFP4 | пересборка артефакта |
| **Q-1** GPTQ вместо RTN | офлайн-квантование плюс батарея точности |
| **S-0** гейт по качеству для разрежённости | один проход Wanda, ноль строк CUDA; **до** него оба ядерных кандидата S-1/S-2 писать нельзя |

## Приборные уроки, стоившие ошибок

1. **Долю инструкции меряют абляцией.** На вопрос «сколько тут MMA» три прибора дали 92.6%
   (от спецификационного потолка бенча), 2.9% (сравнение разных ядер) и **33.8%** (абляция).
   Верна абляция: её прогноз 4.7% сошёлся с независимым замером напарника 4.4%.
2. **Знаменатель — измеренный пик, а не спецификация.** Бенчи печатают спец-числа (419 у fp8,
   209.5 у bf16), измеренные — 530.8 и 266.
3. **«% от тензорного пола» — не запас, пока не посмотрел `logical GB/s`.** У внимания он
   1000–1800% от DRAM: ядро ограничено shared.
4. **Правку, меняющую число перечитываний через L2, мерить только в движке.** Плитка fp8
   256→128: бенч −1.9%, движок +2.3%.
5. **Стенд «только подвоз» врёт двумя способами**, и оба дают числа выше пика HBM: сток,
   недостижимость которого доказуема, и буфер меньше L2.
6. **Изолированный стенд не предсказывает слитое ядро** (пакет 13: сказал выигрыш, вышла
   регрессия 9%).
