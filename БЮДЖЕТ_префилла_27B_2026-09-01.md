# Бюджет префилла на 27B-nvfp4, поядерно, для обоих типов KV. Замерено 1 сентября 2026

Спутник к `БЮДЖЕТ_декода_27B_2026-09-01.md`. Нужен, чтобы кандидаты очереди взвешивались
по нашей модели, а не по чужой.

Инстанс 49380254, RTX 5090, `qwen3_8_27b_nvfp4.ninfer`, промпт 32768, чанк 4096,
`nsys -t cuda --cuda-graph-trace=node`, ветка `p13c` (пакеты 11+12+13 применены).

## ⚠️ Умолчание KV — bf16, а вся кампания мерила int8

`include/ninfer/types.h:117` и `:679`, `apps/cli/options.h:26` — `KvCacheStorage::BFloat16`.
Наши стенды всегда передают `--kv-dtype int8`. То есть **кампания мерила не тот путь, который
получает пользователь из коробки**. Поэтому ниже обе колонки.

| доля (int8) | доля (bf16) | ядро | пусков |
|---:|---:|---|---:|
| **27.5%** (1117.1 мс) | **27.7%** (1120.1 мс) | `causal_attention_prompt_*` | 128 |
| 16.3% (663.2) | 16.3% (660.2) | `fp8_mma` (A) | 384 |
| 15.1% (614.5) | 14.9% (604.6) | `nvfp4_linear_swiglu_w4a4_tma` | 448 |
| 8.4% (343.3) | 8.5% (342.6) | `fp8_mma` (B) | 512 |
| 6.9% (278.9) | 6.7% (271.4) | `nvfp4_w4a4_tma` | 448 |
| 5.7% (232.6) | 5.7% (231.6) | `fp8_mma` (C) | 64 |
| 4.8% (194.7) | 4.8% (193.9) | `fp8_mma` (D) | 128 |
| 3.3% (133.0) | — | GDN `state_passing` | 384 |
| 1.7% (68.6) | — | GDN `prepare_wy` | 384 |
| 1.5% (60.0) | — | GDN `output` | 384 |

Сводно по семействам (int8): **внимание 27.5%, fp8-GEMM 38.2%, nvfp4-GEMM 22.0%, GDN 6.5%.**

## Три вывода

**1. `--kv-dtype int8` не даёт в префилле ничего.** Внимание стоит 1117.1 мс на int8 и
1120.1 мс на bf16 — разница 0.27%, то есть шум. Байты KV вдвое меньше, а время то же: в
префилле внимание упирается в счёт (QK и PV на тензорных ядрах), а не в чтение кеша. Выигрыш
int8-кеша — это ёмкость и декод, но не TTFT. Прежние заявления кампании про int8 в префилле
надо перечитать с этим в руках.

**2. Кандидат 12 на нашей модели весит 27.7%, а не 41.6%.** Напарник поставил ему 41.60%
по своей переписи; на `qwen3_8_27b_nvfp4` при промпте 32768 это **27.7%**. Кандидат от этого не
умирает — он всё равно **первое место с отрывом**, — но арифметику потолка надо пересчитать.

**Достигнутый ярус, чтобы не гнаться за занятостью впустую.** Работа внимания при L=32768,
16 слоёв полного внимания, 24 головы, d=256: QK и PV по `L²/2 · heads · d · 2` = 2.11·10¹⁴ FLOP.
За 1120 мс это **188 TFLOP/s при измеренном пике bf16 в 266**, то есть **71%**. Значит запас
есть — около 29% ядра, то есть **до 8% префилла**, — и оговорка автора кандидата («если ядро
уже берёт 80% своего яруса, поднимать занятость не с чего») **снята замером**: берёт 71%.

**3. Кандидат 1-бис-А (целочисленный ярус) к нашей модели почти не относится.** Он про маршруты
q4/q5/q6/w8, которые разжимаются в bf16. На 27B-nvfp4 таких ядер в префилле нет вовсе: там
`fp8_mma` (38.2%) и `nvfp4_*_tma` (22.0%). fp8-ядра после пакета 11 идут на 88–90% **своего**
яруса 526.9, и переход на int8 поднял бы потолок вдвое — но это смена артефакта, а не правка
ядра. Потолок 1.83× относится к 35B-A3B, и на нашей модели его подтверждать нечем.

## Что это меняет в очереди

| кандидат | было | стало по замеру |
|---|---|---|
| 12, d-split в bf16-внимании | «41.6% префилла» | **27.7% на 27B, ядро на 71% яруса, запас до 8% префилла** — первое место сохраняется |
| 1-бис-А, целочисленный ярус | «потолок 1.83×» | на 27B-nvfp4 **неприменим**: маршрутов с разжатием в bf16 нет |
| всё, что про int8-KV ради префилла | подразумевалось ускорение | **ноль**: 1117 против 1120 мс |

## Сырое

* `/tmp/pk_U01.nsys-rep` (int8), `/tmp/pfbf16.nsys-rep` (bf16) на сервере
* Оба сняты одним бинарём `N1_bench` (ветка `p13c`), различие только во флаге `--kv-dtype`
