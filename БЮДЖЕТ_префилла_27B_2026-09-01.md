# Бюджет префилла на 27B-nvfp4, поядерно, для обоих типов KV. Замерено 1 сентября 2026

Спутник к `БЮДЖЕТ_декода_27B_2026-09-01.md`. Нужен, чтобы кандидаты очереди взвешивались
по нашей модели, а не по чужой.

Инстанс 49380254, RTX 5090, `qwen3_8_27b_nvfp4.ninfer`, промпт 32768, чанк 4096,
`nsys -t cuda --cuda-graph-trace=node`, ветка `p13c` (пакеты 11+12+13 применены).

## ⚠️ Умолчание KV — bf16, а вся кампания мерила int8

`include/ninfer/types.h:117` и `:679`, `apps/cli/options.h:26` — `KvCacheStorage::BFloat16`.
Наши стенды всегда передают `--kv-dtype int8`. То есть **кампания мерила не тот путь, который
получает пользователь из коробки**. Поэтому ниже обе колонки.

| доля (int8) | доля (bf16) | ядро | пусков |
|---:|---:|---|---:|
| **27.5%** (1117.1 мс) | **27.7%** (1120.1 мс) | `causal_attention_prompt_*` | 128 |
| 16.3% (663.2) | 16.3% (660.2) | `fp8_mma` (A) | 384 |
| 15.1% (614.5) | 14.9% (604.6) | `nvfp4_linear_swiglu_w4a4_tma` | 448 |
| 8.4% (343.3) | 8.5% (342.6) | `fp8_mma` (B) | 512 |
| 6.9% (278.9) | 6.7% (271.4) | `nvfp4_w4a4_tma` | 448 |
| 5.7% (232.6) | 5.7% (231.6) | `fp8_mma` (C) | 64 |
| 4.8% (194.7) | 4.8% (193.9) | `fp8_mma` (D) | 128 |
| 3.3% (133.0) | — | GDN `state_passing` | 384 |
| 1.7% (68.6) | — | GDN `prepare_wy` | 384 |
| 1.5% (60.0) | — | GDN `output` | 384 |

Сводно по семействам (int8): **внимание 27.5%, fp8-GEMM 38.2%, nvfp4-GEMM 22.0%, GDN 6.5%.**

## Вывод 1. `--kv-dtype int8` не даёт в префилле ничего

Внимание стоит 1117.1 мс на int8 и 1120.1 мс на bf16 — разница 0.27%, то есть шум. Байты KV
вдвое меньше, а время то же: в префилле внимание упирается в счёт (QK и PV на тензорных ядрах),
а не в чтение кеша. Выигрыш int8-кеша — это ёмкость и декод, но не TTFT. Прежние заявления
кампании про int8 в префилле надо перечитать с этим в руках.

## Вывод 2. Кандидат 12 (d-split в bf16-внимании) ЗАКРЫТ

Ядро весит 27.7% префилла на нашей модели, а не 41.6% из переписи напарника. Но дело не в весе.

Считать надо не от сырого пика bf16 (266 TOP/s), а от **смешанного тензорного потолка QK+PV**,
который `ninfer_causal_softmax_attention_bench` строит сам (он же печатает `mixed_tc_floor` и
`mixed_tc_roofline`). При W=4096, контекст 4096, eager, палиндром не нужен — разброс 0.2%:

| KV | время | TFLOP/s | % своего потолка |
|---|---:|---:|---:|
| bf16 | 3187.0 мкс | 194.08 | **92.6%** от 209.5 |
| int8 | 3118.4 | 198.35 | **94.7%** от 209.5 |
| fp8 | 2902.2 | 213.12 | **76.3%** от 279.3 |

bf16-ядро берёт **92.6% своего потолка при занятости 8.3%** — четыре варпа этот ярус уже
выбирают. Оговорка автора кандидата — «если ядро уже берёт 80% своего яруса, поднимать занятость
не с чего, и кандидат закрывается» — **срабатывает**.

Косвенно это же видно по регистрам: `prompt_bf16` идёт при `REG:254`, впритык к потолку 255, и
всё равно держит 92.6% яруса. Разбиение по D сняло бы регистровое давление (у `prompt_i8` с
d-split `REG:120` при 16 варпах), но брать этим нечего.

⚠️ **Моя ошибка, исправлена.** Часом раньше я записал в очередь «ядро берёт 71%, оговорка снята»,
посчитав от сырого пика bf16. Знаменатель был не тот. Правильное число 92.6%, вывод
противоположный.

## Вывод 3. У fp8-пути 76.3% — это НЕ запас. Ось уже закрыта 1 сентября

Соблазн очевиден: fp8 самый быстрый из трёх (2902 против 3187 у bf16), а до своего потолка
недобирает 24%. Ровно этот сигнал я уже разбирал — `ОТВЕРГНУТО_внимание_fp8_KV.md`, и он ложный.

Рядом бенч печатает `logical= 17942.3 GB/s (1001.2% of 1792)` при `physical_kv_read = 5.8 GB/s
(0.3%)`. Из DRAM ядро почти не читает: весь трафик идёт через кеш и **shared** — стейджинг K и V
плюс круг матрицы P между варпами-производителями и варпами-потребителями. Тысяча процентов от
DRAM-полосы означает, что ограничение памятное, а не тензорное.

Прямое подтверждение уже есть: перебалансировка варпов 8 QK / 8 PV → 4 QK / **12 PV** сделала
ядро на **5.2% хуже**. Дать PV больше варпов — хуже, значит PV не узкое место. И косвенное: три
варианта лежат в пределах 10% друг от друга, хотя стоимость их QK различается **вчетверо**.
Если бы время задавали тензорные ядра, разброс был бы кратно больше.

⚠️ Я второй раз за вечер наступил на один и тот же знаменатель. Правило из того же разбора:
**процент от тензорного пола в этом бенче нельзя читать как «столько можно взять», рядом всегда
надо смотреть на `logical GB/s`.**

**Что при этом остаётся живым.** Кандидат 1, маршрут Б (операнды PV в fp8 с f32-аккумулятором)
целится не в ярус, а в **трафик shared**: он убирает стейджинговый тайл `v_f16` — 32 768 байт из
92 416 — и весь деквант V. Это ровно то направление, на которое указывает мой же разбор
(«целиться надо в трафик shared, а не в баланс варпов»). Маршрут А (f16-аккумулятор со
сворачиванием) яруса PV касается и потому этим замером не поддержан.

## Вывод 4. Кандидат 1-бис-А (целочисленный ярус) к 27B-nvfp4 не относится

Он про маршруты q4/q5/q6/w8, которые разжимаются в bf16. На 27B-nvfp4 таких ядер в префилле нет
вовсе: там `fp8_mma` (38.2%) и `nvfp4_*_tma` (22.0%). fp8-ядра после пакета 11 идут на 88–90%
**своего** яруса 526.9. Потолок 1.83× посчитан по 35B-A3B, и на нашей модели его подтверждать
нечем.

## Что это меняет в очереди

| кандидат | было | стало по замеру |
|---|---|---|
| 12, d-split в bf16-внимании | «41.6% префилла», первое место | **ЗАКРЫТ**: ядро берёт 92.6% своего потолка |
| 1, ярус PV | «×2 на внимании» | жив **только маршрут Б**, и не из-за яруса: ядро ограничено shared, а маршрут Б убирает стейджинг `v_f16` (32 КиБ из 92) |
| 1-бис-А, целочисленный ярус | «потолок 1.83×» | на 27B-nvfp4 **неприменим** |
| всё, что про int8-KV ради префилла | подразумевалось ускорение | **ноль**: 1117 против 1120 мс |

## Сырое

* `/tmp/pk_U01.nsys-rep` (int8), `/tmp/pfbf16.nsys-rep` (bf16) на сервере
* Оба сняты одним бинарём `N1_bench` (ветка `p13c`), различие только во флаге `--kv-dtype`
* Ярусы: `ninfer_causal_softmax_attention_bench --entry append --geometry d256-h24-kv4
  --kv-dtype all --batch 1 --tokens 4096 --context 4096 --execution eager`

---

## Кандидат 12: проверка вариантов, а не только потолка

Закрывать по одной модели потолка мало, поэтому d-split проверен ещё и прямым свипом **внутри
самого маршрута, где он уже есть** — у `prompt_i8` фактор разбиения задан константой
`kCausalPromptI8Warps` (и запинен `static_assert(kCausalPromptI8DConsumers == 4)`).
Оба ассерта ослаблены, палиндром, int8, W=4096, контекст 8192:

| варпов | d-split | время | % потолка |
|---:|---:|---:|---:|
| 8 | ×2 | 6423.1 / 6435.6 мкс | 76.6% |
| **16** (текущее) | **×4** | **5046.1 / 5045.1** | **97.5%** |
| 32 | ×8 | **не запускается** | — |

**Три вывода.**

1. **Разбиение по D действительно решает: вдвое меньше — на 27% хуже.** Но замер
   ⚠️ **смешанный**: `ProducerWarps = RowTiles = 4` фиксировано, поэтому переход 16 → 8 варпов
   меняет не только d-split, но и отношение производителей к потребителям с 4:12 на 4:4.
   Чистой изоляции d-split этот свип не даёт, и выдавать 27% за цену одного только d-split нельзя.
2. **Больше 16 варпов невозможно физически.** 32 варпа = 1024 нити при `REG:120` требуют
   122 880 регистров на SM против 65 536 — `cudaErrorLaunchOutOfResources`. Потолок по регистрам
   — 546 нитей, то есть 17 варпов. Текущие 16 стоят ровно на границе железа. Ось «дать больше
   потребителей» закрыта.
3. **И главный аргумент против переноса d-split в bf16.** Архитектура с разбиением покупает всего
   **2.9 процентных пункта** (97.5% против 94.6% у bf16) — и это при том, что QK у int8 идёт по
   ярусу вчетверо дешевле. То есть сама схема «producer/consumer + P через shared» стоит почти
   столько же, сколько приносит: круг матрицы P через общую память — это ровно тот трафик, в
   который ядро и упирается (`ОТВЕРГНУТО_внимание_fp8_KV.md`).

**Что не проверено и почему не стал.** Настоящий порт d-split в `prompt_bf16` не написан: это
~350–400 строк перестройки на producer/consumer с кругом P через shared. Потолок для него —
5.4% ядра при W=4096 (100 − 94.6), то есть **1.5% префилла**, а плата — тот самый круг P.
Соотношение риска к отдаче не оправдывает; если автор захочет, все три замера выше у него есть.

**Кандидат 12 отмечен закрытым.**
