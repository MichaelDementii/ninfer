# Что ответили по #112 и #113 — заметки напарнику

Состояние перепроверено по живым страницам GitHub **29 августа, 14:05 UTC**.

## Коротко

* **Мейнтейнер не отвечал ни там, ни там.** В обоих PR в разделе «participants» стоит один человек —
  ты. Ни комментария, ни ревью, ни лейбла, ни ответа на вопрос про процесс в теле #113.
* **#112** — ревью бота Codex завершено, **замечаний нет**. Делать ничего не надо.
* **#113** — ревью Codex завершено, **одно замечание, P2**. Это единственное, что требует действия.

---

## #112 — чисто

Бот отчитался `Code Review ✅ Completed 2026-08-29T11:31:58Z` на коммите `e591725` и не оставил ни
одного тред-комментария. По его же описанию он комментирует только когда есть что сказать, а иначе
ставит 👍. То есть по коду вопросов нет.

Ждём мейнтейнера. Ничего не трогаем.

---

## #113 — одно замечание, и оно верное

Тред `discussion_r3886491016`, к строкам +39..+40 (`nvfp4_linear_swiglu_plan.cpp`), бейдж **P2**,
со ссылкой на `AGENTS.md:L280-L282`:

> **Add coverage for a newly fused token width.** This predicate changes both the workspace path and
> floating-point arithmetic for 256, 512, 768, and larger multiples, but
> `tests/ops/linear_swiglu/test_nvfp4.cpp` still tests only `{5, 48, 49, 128, 1024}`; 1024 already
> used the TMA route before this commit. Consequently, the reported `ctest` run exercises none of
> the newly routed cases and cannot catch launch, workspace, or numerical regressions in this
> change. Adding at least T=256 would cover the new predicate without increasing the test oracle's
> existing maximum extent of 1024.

**Спорить не с чем, и это ровно тот вопрос, который у нас самих стоял в черновике вопросом 2** —
входит ли расширение `kA4Cases` в заявку. В отправленное тело PR вопрос не попал, и бот задал его
за нас. Факт верный: `ctest` на ветке действительно не исполняет ни одной новой ширины.

### Правка — одна строка

`tests/ops/linear_swiglu/test_nvfp4.cpp:13`:

```diff
-        constexpr std::array<std::int32_t, 5> kA4Cases{5, 48, 49, 128, 1024};
+        constexpr std::array<std::int32_t, 6> kA4Cases{5, 48, 49, 128, 256, 1024};
```

### Что я проверил — и проверил на голове PR, а не на нашей ветке

Собрал `fe6f70d1` и его родителя `ce09aee5` в одном дереве, добавил 256 в обе руки:

| | ветка `fe6f70d1` | база `ce09aee5` |
|---|---|---|
| `ninfer_linear_swiglu_nvfp4_test` | `OK` | `OK` |
| `T = 256`, `rel_l2` против FP64-оракула | **0.058382** | 0.059000 |
| доля критерия | **36.49%** | 36.88% |
| `gross_ratio` | **0.4856** | 0.5315 |
| `ctest -j1` | `100% passed, 0 failed out of 95`, 1 skipped | то же |

Все остальные строки таблицы A4 совпадают на обеих руках байт в байт, как и должно быть.

**Важная мелочь про 95 против 94.** В теле PR стоит «out of 94» — это счёт на `1fc1cb76`, где все
числа и снимались. Голова PR стоит на `ce09aee5`, а тот коммит добавляет
`tests/test_http_transport.cpp`, поэтому на голове сюита — **95** тестов. Противоречия нет, но в
ответе надо писать 95, иначе выглядит как расхождение.

### Готовый текст ответа в тред

> Fair, and it is the right objection: `ctest` on this branch does not execute the predicate the
> branch adds. Pushed as TBD_SHA — `T = 256` joins `kA4Cases`.
>
> 256 rather than a wider multiple because it is the narrowest width the new predicate admits, so it
> is the one case that exercises both new legs at once: the new leg of `resolve_route` and the new
> leg of `nvfp4_linear_swiglu_workspace_capacity_bytes`. It is also a `T` below the old `kPrimaryT`,
> which is a shape the old route could not produce at all. The criterion is untouched and the case
> list's maximum extent stays 1024.
>
> It is also not decoration. Moving the route without widening the capacity query makes the shipped
> Op test report `exact workspace query/execution high-water mismatch` at 256, 512 and 768 — that is
> how I found the second half of this change. `T = 256` is the case that would have caught it, and
> without it nothing in `ctest` would.
>
> It is a case both arms pass, which is what makes it a regression test rather than a branch-only
> one. With `NINFER_OP_REPORT_STATS=1` at `T = 256`, the parent commit reports relative L2 0.059000
> against the FP64 oracle — 36.9% of the criterion — through `LinearW4A4Post`, and this branch
> reports 0.058382, 36.5%, through the fused route; the gross ratio moves 0.531 → 0.486. Both print
> `OK`, and every other row of the A4 table is identical between the two.
>
> `cd build && ctest -j1`: `100% tests passed, 0 tests failed out of 95`, 1 skipped, on the branch
> and on its parent built in the same directory in the same run. The suite is 95 rather than the 94
> quoted in the description because `ce09aee5` adds `tests/test_http_transport.cpp`; the description
> quotes the count at `1fc1cb76`, where the numbers were taken.
>
> The submission is now two files: the route table and this one line of the test.

### Порядок действий

1. Добавить строку в `tests/ops/linear_swiglu/test_nvfp4.cpp` на ветке
   `perf/nvfp4-fused-swiglu-every-width`, отдельным коммитом поверх `fe6f70d1`.
2. Запушить в форк.
3. Вставить настоящий sha вместо `TBD_SHA` и отправить ответ **в тред**
   `discussion_r3886491016`, а не отдельным комментарием.
4. Тред не резолвить самим — это ревью бота, пусть закрывается сам или мейнтейнером.

---

## Ловушка: `branch.bundle` в `02_nvfp4_swiglu/` старше головы PR

Сверил диффы. Наша сохранённая ветка `436aba7b` и голова PR `fe6f70d1` **различаются
содержательно**:

| | наша ветка | голова PR |
|---|---|---|
| граница маршрута | `tokens >= kPrimaryT` (1024) | `tokens >= kTmaBlockM` (**256**) |
| запрос ёмкости | `max_tokens >= kPrimaryT` | `max_tokens >= kTmaBlockM` |
| `kPrimaryT` | оставлена | **удалена** |

То есть подана **опущенная граница**, и это правильно — она и даёт строки 256/512/768 с ×1.117 /
×1.366 / ×1.293. Просто помни: любые перезамеры снимать на `fe6f70d1`, а не из бандла в папке.

---

## Чего сейчас делать не надо

**Не подавать кандидатов 3 и 4a.** Оба готовы целиком — тексты, бандлы, `make_pr.py` с прибитым
коммитом, — но апстрим держит не больше двух открытых вещей от одного человека, а #112 и #113
открыты. Как только один из них закроется или вольётся, следующая заявка идёт по порядку:

1. **кандидат 3** — `03_moe_stage_from_x`, ветка `7efba321`, 2 файла, +76/−20, побитово;
2. **кандидат 4a** — `06_moe_slice_max`, ветка `b696680e`, 1 файл, +1/−1, побитово.

У обоих правило то же: **Issue первым**, PR только после его ответа.
