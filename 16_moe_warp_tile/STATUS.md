# 16 — плитка варпа 2×2 в маршрутизируемых MoE-GEMM префилла. Состояние

> **Обновлено 2 сентября 2026: правка переставлена на новую базу `upstream/master` `5973313d`,
> перезамерена, и оба висевших хвоста закрыты.** Ниже — новые числа; далее по документу
> сохранён разбор на старой базе `21a0e85f`, потому что прибор и опровергнутая гипотеза там
> описаны и остаются в силе.

## На новой базе: выигрыш вырос, обе претензии сняты

База сместилась под правкой: наш же пакет 03 (`0a204aee`) убрал `gather`, и `q4_gate_up` теперь
стадирует активации прямо из `x` по карте упакованных колонок. В коммите 03 записано, что ядро
**заняло все 80 регистров** при `__launch_bounds__(256, 3)` и «widening this array spills».
Наложение 2×2 раньше поднимало регистры 75 → 80, поэтому первым делом проверены ресурсы.

| ядро | апстрим `U0` | апстрим + 2×2 `U4` |
|---|---|---|
| `q4_gate_up<8,64>` | REG:80 STACK:0 SHARED:33792 | **REG:80 STACK:0 SHARED:33792** |
| `q4_gate_up<4,32>` | REG:124 STACK:0 | **REG:107 STACK:0** |
| `qx_down`, четыре инстанциации | 96 / 79 / 95 / 77, STACK:0 | **81 / 74 / 80 / 73, STACK:0** |

**Спилла нет, занятость широкого плана не изменилась.** У узкого плана и у `qx_down` регистров
стало меньше.

### Настенные часы, префилл 32768, палиндром из шести ног

| чанк | `U0` | `U4` | Δ |
|---|---|---|---:|
| **4096** | 1.753039 / 1.757921 / 1.757741 → **1.756234** | 1.728792 / 1.731897 / 1.732859 → **1.731183** | **−1.43%** |
| 1024 | 2.203889 / 2.205221 / 2.206122 → 2.205077 | 2.202205 / 2.203504 / 2.204060 → 2.203256 | −0.08% |

На старой базе было −1.07%. **Выигрыш вырос**, и это объяснимо: пакет 03 снял копирование, ядро
стало сильнее упираться в чтения из общей памяти — ровно в то, что режет плитка 2×2.

### Поядерно в движке, чанк 4096

| нога | GPU всего | `q4_gate_up` | `qx_down` |
|---|---:|---:|---:|
| `U0` | 5230.8 / 5221.0 → **5225.9** | 994.4 / 991.7 → **993.05** | 551.1 / 549.4 → **550.25** |
| `U4` | 5141.2 / 5140.0 → **5140.6** | 937.6 / 937.5 → **937.55** | 524.8 / 524.9 → **524.85** |
| **Δ** | **−1.63%** | **−5.59%** | **−4.62%** |

На старой базе `q4_gate_up` давал −2.14%. Теперь **−5.59%**, и разброс между ногами 0.01–0.19%,
то есть эффект на порядок больше шума.

В долях измеренного потолка bf16 (256.7 TFLOPS): `q4_gate_up` идёт **51.9% → 54.8%**,
`qx_down` — **46.8% → 49.0%**.

### При чанке 1024 правка не даёт ничего — и это ожидаемо

`q4_gate_up` 1626.1 → 1624.0, `qx_down` 768.7 → 774.5. Причина не в правке: при чанке 1024
плитка колонок шириной 64 наполовину пуста (на эксперта приходится 32 колонки), и ядро упирается
в перемножение набивки, а не в чтения из общей памяти. Экономить чтения там нечего.
Это отдельная ось, она разобрана в `18_профиль_35B/ПРОФИЛЬ_чанк_1024_и_4096.md`.

### ⚠️ Декодная регрессия на новой базе НЕ ВОСПРОИЗВОДИТСЯ

На старой базе декод просел на 0.6% от правки, которая трогает только префилл. Разобрано двумя
приборами:

1. **Поядерная трасса декода на обеих ногах** (круг 14): суммарное GPU-время декода
   `N0 = 1836.2 мс`, `M4 = 1835.1 мс`, **Δ = −0.06%**; наибольшее изменение по одному ядру
   −0.11%. **Ни одно ядро не замедлилось.**
2. **Контроль на новой базе** (круг 19): `U0` 0.455879 / 0.455227 → 0.455553 с;
   `U4` 0.456378 / 0.455125 → 0.455751 с, **Δ = +0.04%** при разбросе внутри ног 0.14% и 0.28%.
   Приёмка совпала до десятого знака (0.5905797101), раундов ровно 460 на всех четырёх ногах.

**Вывод: те 0.6% были раскладкой бинаря в общем модуле `ninfer_ops`, а не ядром.** На новой базе
эффекта нет. Претензия снята.

### ⚠️ clang-format — закрыт

36 строк расхождения были на старой базе. После `clang-format --style=file -i`: **0 строк
расхождения**, и — что важнее — **SASS до и после форматирования совпал побайтово**
(сравнение потока опкодов через `cuobjdump -sass`). Форматирование не изменило код.

### Побитовость

На старой базе жадная генерация совпала token-id в token-id. На новой базе первая попытка
контроля **дала пустой вывод** (модель думающая, 160 токенов ушли в размышление и ответ не
начался) — прибор переделан с `--no-thinking` и охранителем на длину, перезамер стоит в очереди.
До него побитовость на новой базе **не утверждается**.

---


Ветка `perf/moe-warp-tile` от **`upstream/master` `21a0e85f`**, инстанс 49380254, RTX 5090,
артефакт `qwen3_6_35b_a3b.ninfer`, Release, `sm_120a`, CUDA 13.1.115.

| | |
|---|---|
| файл | `src/ops/sparse_moe/prefill/sparse_moe_prefill_kernels.cu`, один файл |
| ядра | `sparse_moe_prefill_q4_gate_up_kernel` (**18.6% префилла**) и `sparse_moe_prefill_qx_down_kernel` (**10.5%**) |
| **префилл 32768, чанк 4096** | **1788.6 → 1769.4 мс, −1.07%** |
| побитово | **да**, жадная генерация совпала token-id в token-id |
| общая память | **не меняется**: 33792 и 31744 байта |
| занятость | **не меняется**: 3 CTA на SM у обоих |
| регистры | gate/up 75 → 80, down 77 → 73, у обоих `STACK:0` — спилла нет |
| патчи | `patch_warp2x2.py`, `patch_warp2x2_down.py` |

## Что меняется

Варп владеет **всеми четырьмя** плитками по строкам и одной по колонкам. За шаг `ki` это четыре
`ldmatrix.x4` на операнд A и одна `ldmatrix.x2` на B ради четырёх `mma` — **пять чтений из shared
на четыре тензорных инструкции**, и фрагмент A не переиспользуется ни разу. Все восемь варпов
читают при этом одну и ту же плитку A целиком.

Две плитки по строкам на две по колонкам дают два чтения A и два чтения B на те же четыре `mma`:
**четыре чтения вместо пяти, минус 20% инструкций и минус 33% байт** из общей памяти.

В gate/up пара строк выбрана как `{rp, rp + 2}` — плитка гейта и парная ей плитка up, поэтому
SiLU-гейт в эпилоге считается внутри варпа, без обмена через общую память. В down пары нет, там
просто `{2rp, 2rp + 1}`.

Порядок накопления по `k` не меняется, каждый выходной элемент по-прежнему считает ровно один
варп — отсюда побитовость, и она **проверена**, а не выведена.

## Прибор, с которого всё началось

Половина чтений операнда A при **неизменном** числе `mma` и всём прочем (`REG:77 STACK:0`,
спилла нет):

| ядро | база | половина чтений A | Δ |
|---|---:|---:|---:|
| `q4_gate_up` | 934.9 мкс | 726.4 | **−22.3%** |
| `qx_down` | 502.1 мкс | 448.0 | **−10.8%** |

## Результат

**Поядерно внутри движка**, префилл 32768, чанк 4096, KV int8, `nsys --cuda-graph-trace=node`,
палиндром из пяти трасс, суммы по трём прогонам:

| нога | GPU всего | `q4_gate_up` | `qx_down` |
|---|---:|---:|---:|
| N0 | 5281.8 | 984.1 | 551.8 |
| **2×2** | 5228.3 | **964.8** | **519.2** |
| N0 | 5290.7 | 986.5 | 553.1 |
| **2×2** | 5235.9 | **966.3** | **520.3** |
| N0 | 5300.0 | 989.5 | 554.9 |
| **среднее N0** | **5290.8** | **986.7** | **553.3** |
| **среднее 2×2** | **5232.1** | **965.6** | **519.8** |
| **Δ** | **−1.11%** | **−2.14%** | **−6.06%** |

Разброс трёх ног N0: по `q4_gate_up` 0.55%, по `qx_down` 0.56%. Эффект у down в одиннадцать раз
больше разброса.

**Настенные часы префилла**, шесть ног подряд, ноги не пересекаются:

| | значения | среднее |
|---|---|---:|
| N0 | 1785.1 / 1789.4 / 1791.3 | 1788.6 |
| **2×2** | 1768.5 / 1769.8 / 1770.0 | **1769.4** |
| | | **−1.07%** |

## ⚠️ Операторный стенд ранжирует эту правку неверно

`ninfer_sparse_moe_bench --distribution trace-like` даёт **противоположный знак**:

| прибор | `q4_gate_up` | вывод |
|---|---:|---|
| стенд, `--distribution trace-like` | 938.4 → 961.4 | **+2.5%, хуже** |
| стенд, `--distribution same` | 791.2 → 754.9 | **−4.6%, лучше** |
| **движок, поядерно** | 986.7 → 965.6 | **−2.14%, лучше** |

**Факт о приборе, который действует на всю кампанию по MoE: синтетическое `trace-like` в
операторном стенде ранжирует правки этого ядра не так, как движок, а `same` — так.** Если бы
решение принималось по стенду, работающая правка была бы выброшена. Мерить MoE-правки надо
поядерно в движке.

### Объяснение расхождения выдвинуто и ОПРОВЕРГНУТО

Напрашивалось: охранитель `col_group * WarpCols < cols` пропускает работу шагом в ширину колонок
варпа — при 4×1 это 8 колонок, при 2×2 уже 16; плитки маршрутов заполнены частично, и грубый шаг
заставляет лишние варпы делать работу, которую выбросят.

Проверка — `patch_warp2x2b.py`: охранитель перенесён **внутрь** цикла по колонкам, шаг в восемь
колонок возвращён. Если гипотеза верна, вариант обязан обогнать оба.

| рука | стенд `trace-like` | стенд `same` | движок, `q4_gate_up` |
|---|---:|---:|---:|
| N0 (4×1) | 938.4 | 791.2 | 986.0 |
| 2×2 | 961.4 | **754.9** | **967.5 (−2.08%)** |
| 2×2 с узким охранителем | **980.7** | 767.1 | 979.5 (−0.66%) |

**Хуже везде**, включая движок. Гипотеза неверна: узкий охранитель не возвращает потерянное, а
добавляет своё — ветка внутри развёрнутого цикла ломает планирование (`REG` падает 80 → 72).
Вариант закрыт, файл оставлен как запись отвергнутого.

**Настоящая причина расхождения `same` и `trace-like` осталась неустановленной.** Под `same` веса
восьми экспертов целиком лежат в L2, под `trace-like` — 256 экспертов и промахи; отделить
заполненность плиток от локальности весов имеющимися приборами не удалось, счётчики `ncu` на этой
карте закрыты (`ERR_NVGPUCTRPERM`). На решение по правке это не влияет.

## Гейты и контроли

| гейт | результат |
|---|---|
| кириллица в диффе | **0 строк** ✅ |
| `ctest` | **101 из 102**, падает только `ninfer_qwen3_6_frontend_test` — тот же, что падал на чистом мастере всю прошлую кампанию ⚠️ контроль на `camp35` ещё не переснят |
| `clang-format` | **36 строк расхождения на ветке при 0 на мастере** ❌ — правится, правка пробельная, поэтому проверяется совпадение SASS до и после |
| контроль декода | ⚠️ **см. ниже: декод просел на 0.6%** |

Ветка `perf/moe-warp-tile`, коммит `c95c95c5`, один файл, **+94/−72**, 277 строк диффа.
Бандл тонкий, от `21a0e85f`.

### ⚠️ Декод просел на 0.6%, хотя правка только в префилле

| нога | декод, с | ток/с | приёмка | раундов |
|---|---:|---:|---:|---:|
| N0 | 0.42830 / 0.42804 | 597.7 / 598.1 | 0.6758893281 | 425 |
| 2×2 | 0.43040 / 0.43111 | 594.8 / 593.8 | 0.6758893281 | 425 |

Приёмка совпала до десятого знака, раундов ровно столько же — **модель не изменилась, изменилось
время.** Разброс внутри ног 0.06% и 0.16%, разрыв 0.6%, то есть это не шум.

Правка меняет только `sparse_moe_prefill_*`, а в декоде при T=4 работают `sparse_moe/small_t/*`:
пороги кодеков (`kSparseMoePrefillQ4Q5Min = 47`) не пускают префилльный маршрут на четырёх
токенах. Значит объяснение либо в раскладке бинаря (сдвиг адресов ядер в том же модуле
`ninfer_ops`), либо какое-то ядро всё же затронуто. **Идёт поядерная трасса декода на обеих
ногах** — до её результата правка не считается готовой к передаче.

## Checks not run

* Узкий план `<4, 32>` (токенов < 768) правка тоже затрагивает: `ColGroups = 2`, `WarpCols = 16`.
  В префилле 35B он не используется и отдельно не мерился.
* Кодек `w8-w8` для маршрутизируемых весов не трогается — там другие ядра
  (`w8_gate_up<true>` / `w8_down<true>`), их форма плитки не разбиралась.
* На плотной 27B правка неприменима: у неё нет MoE.
