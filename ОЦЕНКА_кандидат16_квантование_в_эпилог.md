# Кандидат 16: квантование в эпилог производителя. Оценка на 27B, 1 сентября 2026

Ветка `all123` (пакеты 11+12+13), `qwen3_8_27b_nvfp4`, T=4096, префилл 32768, чанк 4096.

## Квантователь не медленный — он на 91% пика

`nsys` по операторному стенду, геометрия `linear_add` N=5120 K=17408, T=4096:

| ядро | мкс на пуск | доля пары |
|---|---:|---:|
| `nvfp4_w4a4_tma_kernel` (сам GEMM) | 460.4 | 80.4% |
| `nvfp4_w4a4_quantize_kernel` | **112.1** | **19.6%** |

Байты квантователя: читает `T×K` в bf16 = 142.6 МБ, пишет коды `T×K/2` = 35.7 МБ и масштабы
`T×K/16` = 4.5 МБ. Итого **182.8 МБ за 112.1 мкс = 1630 ГБ/с, то есть 91% пика HBM**.

**Вывод: оптимизировать сам квантователь нечего.** Экономить можно только круг через память.

## Арифметика слияния

Сегодня на одну пару «производитель → квантователь» уходит:

| шаг | байты |
|---|---:|
| производитель читает вход | 142.6 МБ |
| производитель пишет bf16 | 142.6 |
| квантователь читает bf16 | 142.6 |
| квантователь пишет коды и масштабы | 40.2 |
| **итого** | **468.0 МБ** |

Слитый вариант: прочитать вход (142.6) и записать коды с масштабами (40.2) = **182.8 МБ**.
**Экономия 285.2 МБ, то есть 61% трафика пары.**

## Сколько это на нашей модели

Квантователи в префилле (из трассы, всего 3884.8 мс):

| ядро | мс | % |
|---|---:|---:|
| `nvfp4_w4a4_quantize` (большой) | 39.6 | 1.00% |
| `fp8_a8_quantize` ×3 | 17.6 | 0.45% |
| `nvfp4_w4a4_quantize` (малый) | 4.2 | 0.11% |
| **итого квантователи** | **61.4** | **1.58%** |

Плюс исчезает запись bf16 у производителя. Нормы в префилле — 58.1 мс (1.50%), из них запись
примерно половина трафика, и слияние касается только тех норм, за которыми идёт квантование.

**Оценка приза: ≈1.7–1.8% префилла.** Это верхняя граница, потому что часть производителей —
не нормы.

## ⚠️ Производителей два разных, и это меняет объём работы

Кандидат сформулирован как «норма → квантование», но на нашей модели за квантователем стоят
**два** разных производителя:

| потребитель | K | производитель | слияние |
|---|---:|---|---|
| вход внимания | 5120 | входная норма | **норма → квант** |
| вход GDN | 5120 | норма | **норма → квант** |
| слитый SwiGLU | 5120 | пост-нормализация | **норма → квант** |
| `linear_add` (down) | **17408** | **слитый SwiGLU** | **SwiGLU → квант** |

Самый жирный квантователь (112 мкс на пуск, K=17408) висит **не за нормой, а за слитым SwiGLU**.
То есть кандидат в его нынешней формулировке покрывает не самую дорогую пару.

## Что мешает, кроме объёма

1. **Эпилогов с квантованием в дереве нет вообще.** `RmsEpilogue` — это `Offset`, `Plain`, `Gated`
   (`src/ops/kernel/rmsnorm.cuh:14`), и ничего похожего на квантование.
2. **Квантование nvfp4 требует absmax по группе из 16 подряд идущих элементов.** В норме и в
   эпилоге SwiGLU соседние элементы разложены по разным нитям, поэтому нужна редукция через
   `__shfl`, и раскладка должна лечь.
3. **Маршрут TMA ждёт блочную раскладку масштабов** (пакеты 12 и 13): слитый эпилог обязан писать
   `[BlockM токенов, 8 групп]` тайлами, а не построчно, иначе маршрут сломается. Предикат
   `nvfp4_w4a4_tma_route` придётся протащить в норму.
4. **У слитого SwiGLU эпилог уже транспонирует через общую память** (`НАХОДКА_эпилог_nvfp4.md`),
   и его буфер — `union` с конвейерным хранилищем. Добавлять туда квантование — значит трогать
   ровно то место, где уже тесно.

## Вердикт

Приз реальный (**≈1.7% префилла**), механизм понятен, но это **новая функциональность в двух
разных эпилогах**, а не параметр. Отдаю с точной арифметикой: чтобы решить, надо знать, что
экономится не работа квантователя (он на 91% пика), а 285 МБ круга на пару, и что самая жирная
пара висит за SwiGLU, а не за нормой.

## Checks not run

* Слияние не написано ни для одного из двух производителей.
* Не проверено, всегда ли вход квантователя — непосредственно выход нормы (возможен residual-add
  между ними, и тогда слияние надо тянуть через него).
* Доля записи bf16 внутри времени норм не измерена отдельно, оценена как половина трафика.
