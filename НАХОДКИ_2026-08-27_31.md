# Всё, что найдено за 27–31 августа. Передача напарнику

Цель кампании: **Qwen3.8-27B `nvfp4`**, чанк префилла 4096, MTP3 с черновой головой, KV int8
group-64. Проверено также на Qwen3.6-35B-A3B и Qwen3.8-27B groupwise-int.

Две находки оформлены пакетами и ветками (`08`, `09`). Остальное — здесь, потому что закрытая ось
и приборный факт экономят время так же, как принятая правка. **Ничего из этого я не подавал и не
буду**: моё дело найти, проверить и передать.

⚠️ **Смена машины 31 августа.** Прежний инстанс (RTX 5090, кап 500 Вт, 2865 МГц, CUDA 13.1) погиб
вместе с диском. Новый — 49380254 (кап **525 Вт**, **3210 МГц**, CUDA 13.1.115). **Абсолютные
числа с разных машин несравнимы**; ниже помечено, где чей замер. База тоже сменилась: работа
велась на локальной `camp/base`, потерянной с диском, и переподнята на чистом upstream
`master` `feaf4dd0`.

---

## 1. Что взято и передано

| # | пакет | ветка | эффект | доказано |
|---|---|---|---|---|
| 08 | `08_rmsnorm_shape_prefetch` | `perf/rmsnorm-shape-prefetch` @ `74dcc514` | нормы раунда −44.8%, раунд −1.49% | операторный бенч + графовая трасса, повторы |
| 09 | `09_mtp_sampled_draft` | `perf/mtp-sampled-draft` @ `ce71f787` | приёмка 45.26→52.57%, ток/с +8.98% | восемь промптов прозы, контроль вырождения |

Детали и сырые числа — в самих пакетах.

---

## 2. Закрытые оси: минусы с механизмом

Это половина работы. Каждая строка избавляет от повторного захода.

### 2.1. Расписания `nvfp4` при T=4 — **30 точек, ни одна не быстрее**

Гипотеза была: у MLP down сетка 640 CTA на 850 мест = 0.75 волны, машина не заполняется.
**Опровергнута**: добавление CTA только ухудшает. Время следует за **числом варпов**, а не CTA —
640 и 320 CTA при двух строках на варп дают одно и то же (16.6797 против 16.6801), переход на
одну строку на варп даёт +3.0%. Причина: активацию (139 КиБ при T=4) перечитывает каждый варп, и
при 2 560 варпах это 356 МБ через L2 против 50 МБ весов из DRAM.

Вторая гипотеза — включить стейджинг активации через shared (`SharedPhase`), как у gate_up, —
**тоже опровергнута**, +2.7%. Причина в барьерах: `SharedPhase` ставит `__syncthreads()` на фазу,
а у `Residual17408` длина K = 17 408 даёт **34 фазы** против 10 у gate_up.

Просвипованы: сетка, строки на варп, сплит по K, ширина загрузки (VPL 16/32), разворот фаз (1/2/4),
цепочки накопителей, режим кэша кодов, стейджинг активаций. Дрейф стенда за оба раунда 0.02%.
*(старая машина)*

### 2.2. Путь масштабов `nvfp4`: `StagedRaw` вместо `Direct` — **−1.7%**

Масштабы лежат тайлами 128 строк × 4 группы = 512 байт. На пути `Direct` каждая полоса читает
**один байт**, и 32 полосы варпа попадают в восемь мест с шагом 512 Б — восемь секторов ради 32
байт. `StagedRaw` (CTA грузит масштабы векторами `uint4` в shared и раздаёт `__shfl`) уже
реализован и используется на пути T=1; индексация совпадает байт в байт.

На `gate_up` эффекта нет (+0.07%), на `down` — **+1.69%, хуже**. Причина: у `down` K = 17 408,
`kGroupsPerRow` = 1 088, и CTA обязана поднять 8 704 байта в shared **целиком в прологе**, тогда
как счёт идёт фазами. Пролог сериализуется и стоит дороже восьми секторов, которые L1 всё равно
переиспользует между строками CTA. *(старая машина)*

### 2.3. Параллелизм `recurrent_record` — **удвоение нитей не даёт ничего**

Ядро читает состояние слоя (3.1 МБ) за 6.66 мкс = 466 ГБ/с при 49 152 нитях на машину, держащую
348 160. Разбиение по `dv` независимо, поэтому нитей можно дать вдвое больше (`kDvPerWarp` 4→2 при
`kNumWarps` 4→8). Результат: **16.7044 против базы 16.698** — ноль. Ядро упирается в
**последовательную цепочку по четырём токенам верификации**, а не в параллелизм. Все точки с
`kBlockDv ≠ 16` вообще не запускаются: константа связана с раскладкой записей.

Заодно: `recurrent_fold` читает и пишет всё состояние GDN (151 МБ в обе стороны) за 178.7 мкс =
**1.69 ТБ/с = 94% паспортной полосы**. Там брать нечего по определению. *(старая машина)*

### 2.4. Одноядерные маршруты gating при T=4 — **все хуже**

Сейчас cols 2..8 идут в `SmallTSplit10` (два ядра: partial 240 CTA и reduce 2 CTA). В каталоге
лежат одноядерные расписания, используемые на тех же формах при больших T. Проверены все:

| расписание | мс/раунд | к базе |
|---|---:|---:|
| **SmallTSplit10 (база)** | **16.4458 / 16.4567** | — |
| MmaUnsplit | 18.1474 | +10.3% |
| MmaCooperativeSplit8 | 16.6297 | +1.1% |
| MmaCooperativeSplit4 | 16.8975 | +2.7% |
| MmaCooperativeSplit2 | 17.3536 | +5.5% |

Чем меньше разбиение по K, тем хуже: при T=4 одноядерным вариантам не хватает независимой работы,
чтобы заполнить машину. Каталог маршрутов автора верен. *(старая машина)*

### 2.5. Гейтинг GDN параллельно с qkvz на втором потоке — **−0.73%**

Самая правдоподобная из оставшихся: `gating_proj_partial` (3.15 мкс, 240 CTA) и `..._reduce`
(1.71 мкс, 2 CTA) зависят только от нормы, ровно как и qkvz (56 мкс, 1024 CTA), и вместе стоят
233 мкс за раунд = 1.42% при том, что читают меньше мегабайта и машину почти не занимают.

Реализовано полностью: боковой поток и два события в `DeviceContext`, отдельный буфер под рабочую
область ветки (общую арену брать нельзя — из неё в это же время раздаётся память под qkvz),
соединение перед первым потребителем `g`/`beta`, хук варианта для обеих целей, выключатель.

| режим | мс/раунд |
|---|---:|
| выключено | 16.4573 / 16.4477 |
| **форк** | **16.5579 / 16.5861** |

Выключенный режим совпадает с базой, значит проигрывает именно параллелизм. Механизм: 48 слоёв
дают **96 дополнительных узлов синхронизации в графе**, около 7 мкс на слой против 4.86 мкс
прячущейся работы.

**Это ответ измерением на один из трёх стандартных вопросов разбора — «сделать параллельными».**
В этом движке цена кросс-потоковой синхронизации внутри графа декода **выше**, чем стоимость
мелкого ядра, которое ею прячут. Значит приём не сработает и на других парах: искать надо не
параллелизм, а сокращение числа ядер. Патч сохранён (`patch_g6.py` в `90_harness/` кампании).
*(старая машина)*

### 2.6. Расписание `fp8_mma` в префилле — **13 точек, ни одна не быстрее**

Все пять геометрий делят одно расписание `<64,128,128,2,4,2,2,cg,cg,PingPong,TokenFast>`, и это
выглядело как умолчание, которое забыли подобрать. Свип: тайл 128 токенов, 256 строк, K 64/256,
стадии 3 и 4, растеризация RowFast/Grouped, без ping-pong, комбинации.

| точка | к базе | | точка | к базе |
|---|---:|---|---|---:|
| `bt128` (CTA/SM 1) | +0.5% | | `rowfast` | +4.4% |
| `st3` (CTA/SM 1) | +6.2% | | `grouped` | +0.1% |
| `br256` (CTA/SM 1) | +2.9% | | `serial` | −0.1% |
| `bk256` (CTA/SM 1) | +4.8% | | `bt128k64s3` | +0.1% |
| `bt128st3` (CTA/SM 1) | +1.1% | | `k64s4` | +2.9% |
| | | | `k64mb4` | +3.0% |

**Механизм виден по знакам.** Всё, что опускает `MinBlocksPerSm` до 1, теряет тем больше, чем
глубже конвейер. Всё, что поднимает его до 4 ценой мелкого K-тайла, теряет 3.0%. Держит **занятость
в два CTA на SM**, а бюджет shared (`Stages · (BlockTokens + BlockRows) · BlockK`, около 100 КиБ на
SM) жёстко связывает тайл со стадиями: база 64×128×128 при двух стадиях — единственная точка, где
два CTA помещаются с максимальным тайлом. Одинаковое расписание у пяти геометрий — не недосмотр,
а единственное решение неравенства.

Порядок обхода и ping-pong тоже проверены: `grouped` и `serial` дают ноль, значит переиспользование
через L2 в порядке, а ping-pong ничего не приносит и не мешает. *(старая машина)*

### 2.7. Приёмка по попаданию в ядро цели — **+53.8% скорости, −26.7 пункта точности**

Вырожденный режим (принять любой черновик, попавший в усечённую опору цели) даёт 88.28% приёмки и
3.642 токена на раунд. Выход перестаёт быть выборкой модели:

| режим | AIME 2025 | AIME 2026 | токенов на задачу | упёрлось в лимит |
|---|---:|---:|---:|---:|
| база | 93.33% | 93.33% | 15 690 | 0/60 |
| точная (Г2) | 96.67% | 96.67% | 15 430 | 0/60 |
| приёмка по ядру | **66.67%** | **66.67%** | 24 320 | **19/60** |

Модель уходит в петли рассуждения и почти в трети прогонов не доходит до ответа; ускорение раунда
съедается ростом длины ответа ещё до учёта качества.

**И отдельно важное: иголка в стоге прошла 5/5 во всех трёх режимах.** Тест на извлечение не
чувствителен к искажению хвоста распределения. Для правок, меняющих выборку, нужен бенчмарк с
длинной цепочкой рассуждений. *(старая машина; см. оговорку в §5 про разрешение AIME)*

### 2.8. Длинный контекст — **оси нет, и это свойство модели**

| контекст | мс/раунд | к 4K | достигнутая полоса на KV |
|---:|---:|---:|---:|
| 4 096 | 16.7640 | — | 1.00 ТБ/с |
| 16 384 | 17.1791 | +2.5% | 1.26 ТБ/с |
| 32 768 | 17.6290 | +5.2% | **1.46 ТБ/с** |
| 65 536 | 18.5784 | +10.8% | 1.39 ТБ/с |

Наклон на длинном контексте — **77–82% паспортной полосы**, то есть ровно туда же, куда упираются
большие весовые GEMM. Ядро внимания на практическом потолке; недобор на коротком контексте (542
ГБ/с при 4K) — это фиксированные ~10 мкс на запуск на все 19 вызовов, и они не растут.

**Свойство, которое стоит знать:** декод этой модели почти не зависит от длины контекста — рост в
16 раз стоит 10.8% времени раунда, потому что 48 слоёв из 64 это Gated DeltaNet с состоянием
постоянного размера. *(старая машина)*

### 2.9. Регистровое давление в нормах: гипотеза верная, но проверенная не тем прибором

Отдельно вынесено, потому что это не минус, а **урок про приборы** — подробности в §5.
Правка «условная предзагрузка» сначала была отвергнута сквозным замером и оказалась нужной:
операторный бенч показал, что без неё нормы теряют **+19.3%** на гейтованной пост-норме GDN при
T=1024. Она вошла в пакет 08 третьей частью.

---

## 3. Найдено, но не наше

### 3.1. Слитый TMA-свиглу вне чанка 1024 — **дубль пакета 02**

Найден независимо: маршрут `nvfp4_linear_swiglu_plan.cpp` отдаёт работу слитому TMA-ядру только
при `tokens == kPrimaryT`, где `kPrimaryT = 1024` — ровно дефолтный чанк движка, тогда как само
ядро общее и требует лишь кратности 256. При чанке 4096 цепочка разъезжается на три ядра.

**Это уже лежит пакетом `02_nvfp4_swiglu`, ветка `fe6f70d1`, PR #113 открыт**, и у напарника
вариант шире — от 256, а не от 1024. Заявку не готовлю.

**Но независимое воспроизведение имеет ценность для треда #113**, потому что снято на другой
машине, другой цели и с контролем:

| чанк | база | с правкой | выигрыш |
|---:|---:|---:|---:|
| 1 024 | 4 736.9 | 4 740.1 | **−0.07%** ← контроль: этот чанк и так был слитым |
| 2 048 | 4 552.8 | 4 488.8 | +1.41% |
| 4 096 | 4 463.0 | 4 372.7 | **+2.02%** |
| 8 192 | 4 515.8 | 4 408.4 | +2.38% |

Строка 1024 — точный контроль механизма. Побочно: с правкой чанк 8192 почти догоняет 4096, то
есть провал широких чанков был отчасти следствием потери слияния. *(старая машина)*

### 3.2. PV в fp16 (`9d556bad`, автор `sergiuszm`) — новые данные для пакета 04

Прежний хеш `a9b6650d` был локальным и потерян с диском; **правка жива в форке как `9d556bad`**.
Измерена на **другой цели**, чем в пакете 04 (там 35B-A3B, здесь nvfp4-27B):

| длина | база | с pv16 | выигрыш |
|---:|---:|---:|---:|
| 32 768 | 4 372.8 мс | **4 214.5 мс** | **+3.76%** |
| 8 192 | 883.4 мс | 871.7 мс | +1.34% |

Рост с длиной воспроизводится. **Качество на этой цели проверилось чище, чем на 35B:** иголка 5/5,
жадная генерация на промптах ~15 тыс. токенов **побитово совпала** с базой, тогда как на 35B три
ответа из четырёх расходились в пересказ. Вероятная причина: здесь KV и так int8, и огрубление
накопления PV на длине 64 ключей ниже шума квантования KV. *(старая машина)*

---

## 4. Открытое: куда идти дальше

| # | что | оценка | почему не сделано |
|---|---|---|---|
| П4 | перевести префилльное `fp8_mma` на TMA и варп-специализацию | **~6% префилла** | ядро с нуля; единственный TMA-путь в дереве — nvfp4 |
| Г7 | расписания fp8 small-T в декоде | ~0.8% | свип начат, прерван; первая точка (`SharedPhase` у Residual6144) нейтральна |
| Г8, Г10 | слияние норм Q/K, `conv` + `recurrent_record` | 0.2% + 0.3% | мелочь |
| Г9 | норма в пролог/эпилог GEMM | было ~2.2%, **осталось мало** | пакет 08 забрал бо́льшую часть дешёвыми средствами |

**П4 — самая крупная незакрытая цель.** Все четыре формы `fp8_mma` в префилле ложатся в узкую
полосу **71–75% измеренного потолка карты**, а соседний `nvfp4_w4a4_tma` берёт 87–95%. Разница
структурная: nvfp4 идёт через TMA, 384 нити (варп-специализация продюсер/консьюмер) и 168
регистров — то есть **ровно один CTA на SM**, — а `fp8_mma` через `cp.async`, 256 нитей, 94–99
регистров и два CTA на SM. Свип §2.6 показал, что в рамках `cp.async`-структуры два CTA на SM
обязательны, поэтому подняться выше можно только сменив структуру.

**Про Г9 честно:** после пакета 08 пул норм в раунде упал с 3.93% до 2.2%, и из этих 2.2% почти
всё — **пол запуска ядра** (около 1.4 мкс на пустой запуск × 228 запусков). Слияние в GEMM теперь
окупается куда хуже, чем выглядело в исходной оценке.

**Общий вывод по декоду:** раунд исчерпан. Голова верификации на 94.4% полосы, черновая на ~98%,
`recurrent_fold` на 94%, внимание на 82% и на длинном контексте это предел, `recurrent_record` и
гейтинг закрыты измерениями. Единственный оставшийся структурный рычаг — **сокращать число ядер**:
пол запуска даёт около 770 мкс = 4.8% раунда на ~550 мелких запусков.

---

## 5. Приборные факты и метод

Это то, что переживёт любую из правок.

### 5.1. Потолки тензорных ядер, измерены заново

Микробенч на регистрах, без памяти в цикле (`mmapeak.cu` в `90_harness/`). Две строки
воспроизводят замеры прошлой кампании и потому служат проверкой харнеса:

| MMA | старая машина | новая машина | прошлая кампания |
|---|---:|---:|---:|
| `bf16×bf16→f32` m16n8k16 | 258.4 | 265.1 | 256.7 |
| **`e4m3×e4m3→f32` m16n8k32** | **515.5** | **530.8** | не мерили |
| `s8×s8→s32` m16n8k32 | 1 026.5 | 1 060.1 | 1 008.6 |

Практические потолки полосы памяти: большие чтения **94%** (LM-голова, 1 271 МБ одним ядром),
мелкие 71–86%, и КПД монотонно растёт с размером чтения.

### 5.2. Разрешение приборов — главное, что надо запомнить

| прибор | что видит | шумовой пол |
|---|---|---|
| сквозные часы, декод | раунд целиком | ~0.1% при палиндроме |
| сквозные часы, **префилл** | префилл целиком | **~1%**: тепловой дрейф внутри прогона до 1.8% |
| операторный бенч | форму ядра при T ≥ 16 | **±1.5%** (по контрольной форме) |
| операторный бенч при T ≤ 8 | **ничего**: упирается в 3.9–4.3 мкс накладных | — |
| поядерное GPU-время из трассы | ядро в графе | **0.0–0.14%** |

**Порядок замеров — палиндром `A B B A`, а не повторённые пары `A B, A B`.** При уезжающем стенде
пары систематически делают B хуже, и три пары в одну сторону выглядят подтверждением эффекта.
Я на этом ошибся дважды.

**Ниже примерно процента переходить на поядерное GPU-время.** Оно нечувствительно и к дрейфу
хоста, и к нагреву, и повторяется до 0.1%.

**Не сравнивать профили с разных машин.** Я один раз вывел «3.22 → 1.61 мкс», сравнив старую карту
с новой. Число случайно совпало с истиной, обоснование — нет.

### 5.3. Бенч держит копию предиката диспетчера

`bench/ops/rmsnorm_bench.cu` в `production_route()` дублирует логику выбора маршрута и печатает её
как `route=` в каждой строке. Правка диспетчера эту копию не трогает — и таблица доказательств
начинает называть **не то ядро, которое исполняется**. Проверять при любой правке маршрута.
Подозреваю, что то же устройство у других операторных бенчей.

### 5.4. Иголка в стоге ничего не доказывает про выборку

5/5 получают и база, и точная правка, и режим, который теряет 26.7 пункта AIME. Тест на извлечение
не чувствителен к искажению хвоста распределения.

### 5.5. Оговорка про AIME, которую надо держать в голове

На 30 задачах **одна задача = 3.33%**. Пара «93.33% → 96.67%» — это разница в одну задачу и
статистически ничего не значит. Разрешение есть у GPQA-Diamond (198) и IFBench (300), и у автора
для этой цели опубликованы числа: **IFBench 77.00%, AIME25 96.67%, AIME26 96.67%,
GPQA-Diamond 90.40%, ERQA 66.25%, RealWorldQA 83.53%**.

### 5.6. Ошибка, стоившая часа, — про необязательный вход в ядро

Указатель на буфер `q` передавался в приёмку **всегда**, а заполнялся только при включённом флаге.
Выключенная нога читала нули, правило `u·q < p` вырождалось в `p > 0`, и база показала 88.28%
приёмки вместо 45.70%. Поймано только потому, что цифра не сошлась с прежним замером того же
конфига. **При добавлении необязательного входа сверять базу с прежним бинарём, а не с
правдоподобием.**

---

## 6. Инфраструктурное

**Модель.** Qwen3.8-27B `nvfp4` — не плотная: 64 слоя = 48 Gated DeltaNet + 16 полного внимания
(чередование 3:1), hidden 5120, intermediate 17408, вокабуляр 248 320 строк, MTP — один слой со
своим вниманием и MLP, веса 19.40 ГиБ. Форматы смешаны: внимание и GDN fp8, MLP — nvfp4 в 56
слоях из 64 и fp8 в восьми, MTP-слой w8, вокабуляр fp8.

**Оптимальный чанк префилла для этой цели — 4096** (дефолт движка 1024 стоит 5.2%). Это отличается
от 35B-A3B, где кампания намерила оптимум 8192: там работу режут слайсами по 4096 ядра MoE,
здесь MoE нет.

**Сборка.** Проект требует **CUDA 13.1**, в образе vast.ai обычно 12.8. Бенчмарки выключены по
умолчанию: без `-DNINFER_BUILD_BENCHMARKS=ON` каталога `build/bench` не будет.

**Столбцы CSV у `ninfer_bench` на `feaf4dd0` сдвинуты на один** относительно прежнего дерева (нет
`vision_handoff_capacity_bytes`): `spec_rounds`=22, приёмка=24, `repetitions`=25,
`prefill_tok_s`=26, `prefill_seconds`=33, `decode_seconds`=34. Со старыми индексами формула молча
даёт `inf` или `nan`.

**`ctest` на `feaf4dd0`:** падает `ninfer_qwen3_6_frontend_test` (Subprocess aborted), причём
**на чистом мастере тоже**. В `ОЧЕРЕДЬ.md` записано, что на `1fc1cb76` сюита была зелёная — либо
поломка приехала с базой, либо разница в окружении. Решать перед подачей.

**Восстановление после потери диска.** Все правки лежат якорными патчами
(`patches_круга_2026-08-31/` в проекте) плюс бандлы веток. Перенос на другую базу потребовал
двух вещей, обе в `g2port.py`: якорь как список альтернатив (прошлый круг правил те же места) и
запасное сопоставление по регулярке, где прогоны пробелов эквивалентны (добавление переменных
сдвигает колонку `=` у соседних строк, и якорь несёт уже новое выравнивание).

---

## 7. Что доделывается прямо сейчас

Штатный `eval/` (IFBench 300, AIME25 30, AIME26 30, GPQA-Diamond 198) на ветке пакета 09.
Результаты дописываются в `08_rmsnorm_shape_prefetch/STATUS.md` и
`09_mtp_sampled_draft/STATUS.md`.

Для 09 это **обязательное** доказательство: правка меняет то, что движок сэмплирует, а иголка и
побитовое сравнение под `--greedy` проверяют вырожденный режим, а не рабочий. До результатов
единственный аргумент — что обе схемы суть точная спекулятивная выборка и распределение выхода не
меняется, а это свойство, а не замер.
