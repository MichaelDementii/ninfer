# nvfp4-GEMM префилла идут на 54% кормимого потолка. Что это не — проверено

**Находка, не заявка.** Механизма исправления у меня пока нет; ниже — измеренный разрыв и список
того, что уже исключено замером. Ветку не готовлю.

## Приборная ошибка, из-за которой ось казалась закрытой

Всю кампанию потолок nvfp4 принимался как **удвоенный fp8** — 1061.6 TOP/s. При этом
`nvfp4_linear_swiglu` при T=4096 даёт 1033.4 TFLOP/s, то есть «97.3%», и ось выглядела закрытой.

Потолок измерен напрямую, тем же микробенчем, что и остальные три типа. Инструкция —
та самая, что в ядре:

```
mma.sync.aligned.kind::mxf4nvf4.block_scale.scale_vec::4X.m16n8k64.row.col.f32.e2m1.e2m1.f32.ue4m3
```

```
bf16      266.0 TOP/s
fp8       531.9
s8       1060.7
nvfp4    2116.6   <- вчетверо больше fp8, а не вдвое
```

⚠️ Компилировать надо `-gencode arch=compute_120a,code=sm_120a`. С `-arch=sm_120a` ptxas говорит
«Instruction 'mma with block scale' not supported on .target 'sm_120'».

## Кормимый потолок, а не голый

Голый цикл держит всё в регистрах, настоящее ядро обязано кормить MMA через `ldmatrix`. Замерено
три режима кормления (`90_harness/mmafeed2.cu`):

| режим | голый цикл | 1 `ldmatrix` на 1 MMA | **4×8, как в ядре** | 4×8 при 1 CTA на SM |
|---|---:|---:|---:|---:|
| fp8 | 531.9 | 516.4 | **526.9** | 522.4 |
| nvfp4 | 2116.6 | 1243.9 | **1897.4** | 1886.0 |

Раскладка 4×8 — ровно та, что в `nvfp4_w4a4_tma_kernel` и в моём fp8-ядре: 4 загрузки `ldmatrix.x4`
для A и 8 `ldmatrix.x2` для B кормят 4×8 = 32 MMA. fp8 кормится почти даром (99% от голого),
nvfp4 — только при этой амортизации.

## Разрыв

| ядро, T=4096 | достигнуто | % от кормимого потолка |
|---|---:|---:|
| `nvfp4_linear_swiglu_w4a4_tma` | 1033.4 TFLOP/s | **54.5%** |
| `nvfp4_w4a4_tma` k=17408 (712.7 мкс) | 1024.0 | **54.0%** |
| `nvfp4_w4a4_tma` k=6144 (256.0 мкс) | 1006.9 | **53.1%** |
| fp8 после пакета 11 | 463–476 | **88–90%** |

nvfp4-GEMM — **2042 мс из 8041 мс GPU префилла (25.4%)**. Подтянуть их к тем же 88%, что у fp8,
означало бы около **9% префилла**. Это больше, чем всё, что найдено за круг.

## Что уже исключено замером

| подозрение | проверка | вывод |
|---|---|---|
| расписание не подобрано | свип `Nvfp4W4a4TmaSchedule<BlockM, Stages>`: M256 S3 (текущее) 712.7 / 256.0 / 1411.1; M256 S2 хуже на свиглу; M256 S4 не собирается (shared); M128 S3 и S4 хуже на 20% | **текущее — лучшее**, повтор точки воспроизвёлся до микросекунды |
| низкая занятость | кормимый потолок при 1 CTA на SM (2 варпа на планировщик) — 1886.0 против 1894.7 при 4 CTA | **занятость ни при чём** |
| MMA нечем кормить | 4×8 даёт 1897.4, ядро даёт 1033 | **кормление не мешает** |
| конфликты банков на загрузке масштабов | в микробенче раскладка масштабов заменена на конфликтную (шаг 16 Б между линиями, как в ядре) — стало **2022.6** | **конфликты ни при чём** |

## Что осталось подозревать

Счёт бюджета стадии. Одна стадия покрывает K=128, то есть 2 шага k64 × 32 MMA = 64 MMA на варп,
512 на CTA. Это 8.39 МФЛОП; при 1890 TOP/s на GPU и 170 SM это **0.76 мкс** счёта на стадию.
Подвоз той же стадии — 29.7 КиБ (`(256+128)·64` кодов плюс масштабы); при наблюдаемых по трассе
~3 ТБ/с суммарного трафика это **≈1.7 мкс**. Подвоз вдвое дороже счёта, и трёх стадий не хватает,
чтобы его спрятать.

Отсюда две мысли, обе упираются в бюджеты:

* **Четвёртая стадия.** `3 · 29696 = 89 КиБ`; четвёртая даёт 116 КиБ — не влезает.
* **Шире тайл, чтобы уменьшить перечитывание.** При N-тайлах 40 и M-тайлах 16 трафик A ≈ 1.43 ГБ,
  трафик B ≈ 0.71 ГБ; A доминирует, значит помог бы **BN=256**. Но тогда либо `kMmaN=16` и 256
  регистров-аккумуляторов (потолок 255), либо `kWarpsN=4` и снова 256. Заблокировано регистрами —
  ровно то же ограничение, в которое упёрся я на fp8.
* **Шире коробка TMA.** Сейчас внутреннее измерение дескриптора 64 байта; 128 байт со свизлом 128B
  дало бы вдвое меньше запросов, но `2 · 57 КиБ = 114 КиБ` — снова не влезает.

## Почему это стоит отдать автору

Ядро его, и ограничение похоже на компромисс, который он выбирал сознательно. Здесь измеренный
разрыв и четыре снятых подозрения — этого достаточно, чтобы он сразу сказал, известно ли ему это и
почему так. Мне без счётчиков `ncu` (на этой карте `ERR_NVGPUCTRPERM`, проверено ещё раз 1 сентября)
дальше не разложить.

## Сырое

* `90_harness/mmapeak4.cu` — потолки четырёх типов, включая nvfp4
* `90_harness/mmafeed2.cu` — кормимый потолок в раскладке 4×8; `BLOCKS 170` даёт вариант «1 CTA на SM»
* `90_harness/mmafeed4.cu` — та же раскладка с конфликтными масштабами
* `90_harness/q84_nvfp4_schedule_sweep.txt` — свип расписания, шесть точек
