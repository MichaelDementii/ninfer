# Пакеты 03 и 05 живы на актуальном мастере, 1 сентября 2026

Обе ветки помечены «готово к подаче» с 29 августа и собраны на `1fc1cb76`. С тех пор апстрим ушёл
далеко: актуальный `upstream/master` — **`da49c0d6`**. Проверено, что они не сгорели.

## Перенос

| пакет | ветка | перенос на `da49c0d6` | размер после переноса |
|---|---|---|---|
| 03 | `perf/moe-stage-from-x` @ `7efba321` | **чисто** | 2 файла, +76/−20 |
| 05 | `perf/moe-wide-bn` @ `786db14f` | **чисто** | 2 файла, +89/−43 |

Обе собираются, обе проходят `ninfer_sparse_moe_test` («OK sparse_moe correctness»).

⚠️ Они по-прежнему трогают один и тот же файл `sparse_moe_prefill_kernels.cu`, так что между собой
конфликтуют — второму нужен ребейз после первого, как и было записано.

## Операторный бенч на новой базе

`ninfer_sparse_moe_bench --execution graph --cache cold`, палиндромный порядок из шести прогонов
`B0 A3 A5 A5 A3 B0`, три арма собраны из одного дерева.

| форма | база `da49c0d6` | 03 | 05 |
|---|---:|---:|---:|
| q4-q5, T=1024 | 755.74 мкс | **725.02 / 727.04** → **0.960** | 755.74 / 755.74 → 1.000 |
| q4-q5, T=4096 | 1906.72 / 1908.74 | **1820.67 / 1822.72** → **0.955** | **1812.48 / 1814.53** → **0.951** |
| w8-w8, T=1024 — **контроль** | 964.64 | 964.64 / 966.69 | 964.64 / 966.66 |
| w8-w8, T=4096 — **контроль** | 2406.40 / 2409.47 | 2405.38 / 2407.42 | 2405.38 / 2407.46 |

**Прежние числа держатся.** На `1fc1cb76` было: 03 — ×1.038…1.050, 05 — ×1.059…1.069 на 4096 и
8192 при контроле 0.9981…1.0015. Здесь 03 даёт ×1.042 и ×1.047, 05 — ×1.051 на 4096 и ровно 1.000
на 1024 (его порог выше, это по построению).

**Контроль безупречен:** обе базовые точки на `q4-q5 T=1024` совпали до микросекунды (755.744
дважды), а нетронутый кодек `w8-w8` не сдвинулся ни на одной из четырёх точек. Дрейф стенда за весь
прогон меньше 0.13%.

## Что это значит для очереди

Ни 03, ни 05 не сгорели за 111 коммитов. Тексты Issue/PR, `make_pr.py` и бандлы в их папках
по-прежнему годны; поменять надо только базу, на которую ссылается заявка, и цифры — на те, что
выше, потому что они сняты на той базе, куда правка ляжет.

## Сырое

`90_harness/q78_moe_on_new_base.txt` — полный лог трёх сборок, теста и шести прогонов бенча.
