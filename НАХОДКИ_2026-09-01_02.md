# Всё, что найдено за 1–2 сентября. Передача напарнику

Цель круга: **Qwen3.6-35B-A3B** (MoE, 3B активных), RTX 5090, инстанс 49380254.
База — **`upstream/master` `5973313d`**, Release, `sm_120a`, CUDA 13.1.115.

Одна находка оформлена веткой (**П7 + П8**, одна заявка). Остальное — здесь.
**Ничего из этого я не подавал и не буду**: моё дело найти, проверить и передать.

⚠️ **База сменилась относительно августовского круга.** Прежние числа мерялись на стеке
`arvi/pr-consolidated`; здесь — чистый upstream, в котором уже лежат наши П1 (`0a204aee`) и
пакет 08 (`9954867a`). **Числа с теми не складываются.**

🔷 **Разделение, которое надо держать при чтении.** Побитово точных правок у нас **две**, и только
их можно называть чистым ускорением. Всё остальное — ветка напарника и настройки — **меняет
вывод**, и что с этим делать, решается отдельно. Помечено значком 🔶.

---

## 1. Что взято и передано

| # | что | ветка | эффект | доказано |
|---|---|---|---|---|
| **П7+П8** | плитка варпа 2×2 в маршрутизируемых MoE-GEMM + порог широкого плана 768 → 1280 | `perf/moe-warp-tile` @ `0e33d990`, **1 файл, +105/−83** | префилл **−2.4…−3.6%** на умолчаниях движка, до **−4.1%** на промптах 768–1152; поядерно `q4_gate_up` −5.6%, `qx_down` −4.6% | побитово; перплексия совпала до седьмого знака на 1 044 557 токенах; `ctest` 102/102 на трёх деревьях; SASS после форматирования совпал |

Бандл и дифф — `16_moe_warp_tile/moe_warp_tile_camp36.{bundle,diff}`.
Полный разбор — `16_moe_warp_tile/STATUS.md`.

### Где именно правка даёт эффект

Порог действует, когда **число токенов в чанке попадает в 768–1279**. Умолчание движка — чанк
**1024**, то есть внутри окна: на умолчаниях порог срабатывает на **каждом чанке каждого длинного
префилла**.

| режим | Δ префилла |
|---|---:|
| промпт 48–512 (оба плана узкие) | 0 (+0.03…−0.73%) |
| **промпт 768–1152** | **−3.3…−4.1%** |
| промпт 1280–1536 (оба широких) | −0.11…−0.57% |
| **pp2048, чанк 1024** | **−3.64%** |
| **префилл 32768, чанк 1024** | **−2.40%** |
| префилл 32768, чанк 4096 | −1.43% |
| декод | **0** — приёмка и раунды совпадают точно |
| сервер, 1 / 2 / 4 линии | −0.77% / −0.05% / −1.12% |

**Сквозной запрос pp2048 + tg128 на умолчаниях: 0.271600 → 0.266561 с, −1.86%.**

---

## 2. 🔶 Не побитовое — решение отложено

### Ветка напарника `perf/pv-f16acc` @ `8f70d975`

Накопитель `PV` во внимании в fp16 вместо fp32. **Идея не наша** — она из issue #27
(`sergiuszm`), в реестре числится как П3; напарник довёл её до кода, я проверил в движке.

| режим | Δ |
|---|---:|
| внимание поядерно | **−14.67%** |
| префилл 32768, чанк 4096 | **−3.90%** |
| вместе с П7+П8, 32768 | **−5.48%** |
| вместе с П7+П8, **65536** | **−7.36%** |

**Выигрыш растёт с контекстом:** доля внимания поднимается с 28.2% при 32768 до 44.0% при 65536.

**Качество:** перплексия ±0.01% со **знаком, неустойчивым между подвыборками** (`--quick` даёт
−0.011%, полный корпус +0.0038%) — то есть неотличимо от нуля. Но правка меняет численность, и в
базовом наборе это стоило трёх лишних раундов и трёх откатов на промпте в 512 токенов.
**Подавать отдельно от П7+П8.**

### Настройки: крупнейшие числа круга, и все меняют вывод

| что | эффект | цена |
|---|---:|---|
| **`--lm-head-draft`** (выключен по умолчанию) + окно MTP 3 | **+23.7…25.9% декода** | вывод может измениться |
| **чанк 4096 вместо умолчания 1024** | **−19.8% префилла** | воркспейс 109 → 438 МБ; **чанк 2048 берёт 86.8% выигрыша за половину памяти** |
| **кодек KV `fp8` вместо `int8`** | декод **+1.4 / +3.3 / +5.1%** при префилле 1K / 8K / 32K; префилл **−2.7%** | перплексия **+0.028%** |
| кодек KV `k8v4` | **быстрее** fp8 и KV на 22% меньше | перплексия вчетверо хуже fp8; уступает **только в качестве** — поправлено замером 2 сентября |

Механизм черновой головы разобран поядерно: это **отдельное ядро `q4_rowsplit_gemv`** на 142.6 МБ
вместо Q6-головы на 397.4 МБ; скалярный Q6-маршрут исчезает на **99.6%**. Флаг ещё и поднимает
приёмку 0.5906 → 0.7073, поэтому раундов становится меньше — выигрыш складывается из двух
независимых частей.

**Оптимум окна MTP зависит от флага:** без него минимум на 2, с ним — на 3. Две ручки нельзя
крутить порознь.

---

## 3. Закрытые оси: минусы с механизмом

| ось | почему закрыта |
|---|---|
| узкий план MoE `<8,32>` на восьми варпах | −0.41% против −2.40% у `<4,32>` с 2×2: под 2×2 при BN 32 выходит плитка **2×1**, 1.5 чтения на `mma` против 1.0. Форма при этом **невыразима** без нашей правки — пакет её открывает, и она хуже |
| порог отдельно для gate/up и для down | **по построению:** `route_job_bn` уходит в `scan`, тот строит **один список работ на оба ядра**; второй проход скана стоит 0.30–0.35%, приз 0.1% |
| геометрия `NStrip 16` для `gdn::state_passing` | **побитово**, но **+4.44%**: простаивающие SM дешевле, чем повторное чтение `W`, `U`, `k`, `g_cumsum` каждым блоком. Порог автора верен |
| раскладка 4×1 в W8-ядрах общего эксперта | форма та же, правка перенеслась бы побитово, но приз **0.2% префилла** — ниже порога |
| распаковка V во внимании | **14 мс из 294**: её делают выделенные варпы параллельно с MMA |
| чтения V из общей памяти во внимании | **ровно 0**. Устройство `PV` уже правильное: фрагмент `P` грузится раз на `k`. **«Сделать во внимании то же, что в MoE» не работает — там уже сделано** |
| запись `P` в общую память | **5 мс из 294** |
| дедупликация экспертов в декоде | потолок **3.4% декода**: вчетверо меньше весов даёт `moe_d4` всего −2.5%, то есть он в полосу не упирается |

**Вывод по вниманию:** четыре независимых прибора, опознано **57 мс из 294**. Дорогой операции
нет; время съедает устройство — 92 672 байта общей памяти, **один CTA на SM**, конвейер с
барьерами. Рычаг один: уложиться в 50 КБ ради двух блоков на SM.

---

## 4. Открытое: куда идти дальше

1. **237 мс из 294 не-MMA работы во внимании (81%)** — не в операциях, а в устройстве ядра.
2. **Немонотонность кривой чанка:** 1280 даёт 1.9784 с, 1536 — 2.0148 с. Больший чанк медленнее,
   и ни числом колонок на эксперта, ни числом чанков это не объясняется.
3. **`state_passing` спиллит 32 байта** в апстриме; границами запуска не лечится — при 512 нитях
   и одном блоке на SM потолок ровно 128 регистров, и ядро уже на нём. Стоит 2.7% префилла.
4. **Кандидат 2** — слияние редукции MoE в эпилог `down` через `red.global.add`. Потолок ~1.0%
   префилла измерен, ISA на `sm_120a` подтверждена, **не построен**; цена — побитовость.
5. **MoE не масштабируется по линиям.** Плотные веса и `lm_head` амортизируются почти полностью
   (×1.06 и ×1.07 времени за ×2.66 работы), маршрутизируемые эксперты — нет (`moe_d4_token`
   ×2.56 за ×2.61). Четыре линии дают 1.38× на префилл-тяжёлой нагрузке и 1.61× на декод-тяжёлой.

---

## 5. Приборные факты и метод

**Потолки MMA переснял на этой карте** (`90_harness/mmapeak_f16acc.cu`):

| форма | измерено |
|---|---:|
| `m16n8k16.f32.bf16.bf16.f32` | 266.1 TFLOPS |
| `m16n8k16.f32.f16.f16.f32` | 266.1 TFLOPS |
| **`m16n8k16.f16.f16.f16.f16`** | **531.0 TFLOPS** (ровно ×1.995) |
| `m16n8k32.s32.s8.s8.s32` | 1061.6 TOPS |

Строки про f16-накопление в прежней таблице не было, а без неё половина `PV` считалась не от того
знаменателя. Карта: L2 **96.0 МБ**, **170 SM**, 100 КБ общей памяти на SM, 65 536 регистров на SM,
полоса **1792.1 ГБ/с**.

**КПД четырёх главных ядер префилла** от измеренного потолка: плотный `w8_rowsplit_gemm_mma`
**73.2%** (знаменатель сверен с размером артефакта, расхождение 0.4%), `q4_gate_up` 51.9%,
`qx_down` 46.8%, внимание 43.5% смешанного int8/fp16. **Плотное ядро — образец, MoE отстаёт на
двадцать пунктов при той же арифметике.**

### Восемь приборных уроков круга

1. **`cuobjdump -res-usage` на каждой руке.** В смердженном П1 записано, что ядро заняло все 80
   регистров; без проверки наложение 2×2 могло дать спилл и выглядеть медленнее.
2. **Охранитель на длину вывода.** Проверка побитовости сказала «совпало, 1 байт»: модель
   думающая, 160 токенов ушли в размышление, и совпали два пустых файла.
3. **Прибор должен быть непустым.** Проверка «меняет ли чанк вывод» шла на промпте в двадцать
   токенов, который при любом чанке укладывается в один. Ответ «не меняет» ничего не значил.
4. **Приёмка — свойство текста, а не кодека.** Сравнение кодеков KV по декоду мерило везение
   черновика: приёмка скакала 0.373…0.995 и переворачивала знак. Чистый замер — без спекуляции.
5. **В MoE-модели абляция выше роутера портит распределение экспертов ниже него.** Абляция
   записи `P` уронила `select_count` на 58% и дала красивый ложный результат. Лечится измерением
   **вверх** (удвоить работу, а не убрать) и контрольным ядром в таблице.
6. **На префилле в 0.02 с одна помеха в одном повторе сдвигает среднее на десяток процентов.**
   Ложный «регресс +16.7%» при pp=128 исчез при шести ногах, `-r 20 --warmup 5` и сравнении по
   медиане: разброс упал с 18% до 0.25–0.38%.
7. **`static` внутри device-функции — это глобальная память.** Поймано до сборки.
8. **Имя ядра в `nsys` содержит `<unnamed>::`.** Резка по `<` до снятия префикса схлопывала все
   MoE-ядра в пустую строку — 37% времени префилла пропадало из таблицы.

---

## 6. Инфраструктурное

* **`ninfer_bench` не умеет параллельные потоки.** Линии живут в `apps/serve`:
  `--max-concurrency N`, диапазон [1,8], умолчание 1. Драйвер — `90_harness/parbench.py`.
* **Порт 8080 на этой машине занят jupyter-notebook**; сервер падал на `phase=bind` без внятного
  сообщения.
* **`kv_capacity` ограничен правилом** `страниц ≤ max_concurrency × page_count(max_context)`
  (`layouts_impl.h:597`) — одно число на все руки не годится.
* **Сервер отвечает `404 model_not_found`** на произвольное имя модели; имя надо спрашивать
  у `/v1/models`.
* **Корпус бенча короче 131 072 токенов** — `pp131072 exceeds the token-id corpus`. Предел
  прибора, не движка.
* **`tools/perplexity/prepare_corpus.py` удалён апстримом** (`a2761ec1`, −703 строки). Остаётся
  штатный корпус `eval/corpora/perplexity-1m/manifest.json` или `--text FILE`.
* **Один распорядитель вместо цепочки ожиданий.** Две копии круга поделили GPU, одна упала с
  «свободно 9.5 ГБ из 32», записала маркер завершения раньше времени и разблокировала следующие.
* **`ninfer_qwen3_6_frontend_test` починен апстримом** (`5973313d`, самодостаточные фикстуры).
  Он падал у нас всю прошлую кампанию и на чистом мастере тоже; теперь контроль чистый.

---

## 7. Что доделывается прямо сейчас

Ничего не висит. Все круги закрыты, все числа записаны, ветка П7+П8 собрана с бандлом.

**Ждёт решения:** что делать с не побитовым — ветка `perf/pv-f16acc` и четыре настроечные
находки. По ним всё измерено, включая качество; не хватает только решения о порядке подачи.
