# nvfp4-префилл: эпилог стоит 11% ядра при длинном K и 26% при коротком

Замерено 1 сентября 2026. Инстанс 49380254, RTX 5090, ветка `all123` (пакеты 11+12+13),
`qwen3_8_27b_nvfp4`, T=4096.

## Где стоит nvfp4 после пакетов 12 и 13

Потолок — **кормимый** nvfp4 1897.4 TOP/s (замерено `90_harness/mmafeed2.cu`), не сырые 2116.6.

| геометрия | % префилла | TFLOP/s | % кормимого потолка |
|---|---:|---:|---:|
| слитый SwiGLU 34816×5120 | 15.1% | 1246.5 | **65.7%** |
| `linear_add` 5120×17408 | ~4% | 1264.2 | **66.6%** |
| `linear_add` 5120×6144 | ~3% | 1048.6 | **55.3%** |

Взвешенно **≈65%**, тогда как fp8 после пакета 11 идёт на **86.4%**.

⚠️ **Читать это как «есть 35% запаса по ярусу» НЕЛЬЗЯ** — см. раздел про абляцию в конце файла:
тензорные инструкции занимают у nvfp4 всего **8% времени ядра**. Разрыв до яруса здесь не
означает запаса, потому что ярус не является ограничением.

## Разложение по двум точкам K

Одинаковое T=4096, одинаковое N=5120, разное K — значит разное число k-плиток при одинаковом
эпилоге:

| K | k-плиток | время |
|---:|---:|---:|
| 6144 | 48 | 245.76 мкс |
| 17408 | 136 | 577.54 мкс |

Линейная подгонка: **3.77 мкс на k-плитку и 65 мкс фиксированной цены.**
Проверка: 136 × 3.77 + 65 = 577.7 против измеренных 577.5.

**Фиксированная часть — это 11% ядра при K=17408 и 26% при K=6144.**

## Проверка модели по T

Развёртка по T при K=17408 (те же 136 плиток, растёт только сетка):

| T | время | TFLOP/s |
|---:|---:|---:|
| 1024 | 153.60 мкс | 1188.4 |
| 2048 | 292.86 | 1246.6 |
| 3072 | 436.22 | 1255.3 |
| 4096 | 579.58 | 1259.8 |
| 6144 | 864.26 | 1267.2 |
| 8192 | 1146.88 | 1273.3 |

Наклон ровный — **142.3 мкс на 1024 токена**, свободный член всего **≈11 мкс**. Значит
те 65 мкс — не цена запуска, а **поCTA-шный эпилог, который масштабируется вместе с сеткой**.
Отсюда же видно, почему КПД растёт с T только на 7%: амортизируется лишь 11-микросекундный пуск.

## Что именно делает эпилог и почему он не перекрывается

`nvfp4_w4a4_tma.cuh`, после основного цикла:

1. `bar.sync` — дождаться, пока все консьюмерские варпы дочитают тензорное хранилище;
2. запись аккумуляторов в общую память как bf16, шаг `kBlockN + 8 = 136` (набивка против
   конфликтов банков);
3. `bar.sync`;
4. чтение обратно векторами по 8 bf16 и запись в глобальную память.

Круг через общую память нужен потому, что **аккумуляторы лежат `[токен][строка]`, а выход
`[строка][токен]`** — это транспонирование, а не лишнее копирование.

**И вот главное: перекрыться с основным циклом эпилог не может по построению.** Его буфер
`shared.scratch.output` — это `union` с конвейерным хранилищем `tensors`. Union нужен, чтобы
влезли три стадии: без него общая память была бы 82 944 + 69 632 = 152 576 Б при потолке 101 376.
Поэтому CTA обязана сначала полностью закончить цикл, потом писать выход, и во время эпилога
у неё в полёте нет ни одной TMA-передачи.

## Сколько из этих 65 мкс — настоящая работа

На CTA выход 256×128 = 32 768 элементов bf16 = 65 536 Б; при 640 CTA это **41.9 МБ** в глобальную
память. При пике HBM 1792 ГБ/с это **23.4 мкс**, плюс двойной круг через shared ≈ 4 мкс.
Модель даёт ≈28 мкс против измеренных **65**.

Разница в 2.3 раза — это и есть цена сериализации: запись выхода ни с чем не перекрыта, а волн
всего 3.8 (640 CTA на 170 SM), так что хвост заметен.

## Куда бить, если браться

Не в сам эпилог — он уже транспонирует через shared с набивкой и пишет 16-байтовыми векторами.
Бить надо в **сериализацию**: сделать CTA персистентной, чтобы эпилог плитки *i* перекрывался с
основным циклом плитки *i+1*. Это снимает union как ограничение (буфер выхода нужен только на
время эпилога одной плитки) и убирает 3.8-волновой хвост.

Потолок направления: если эпилог перекрыть целиком, 577.5 → ≈513 мкс, то есть **−11% на
nvfp4-ядрах**, а это 22% префилла → **−2.4% префилла**. На коротком K выигрыш вдвое больше.

⚠️ Это перестройка ядра автора, а не параметр. Отдаю как находку с числами.

## Checks not run

* Персистентная схема не написана и не мерена — оценка −11% получена из подгонки, а не из правки.
* Разложение сделано на двух точках K и подтверждено шестью точками T, но третьей точки K у бенча
  нет (`--k 6144|17408`), поэтому подгонка двухпараметрическая по построению.
* Слитый SwiGLU в это разложение не входил: у него другая геометрия выхода.

---

# Дополнено: абляция MMA. Доля тензорных инструкций — 8%, а не «разрыв до яруса»

Тем же прибором, что и по вниманию: `mma_nvfp4_e4m3` заменена дешёвой зависимостью, держащей все
восемь операндов живыми (иначе TMA-стейджинг и `ldmatrix` выбрасываются оптимизатором).
Палиндром, T=4096:

| геометрия | с MMA | без MMA | доля MMA |
|---|---:|---:|---:|
| `linear_add` k=17408 | 577.54 / 577.54 мкс | 536.58 / 532.48 | **7.4%** |
| `linear_add` k=6144 | 247.81 / 246.75 | 227.33 / 227.39 | **8.1%** |
| слитый SwiGLU | 1171.46 / 1173.54 | 1058.82 / 1062.91 | **9.5%** |

**Это отменяет трактовку «nvfp4 на 65% кормимого потолка, значит есть 35% запаса по ярусу».**
Ярус тут ни при чём: тензорные инструкции занимают 8% времени. Для сравнения, у
`prompt_bf16` абляция той же процедурой даёт **33.8%** — там ярус действительно рычаг, у
nvfp4 нет.

## Полное разложение ядра nvfp4 (k=17408, T=4096)

| часть | доля | чем измерено |
|---|---:|---|
| подвоз (коды и масштабы через TMA) | **≈59%** | ядро только с подвозом, 341 мкс из 577.5 |
| прочая обвязка (`mbarrier`, `ldmatrix`, индексы) | ≈22% | остаток |
| эпилог с транспонированием | **≈11%** | подгонка по двум точкам K |
| **тензорные инструкции** | **≈8%** | **абляция** |

## Что это делает с кандидатами 4 и 5

Оба они про эффективность вокруг MMA:

* **кандидат 4** (регистровая конвейеризация K-шага через границу стадии) целится в перекрытие
  `ldmatrix` с MMA — это часть тех 22% обвязки;
* **кандидат 5** (`BlockK = 256`) вдвое режет барьеры и открывает свизл 128 Б — тоже обвязка
  и подвоз.

Даже при идеальном исполнении оба вместе ограничены **22% ядра**, а заявленный кандидатом 4
«потолок 5–7% на этом ядре» с этим согласуется. В пересчёте на префилл (nvfp4 — 22%) это
**до 1.5% префилла у кандидата 4** и столько же у пятого, а не то, что можно было прочитать из
разрыва до яруса.

**Приоритет внутри nvfp4-ядра, по замеру:** подвоз (59%) → эпилог (11%) → обвязка (22%,
разделена между кандидатами 4 и 5) → MMA (8%, трогать бессмысленно).
Подвоз уже дважды правился пакетами 12 и 13; следующий по величине — эпилог.

---

# Дополнено: эпилог измерен абляцией, а не подгонкой. И цена не там, где казалось

Подгонка по двум точкам K — приём с двумя параметрами по двум измерениям, то есть без запаса.
Заменил её абляцией в две ступени, чтобы отделить транспонирование от записи:

* **ступень 1** — убрана только глобальная запись, круг через общую память остаётся;
* **ступень 2** — убран весь эпилог; аккумуляторы скормлены условию, которое компилятор не может
  доказать ложным, чтобы основной цикл и TMA-стейджинг выжили.

Палиндром, T=4096:

| вариант | k=17408 | k=6144 |
|---|---:|---:|
| полный | 577.54 / 577.54 мкс | 245.76 / 245.76 |
| без глобальной записи | 571.39 / 571.39 (**−1.1%**) | 245.76 / 245.76 (**0%**) |
| без эпилога целиком | 526.34 / 526.34 (**−8.9%**) | 194.56 / 194.56 (**−20.8%**) |

## Две поправки к тому, что я написал выше

**1. Цифры меньше.** Подгонка давала 11% и 26%, абляция даёт **8.9% и 20.8%**. Разница в том,
что «фиксированная часть» подгонки включала ещё и пролог с ценой запуска, которые абляция
оставляет на месте. Верить надо абляции.

**2. Дорого не то, что я назвал.** Я писал «на CTA выход 65 536 Б, при 640 CTA это 41.9 МБ,
при пике 23.4 мкс». Но ступень 1 показывает, что **глобальная запись стоит 1.1% при длинном K
и ровно ноль при коротком**. Вся цена эпилога — это **транспонирование через общую память**:
запись 32 768 аккумуляторов как bf16 с шагом 136, барьер, чтение обратно векторами по 16 байт.

Это меняет и адрес правки. Бить надо не в перекрытие записи с чем-нибудь, а в сам круг: либо
не транспонировать (менять раскладку аккумуляторов под выходную), либо перекрывать
транспонирование плитки *i* с основным циклом плитки *i+1* — и вот для второго персистентная
CTA как раз и нужна, потому что `union` с конвейерным хранилищем сегодня это запрещает.

## Пересчитанный потолок направления

| геометрия | % префилла | доля эпилога | вклад |
|---|---:|---:|---:|
| `linear_add` k=17408 | ~4% | **8.9%** (замер) | 0.36% |
| `linear_add` k=6144 | ~3% | **20.8%** (замер) | 0.62% |
| слитый SwiGLU K=5120 | 15.1% | ~20% (**не мерено**, K короткий как у 6144) | ~3.0% |

**Потолок ≈4% префилла**, а не 2.4%, как я оценил подгонкой. Основной вклад даёт слитый SwiGLU,
потому что у него K=5120 — коротких k-плиток всего 40, и эпилог амортизируется хуже всех.

⚠️ **Доля эпилога у слитого SwiGLU не измерена.** У него собственный эпилог в
`nvfp4_linear_swiglu_w4a4_tma.cuh`, моя абляция его не задевала. Число ~20% перенесено по
аналогии с той же длиной K и потому помечено оценкой, а не замером.

---

# Дополнено: эпилог слитого SwiGLU тоже измерен. Потолок направления 3.5% префилла

У слитого SwiGLU собственный эпилог (`nvfp4_linear_swiglu_w4a4_tma.cuh`), и он делает на шаг
больше: гейт SiLU, потом транспонирование через shared, потом запись. Абляция теми же двумя
ступенями, палиндром:

| вариант | T=4096 | T=8192 |
|---|---:|---:|
| полный | 1173.5 / 1172.5 мкс | 2287.6 / 2282.5 |
| без глобальной записи | 1118.2 / 1122.3 (**−4.5%**) | 2135.0 / 2134.0 (**−6.6%**) |
| без эпилога целиком | 970.8 / 970.8 (**−17.2%**) | 1853.4 / 1851.4 (**−18.9%**) |

Оценка «~20% по аналогии» подтвердилась: **17.2%**. И здесь глобальная запись стоит заметно
больше, чем у `linear_add` (4.5% против 1.1%) — у SwiGLU на токен пишется 17408 значений, байт
кратно больше.

## Итоговое разложение эпилога, оба ядра измерены

| ядро | глобальная запись | гейт и транспонирование | весь эпилог |
|---|---:|---:|---:|
| `linear_add` k=17408 | 1.1% | 7.8% | **8.9%** |
| `linear_add` k=6144 | 0.0% | 20.8% | **20.8%** |
| слитый SwiGLU K=5120 | 4.5% | 12.7% | **17.2%** |

Общее правило видно: **чем короче K, тем дороже эпилог**, потому что он не амортизируется
основным циклом. У SwiGLU K=5120, то есть всего 40 k-плиток — худший случай из наших.

## Потолок направления: 3.5% префилла, всё по замеру

| геометрия | % префилла | доля эпилога | вклад |
|---|---:|---:|---:|
| слитый SwiGLU | 15.1% | **17.2%** | **2.60%** |
| `nvfp4_w4a4_tma` (обе геометрии) | 6.9% | 8.9…20.8% | ~0.90% |
| **итого** | | | **≈3.5%** |

Это **самая крупная измеренная цель, оставшаяся в префилле 27B**. Для сравнения: f16-аккумулятор
во внимании давал 4.4%, но отклонён по качеству; здесь качество не затрагивается вовсе —
перестановка работы, а не смена численности.

## Что именно надо сделать

Не трогать саму запись (она дешёвая) и не «перекрывать её с чем-нибудь». Круг транспонирования
через общую память — вот цена. Два пути:

1. **Не транспонировать.** Аккумуляторы лежат `[токен][строка]`, выход `[строка][токен]`. Если
   выходной тензор согласиться хранить в другой раскладке, круг исчезает целиком. Это меняет
   контракт между операциями, поэтому решение автора.
2. **Перекрыть транспонирование плитки *i* с основным циклом плитки *i+1*** — персистентная CTA.
   Сегодня запрещено тем, что буфер выхода `union` с конвейерным хранилищем; персистентная схема
   как раз и снимает это, потому что буфер нужен только на время одного эпилога.
