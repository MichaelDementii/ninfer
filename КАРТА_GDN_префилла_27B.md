# GDN в префилле 27B: 8.4% времени, усиление трафика в 5.3 раза. 1 сентября 2026

Семейство GDN кампания ни разу не мерила. Вот его карта.

Инстанс 49380254, RTX 5090, ветка `all123` (пакеты 11+12+13), `qwen3_8_27b_nvfp4`,
геометрия `state_dim=128 qk_heads=16 value_heads=48 batch=1`, T=4096.

## Вес в префилле

Из трассы префилла 32768 (всего 3884.8 мс):

| ядро | % префилла | мс |
|---|---:|---:|
| `chunked::state_passing` | **3.4%** | 131.4 |
| `chunked::prepare_wy_wu` | 1.7% | 67.5 |
| `chunked::output` | 1.5% | 60.0 |
| `causal_conv1d_prefill_pairs` | 1.2% | 47.1 |
| `bf16_gdn_gating_proj_gemm_mma` | 0.5% | 19.0 |
| `l2norm_warp_bf16x2` | 0.2% | 8.6 |
| `causal_conv1d_prefill_state` | 0.02% | 0.9 |
| **итого** | **8.4%** | **334.5** |

## Разбивка по узлам, на один пуск

`nsys` по стенду `ninfer_gated_delta_net_bench --tokens 4096`:

| узел | мкс на пуск | доля GDN |
|---|---:|---:|
| `state_passing` | 328.5 | **47.2%** |
| `prepare_wy_wu` | 175.1 | 25.2% |
| `output` | 155.0 | 22.3% |
| `l2norm` (два пуска) | 18.6 | 5.3% |

## Главное число: трафик в 5.3 раза больше полезного

Стенд печатает это сам:

```
T=4096 B=1  chunks=64  nodes=5  ws=272.75 MiB   median=700.4 us
bytes logical=135.50 MiB  traffic=713.75 MiB  intermediate=578.25 MiB
bandwidth logical=202.9 GB/s  traffic=1068.5 GB/s
```

**578.25 МиБ из 713.75 — это материализованные промежуточные**, то есть пять узлов передают
друг другу результаты через HBM. Полезных данных всего 135.50 МиБ.

Ядро идёт на **1062–1068 ГБ/с, то есть 59% пика HBM**, и упирается именно в трафик: время
сходится с трафиком (713.75 МиБ / 1068 ГБ/с = 700 мкс, измерено 700.4).

**Потолок направления «убрать промежуточные»**: если бы двигались только полезные 135.5 МиБ на
пике, узел шёл бы 79 мкс вместо 700. Это, конечно, недостижимо, но показывает масштаб: **основная
цена GDN — не счёт и не полоса, а то, что узлы общаются через память.**

## Что уже проверено и закрыто

**Ручка `NStrip` — выбор автора верен.** `state_passing` берёт `NStrip = 16` при `H_v >= 48`
и 32 иначе; сетка `H_v × D_STRIPS`, то есть 48 × 8 = **384 CTA на 170 SM** (2.26 волны).
Палиндром на нашей геометрии:

| NStrip | сетка | время | трафик |
|---:|---:|---:|---:|
| **16** (текущее) | 384 CTA | **704.5 / 704.5 мкс** | 1062.3 ГБ/с |
| 32 | 192 CTA | 745.5 / 745.5 (**+5.8%**) | 1004.0 |

Регистры обоих: `REG:124/128 STACK:32`. Ось закрыта.

**Полное слияние GDN-префилла в одно ядро** закрыто раньше ячейкой `V5`: грид `seq × head`
даёт 32 CTA на 170 SM при B=1.

## Что остаётся живым — ⚠️ ПЕРЕСМОТРЕНО, см. дописанный раздел в конце файла

> Ниже я предполагал, что состояние в префилле лежит в FP32. Это неверно: `h_chunk` уже BF16.
> Правильная разбивка 578 МиБ по тензорам и переформулировка кандидата 8 — в конце файла.

**Кандидат 8 (пул состояния в FP16) — прежняя, ошибочная оценка.** Состояние зашито в fp32
(`gated_delta_net.cpp`), у всех трёх чужих движков это параметр. Промежуточный `h_chunk` —
по чанку на каждый из 64 чанков × 48 голов — один из главных вкладов в те 578 МиБ.
Половина его трафика — это, по грубой оценке, **1.5–2% префилла**, плюс декод и VRAM
(кандидат обещает 80 мкс на раунд и 144 МиБ на 27B).

⚠️ Это правка на точность в первую очередь: состояние рекуррентное, ошибка копится по чанкам.
Ровно тот механизм, на котором отклонился f16-аккумулятор во внимании
(`ЗАМЕР_доля_MMA_во_внимании.md`), поэтому проверять надо длинным контекстом, а не перплексией.

**Кандидат 9 (L2-норма q/k внутрь свёртки) на нашей модели мелкий:** сам `l2norm_warp` это
**0.2% префилла** (8.6 мс). Даже если он исчезнет целиком вместе со своим круговоротом, это
единицы десятых процента, а не заявленные «2.2 мс на чанк». Приоритет низкий.

## Checks not run

* Трафик по узлам отдельно не разложен — стенд печатает суммарный, поэтому вклад `h_chunk`
  в 578 МиБ оценён из геометрии, а не измерен.
* FP16-состояние не пробовалось: ни на скорость, ни на точность.
* `prepare_wy_wu` и `output` (вместе 47.5% GDN) не разбирались вообще.

---

# Дополнено: 578 МиБ промежуточных разложены по тензорам

`chunked/launch.h:32-46`, при T=4096, `value_heads=48`, `kStateDim=128`, `kChunkSize=64`
(то есть 64 чанка):

| тензор | тип | размер | трафик (запись + чтение) | доля трафика GDN |
|---|---|---:|---:|---:|
| **`h_chunk`** | **BF16** | 100.7 МБ | **201.4 МБ** | **27%** |
| `W` | BF16 | 50.3 | 100.6 | 13% |
| `U` | BF16 | 50.3 | 100.6 | 13% |
| `v_new` | BF16 | 50.3 | 100.6 | 13% |
| `g_cumsum` | FP32 | 0.8 | 1.6 | 0.2% |

Сумма 504.8 МБ против измеренных стендом 578.25 МиБ промежуточных — сходится с точностью
до тех тензоров, что читаются больше одного раза.

## ⚠️ Это переформулирует кандидата 8

Кандидат говорит «состояние GDN зашито в FP32, у всех троих чужих это параметр». **Промежуточный
`h_chunk` уже BF16** (`launch.h:42`), и именно он даёт 27% трафика. В FP32 у нас только
`g_cumsum` (0.2% трафика) и **персистентное** `ssm_state`, которое в префилле читается и пишется
один раз на чанк — 3.1 МБ на слой, то есть **менее процента** трафика GDN.

Значит:

* **в префилле кандидат 8 почти ничего не даёт** — то, что он предлагает ужать, уже ужато;
* его настоящая ценность — **декод** (там состояние трогается каждый раунд: заявлено 80 мкс на
  раунд у 27B, это ≈0.5% раунда) **и VRAM** (144 МиБ).

## Что осталось бы настоящим рычагом и почему оно закрыто

`h_chunk` вообще не обязан материализоваться: `state_passing` вычисляет `h` для чанка, а `output`
тут же его потребляет. Слияние сняло бы **201.4 МБ = 27% трафика GDN = 2.3% префилла**.

**Но оно заблокировано разбиением.** `state_passing` идёт сеткой `H_v × D_STRIPS` — CTA владеет
полосой по измерению состояния и проходит по всем чанкам последовательно (это скан). `output`
же разбит по парам (чанк, голова). CTA, владеющая полосой состояния, не может выдать полный
выход чанка: у неё только часть `h`. Это тот же барьер, что закрыл полное слияние GDN в ячейке
`V5`, только на уровень ниже.

Понижение `h_chunk` до fp8 дало бы 100.7 МБ = 13.5% трафика GDN = **1.1% префилла**, но это
рекуррентная величина, и риск ровно тот, на котором отклонился f16-аккумулятор во внимании.
