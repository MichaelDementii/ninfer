# Стенд кампании по кодеку KV

Всё, что нужно, чтобы повторить замеры или оспорить их.

## Машина

Арендованный инстанс **vast.ai № 49380254**, контейнер `99ec9f558191`.

| | |
|---|---|
| **GPU** | NVIDIA GeForce RTX 5090, 32607 МиБ, compute capability **12.0** (sm_120a) |
| UUID GPU | `GPU-6ad9e3f5-7938-5ee4-b19d-c41f437d6870`, шина `0000:01:00.0` |
| Тактовые потолки | SM **3210 МГц**, память **14001 МГц** |
| Лимит мощности | 525.00 Вт |
| Драйвер | 580.173.02 |
| **CPU** | AMD EPYC 7B12, **256 ядер** |
| **RAM** | 503 ГБ |
| Диск | 122 ГБ, свободно 37 ГБ |
| ОС | Ubuntu 24.04.4 LTS, ядро 5.15.0-187-generic |

**Измеренные свойства карты** (наши прежние замеры, `РЕЕСТР_ПАТЧЕЙ.md`): L2 96.0 МБ,
170 SM, 100 КБ общей памяти на SM, 65536 регистров на SM, шина 512 бит на 14001 МГц →
**1792.1 ГБ/с**. Потолки MMA: bf16 266.1 TFLOPS, f16→f16 531.0, s8 1061.6 TOPS.

## Доступ

```
ssh -i patches_круга_2026-08-31/id_vast -p 29520 root@137.175.22.196
```

Ключ лежит в рабочем каталоге, **в репозиторий не кладётся**. Инстанс также публикует
порты 29532 (→8080), 29574 (→6006), 29594 (→72299); порт 8080 внутри занят
jupyter-notebook, поэтому наш сервер поднимался на **18080**.

Внутри контейнера есть `CONTAINER_API_KEY` — **в репозиторий не переносится и здесь не
приводится**.

## Инструменты сборки

| | |
|---|---|
| CUDA | 13.1, V13.1.115 (`cuda_13.1.r13.1/compiler.37061995_0`) |
| gcc | 13.3.0 (Ubuntu 13.3.0-6ubuntu2~24.04.1) |
| cmake | 3.28.3 |
| EvalScope | 1.10.0, Python 3.12.3 (venv `eval/.venv`) |

## Дерево и двоичные

| | |
|---|---|
| Каталог | `/root/ninfer_d4` |
| База | `5973313d7f1e5dd8b7b74c197278372ecd89a900` — «test(frontend): make fixtures self-contained» |
| Правки поверх | 2 файла: `sparse_moe_prefill.h` (порог 1280), `sparse_moe_prefill_kernels.cu` (плитка 2×2) |
| Двоичные | `/root/bins/CAND/` — `ninfer`, `ninfer-serve`, `ninfer-perplexity`, `ninfer_bench`, `ninfer_sparse_moe_bench`, собраны 2026-09-02 19:15 UTC |

**Резидентность ключевого ядра** `sparse_moe_prefill_q4_gate_up_kernel<8,64>`:
`REG:80 STACK:0 SHARED:33792 LOCAL:0` — регистры ровно в потолке, заданном комментарием
автора, разлива нет.

## Модель

| | |
|---|---|
| Артефакт | `/root/models/qwen3_6_35b_a3b.ninfer`, 22 783 246 080 байт |
| SHA-256 | начинается с `1fb9ea0b5b8561e4` |
| Дата файла | 2026-08-31 12:16 |

Модельная карта: `model-cards/Qwen3.6-35B-A3B-NInfer/`. Опубликованные ею числа —
AIME25 90.00%, AIME26 90.00%, GPQA-Diamond 85.35%. **Наш стенд их воспроизвёл**: bf16
дал 27/30, 27/30 и 168/198.

## Внешние активы, загруженные для кампании

По инструкции самого репозитория (`eval/README.md`):

* корпус NIAH — `AI-ModelScope/Needle-in-a-Haystack-Corpus`, файлы `PaulGraham_Essays.txt`
  и `Journey_to_the_West.txt`, в `/root/.cache/modelscope/datasets/`;
* токенизатор — `Qwen/Qwen3.6-35B-A3B` (только `tokenizer*.json`, `vocab.json`,
  `merges.txt`, `config.json`), в `/root/.cache/modelscope/models/`.

Датасеты AIME25, AIME26, GPQA-Diamond и IFBench уже были в кеше ModelScope.

## Каталоги на сервере

| путь | что |
|---|---|
| `/root/ninfer_d4` | дерево движка |
| `/root/bins/CAND/` | двоичные кампании |
| `/root/models/` | артефакт модели |
| `/root/exp/` | скрипты и журналы кампании (`kv_*.sh`, `kv_*.log`) |
| `/root/exp/kv/` | сырые результаты: CSV, перплексия, журналы серверов и запросов |
| `/root/ninfer_d4/eval/runs/` | 41 прогон оценки, 1.4 ГБ |
| `/root/exp/kv_results/` | собранный пакет результатов (он же в `результаты/`) |
